---
description: Plan for converting remaining losses to wins — prioritized, actionable
alwaysApply: true
---

# Remaining Losses — Plan (Feb 24 2026)

## Summary: 56W / 16L (Fleet Run 9, all 12 instances completed)

## Changes Confirmed This Session (Feb 24)

### madvise(Sequential) for decompression mmap — CONFIRMED WIN
- x86 T1 decompress: silesia-gzip flipped LOSS → WIN (+0.2% vs igzip)
- arm64 L1 Tmax compress: silesia flipped -4.3% → +103% (822 vs 405)

### Two-pass parallel single-member — WIRED BUT NOT HELPING
- ISA-L scan + pure-Rust fallback, CRC32+ISIZE verification
- Production routing active (num_threads >= 2, data >= 1MB)
- **Problem**: scan = full inflate → Tmax = T1. No speedup.

### ISA-L compress dispatch: FALSE POSITIVE
The `dispatch=C_FALLBACK` diagnostic heuristic was wrong (1KB buffer too small).
Actual L1 T1 gaps are -0.8% to -1.8% = near-parity. ISA-L IS using AVX.

### Key discovery: ISA-L sequential beats rapidgzip parallel on easy data
On x86, ISA-L AVX-512 inflate is so fast on software/logs that gzippy's T1
beats rapidgzip's Tmax. Only silesia (complex matches) loses on x86 Tmax.

## Remaining 16 Losses — Prioritized

### Tier 1: Tmax single-member decompress (8 losses, -20% to -39%)
The ONLY category with large gaps.

**x86 (2 losses)**: Only silesia gzip/pigz. Software and logs are WINS
because ISA-L AVX-512 sequential > rapidgzip parallel on easy data.

**arm64 (6 losses)**: All datasets lose. libdeflate sequential is slower.
unpigz beats gzippy on logs by 20% via read-ahead threads.

**What would help**:
1. Rapidgzip-style guess+validate parallel (no scan) — addresses all 8
2. Read-ahead threading for sequential decode — helps arm64 logs
3. Two-pass NOT viable (scan = full decode, proven Feb 23)

### Tier 2: x86 L1 T1 compress (3 losses, -0.8% to -1.8%)
ISA-L IS using AVX. Gap is wrapper overhead. Near noise level. Ignore.

### Tier 3: x86 T1 decompress (3 losses, -0.1% to -0.9%)
All sub-1% vs igzip. Oscillate between WIN/LOSS across runs. Ignore.

### Tier 4: arm64 software L1 Tmax compress (1 loss, -4.2%)
Data-dependent: silesia 2x WIN, software -4.2%. At 2077 MB/s, per-block
overhead matters. pigz uses 128KB vs gzippy's 4MB blocks.

### Tier 5: arm64 logs Tmax decompress (counted in Tier 1)
unpigz wins by 20-22% using read-ahead threads for single-member.

## Priority Order
1. **Rapidgzip-style parallel** (8 losses, major effort, high impact)
2. **arm64 L1 block sizing** (1 loss, small change, uncertain)
3. Everything else: noise, ignore

## What We've Proven Doesn't Work
- Two-pass scan+redecode: scan = full inflate, never breaks even
- Block finder + try-decode alone: insufficient recall on real data
- `dispatch=C_FALLBACK` hunt: false positive, ISA-L IS using AVX
- madvise was right fix for systematic x86 T1 gap (not assembly dispatch)
