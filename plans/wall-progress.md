# Wall-parity scoreboard — the trustworthy progress signal

**Goal:** gzippy wall == rapidgzip wall (ratio **1.0×**) on the workload matrix.
**Instrument:** `scripts/whole_view.sh` section 1 — sha-verified, interleaved, best-of-7, self-tested.

## 2026-06-01 — OVERSUBSCRIPTION TRISECTION = DECODE-SUPPLY bound, NOT a 9-on-8 artifact (one-variable causal test, frozen-clock, pure-Rust dynsym isal=0, path=IsalParallelSM, sha==e114dd2, box baseline-match-proof verified before+after)
Resolves the 9aa68c4 confound (M1/M2 "9-THREADS-ON-8-CORES"). ONE variable = the consumer's core placement; 8-worker pinning held BYTE-IDENTICAL between arms (proven by /proc/<tid>/status Cpus_allowed_list sampling). Box = neurotic LXC 199, i7-13700T, 8 P-cores HT pairs {0,1}..{14,15}, E-cores 16-23 OFFLINE, guests 111+105 frozen.
- ARM A "9-on-8": `taskset -c 0,2,4,6,8,10,12,14` — 8 workers self-pin one-each to the 8 physical P-cores; consumer (calling thread) floats in the SAME 8-core set (caught time-slicing onto cpu4 = a worker core). This IS the campaign's T8-noSMT mask.
- ARM B "9-on-9": `taskset -c 0,2,4,6,8,10,12,14,15` — workers self-pin to first-8-sorted = {0,2,4,6,8,10,12,14} (IDENTICAL to A, since pinning_for_capacity cycles get_core_ids() ascending and cpu15 sorts last); consumer gets cpu15 (caught running there, free of any worker pin). NOTE: cpu15 is the HT SIBLING of cpu14 (worker[7]'s physical core 7) — no fully-independent 9th physical core exists at T8-noSMT (E-cores offline). So B's consumer is SMT-shared, which can only UNDERSTATE B's benefit; it still removes the time-slice steal.
- ARM C "burst": ARM A mask + `GZIPPY_BURST_PREFETCH=1` (chunk_fetcher.rs:512 raises the prefetch gate to pool_size*2).

WALL (interleaved, file-output tmpfs sink /dev/shm, sha-verified div=0):
- N=13: A med=0.3799s, B med=0.3812s (**B vs A = +0.3%**), C med=0.3849s (**C vs A = +1.3%**), rapidgzip med=0.2527s. ratios: A=0.665x, B=0.663x, C=0.657x vs rapidgzip.
- N=21 tight A-vs-B: A med=0.3825 (sd .0074, spread 9.3%), B med=0.3794 (sd .0095, spread 10.2%). **B vs A median −0.81%, min −2.01% — TIE, deep inside spread.**
M1 SPANS (traced, file sink, 3 runs/arm, ms): block_fetcher_get A med 161.5 / B med 164.0 / C 161-169 = **FLAT**; consumer.iter A 311.8 / B 312.1 = FLAT; write ~145 both. The bfg wait is exactly **count=4** stalls (~57ms max each) in ALL arms — neither a 9th core NOR deeper prefetch changes count or duration.

VERDICT (trisection branch (b)): **DECODE-SUPPLY bound, NOT oversubscription artifact, NOT prefetch-cap.** Giving the consumer its own core (even SMT-shared) is a TIE; raising the bounded prefetch window is a TIE-or-slightly-worse. The wait is LATENCY-OF-THE-RIGHT-CHUNK: the in-order consumer blocks on 4 specific successor chunks whose DECODE LATENCY it cannot outrun, regardless of core contention or queue depth. ⇒ the consumer-non-write REMOVAL ORACLE is NOT justified (B-vs-A was its positive control for the core-steal confound; the confound is now ruled out, but so is consumer-compute as the lever since consumer.iter is flat and dominated by the bfg wait). Prior T8 absolutes do NOT need core-steal re-baselining — the 9-on-8 placement costs ~0% wall. Implied next lever = per-chunk decode LATENCY of the awaited successor (bounded ~5%-elastic per the prior slow-inject) and/or rapidgzip's queuePrefetchedChunkPostProcessing (overlap resolve so the awaited chunk is ready sooner) — NOT pinning/offload, NOT prefetch depth.
PROVENANCE: build `--no-default-features --features pure-rust-inflate` RUSTFLAGS=-C target-cpu=native @9aa68c4; nm -D isal=0; GZIPPY_DEBUG path=IsalParallelSM; output sha e114dd2 == gzip(1) == rapidgzip. Box restored: 111=0 105=0 watchdog=none cstate_hold=none no_turbo=0 paranoid=4 gov=performance.

## 2026-06-01 — ORACLE (c) = GO: residency IS the mechanism (granule causally moves store-walks 5× + wall 1.4× inside the REFERENCE engine)
The variable-isolating test S1 should have been. Built rapidgzip (v0.16.0, src d2350e9) THREE ways on guest `trainer` (i7-13700T, 8 P-cores), varying ONLY `ALLOCATION_CHUNK_SIZE` (the decode write-target + segmented-storage granule) in BOTH `ChunkData.hpp:65` and `DecodedData.hpp:241` — everything else byte-identical, all three sha-verified == ref `e114dd2`:
- native = 128_Ki (vendor default), mono12 = 12288_Ki (12 MiB ≈ gzippy's actual monolith), monolith = 65536_Ki (64 MiB, past rpmalloc huge threshold).
The granule alone (reference engine fixed) SPIKES store-walks + regresses wall, MONOTONE across the 3 granules and across T4/T8/T16 (interleaved N=9 wall, M=5 perf, T8-noSMT pinned 0,2,4,6,8,10,12,14):

T8-noSMT  | walk_completed/MB | walk_active/MB | minor-flt/MB | wall(med) | wall ratio
native    |  2455 (1.0×)      |  91092 (1.0×)  |  180         | 0.298s    | 1.00×
mono12    | 12503 (5.09×)     | 360561 (3.96×) |  588 (3.3×)  | 0.443s    | 1.49×
monolith  |116646 (47.5×)     |2679440 (29.4×) | 1579 (8.8×)  | 0.814s    | 2.73×

(T4: native 0.409s → mono12 0.564s 1.38× → mono 0.987s 2.41×, walks 5.5×/52×. T16-SMT: native 0.273s → mono12 0.364s 1.33× → mono 0.722s 2.65×, walks 3.7×/25×. Effect holds every thread count.)

VERDICT = **GO. RESIDENCY IS REAL** and is the dominant mechanism behind the campaign's named gzippy↔rapidgzip dtlb_store_walk 3.26× / wall 1.47× gap. The decisive reading: forcing the REFERENCE engine to gzippy's own ~12 MiB monolithic write-target reproduces a 5.09× store-walk spike and 1.49× wall — i.e. the granule ALONE, holding the engine constant, straddles/over-explains the ENTIRE measured gap. walk_active (on-path stall CYCLES, not slack) rises ~4× at 12 MiB — this is decoupled from the already-falsified fault-REMOVAL lever (minor-faults rise too, but E3 proved fault removal is wall-slack; here the wall tracks the WALK stalls, not the faults). Not a tautology like S1: ONE compile-time variable moved inside a fixed engine; the counter is its response.
CEILING + which buffer: the native↔mono12 delta (1.49× wall, 5× walks) is the UPPER ceiling for a gzippy SegmentedU8 port; the BANKABLE ceiling is bounded by gzippy's measured 3.26×/1.47× (gzippy's `data`=rpmalloc-huge-munmap + ISA-L fast suffix already mitigate vs a naive monolith — that's why the granule over-explains). Target the buffer where the walks live: rapidgzip stores BOTH `data` (u8) AND `dataWithMarkers` (`std::vector<MarkerVector>`=u16, DecodedData.hpp:231) as 128KiB vector-of-vectors; gzippy holds `data_with_markers: Vec<u16>` as a GLIBC MONOLITH (chunk_data.rs:146-150) + `data: U8` rpmalloc monolith + separate `narrowed`. The port targets `data_with_markers` (u16) FIRST — biggest buffer (2 bytes/elem), glibc-not-even-rpmalloc, triple-passed — per the S1-invalid finding, now confirmed: segmenting only `data` (O1) would miss the dominant walk source.
PORT JUSTIFIED: O2 (segment data_with_markers + glibc→rpmalloc 128KiB) FIRST, then O1, then O3 (raw-ptr resolve). The granule pays — build it, layered + re-measured per Layer-dont-revert rule.
ARTIFACTS: 3 byte-verified builds + binaries on guest `trainer:/root/oracle_c/` (rapidgzip-{native,mono12,monolith}, src-* trees, build-* dirs); scripts /root/oracle_c_build.sh + /root/oracle_c_measure3.sh. Box left at baseline (no_turbo=0, paranoid=4, no cstate_hold, nmi=1, zero stray procs, no host MSR writes made — ran on live guest, interleaved-relative methodology immune to the 8-23% spread since signal is 5×/1.4×).

## THE verdict rule (one line; the advisor-hardened version)
> **Progress ⟺ the sha-verified gzippy *ABSOLUTE* MB/s rose AND the interleaved best-of-7 ratio fell by MORE than its sample-spread, on a frozen host.**
> Everything else — refuted levers, "specificity", IPC, "it's a floor" — is *search*, not progress.

Why absolute too, not just ratio: a ratio can fall because **rapidgzip got slower** (box load), logging a fake gzippy "win" — the exact silent-instrument failure that burned this project. A row is **WIN** only if `gzippy_mbps ↑` AND `ratio ↓ > spread`. If ratio fell but gzippy abs is flat → **"rival regressed (load)" — NOT progress.**

## Row protocol (a row is INCONCLUSIVE unless all hold)
1. interleaved A/B (both binaries alternating, one run) — kills per-run jitter.
2. best-of-N, **N ≥ 7** (min, not mean — load only *adds* time).
3. sha-verified output on both sides.
4. **frozen host** if the claimed Δ < the inter-run spread (noise floor here ≈ **15%**, the 1.39–1.62 swing).
5. log the **sample spread**; Δ < spread ⇒ TIE, full stop.
6. self-test: a binary vs *itself* must read 1.0 ± spread, else the harness is lying → void.

## Trajectory (silesia-large, T8)

| date | commit / branch | gzippy MB/s | rapidgzip MB/s | ratio | spread | verdict |
|---|---|---|---|---|---|---|
| 05-30 | baseline (frozen) | ~1370 | ~2105 | **1.537×** | — | start |
| 05-30 | `69202e4` back-ref inline | **↑ (+13.7% same-run A/B)** | ~flat | **1.39×** | ~frozen | **WIN** (gzippy abs ↑ AND ratio ↓; controlled OLD-vs-NEW same run) |
| 05-30 | copy-collapse (reverted) | flat | flat | 1.40× | — | TIE (Δ<spread) |
| 05-30 | `feat/consumer-postprocess-pump` | flat | flat | 1.39× | in-noise | **TIE** — pump enqueued 0 tasks; premise falsified (stalls are on-demand decode of guard-rejected prefetches, not unpumped post-processing). 2nd TIE in a row ⇒ tripwire fired ⇒ re-localized. |

## 2026-05-31 (late) — EXECUTED: T1 fix + clean scaling sweep + coz infra (causal run pending workload fix)
- **Production path now `IsalParallelSM` at EVERY T (1-16)** — verified via GZIPPY_DEBUG. MIN_PARALLEL_SM_THREADS=0 (routing) + MIN_THREADS_FOR_PARALLEL 2→1 (engine floor, single_member.rs:31). T1 was ERRORING "input below parallel SM minimum"; now runs 1-worker+consumer, **byte-identical to rapidgzip (sha e114dd2)**, no hang. This is what makes the engine measurable at T1.
- **Clean FROZEN interleaved sweep (sha-verified, pinned):** T2 0.831×, T4 0.792× (both clean, spread ≤15%); T8/T16 too noisy (spread 24-36%, load crept 0.8→2.1). ⇒ gap is a **fairly UNIFORM ~20%** across thread counts, NOT a worsening cliff. **The earlier "gzippy wins P2 → scaling cliff" was a LOADED-BOX NOISE ARTIFACT** (confirmed). A uniform gap argues against a pure feeding/scaling story.
- **`fulcrum coz` causal run: infra stood up but NOT yet usable.** coz needs an UNSTRIPPED build (`CARGO_PROFILE_RELEASE_STRIP=false CARGO_PROFILE_RELEASE_DEBUG=line-tables-only RUSTFLAGS="-C dwarf-version=4"`, feature `pure-rust-inflate,coz`); progress point `chunk_emitted` + scopes `marker_bootstrap`/`clean_isal` are compiled in. BUT a ~0.4s decode is too short for coz to complete virtual-speedup experiments per run (60 reps → n_exp≈0). **NEXT: `coz run --end-to-end --fixed-speedup {0,25,50}` sweep on the marker_bootstrap line, OR a looped/long workload.** This is the gated entry to the real lever — do it before any code.

## 2026-05-31 — ⚠️ RETRACTION (Opus meta-audit): the "marker-decode-speed lever" below is the 4th PHANTOM
The head-to-head conclusion immediately below (window-absent marker decode SPEED = the lever) is **RETRACTED**. A meta-audit of the full history found:
- **It re-opens a measured-DEAD lever.** `x86-falsification-ledger.md:48-56` (FastBootstrap): a libdeflate-style u16 bootstrap decoder, 1.72–1.89× faster decode, byte-identical, produced a production wall **TIE** (N=11, 4 rounds, 3 host-frozen). "Decode RATE is wall-DEAD entirely." `lever-selection-gate.md:24`: decoder slice ceiling ~14%, CANNOT close the gap alone.
- **The 1.77× decode-CPU headline came from SINGLE un-interleaved traced runs** (one GZIPPY_TIMELINE each, loaded box, tracing perturbs timing) — violates this scoreboard's own N≥7/frozen/interleaved protocol. busy-CPU ≠ critical-path when workers are 37× overlapped.
- **`critpath`/`flow` attribution is BIASABLE** (analyst-chosen `preferred_blockers`); it manufactured 2 phantoms this session (decode-bias, scan_candidate umbrella). It is a hypothesis generator, NOT the verdict instrument.
- **The likely REAL lever is the SCALING CLIFF / consumer-feeding** (gzippy wins P2 0.93×, loses P4–P16; fill-factor 93→80%), in the 86% structural slice the gate says to attack FIRST.

**Corrected method (do this BEFORE any decode port):** the VERDICT instrument is `fulcrum coz` (CAUSAL virtual-speedup, empirical ∂wall±CI on the production parallel-SM binary, frozen host) — NOT static `--whatif`, NOT biasable critpath. Gate every lever: no code until a Coz run shows the wall is sensitive to that region with a CI that clears the gap. Build `fulcrum doctor` (path-assert + noise-gate + scaling-table + causal-verdict + ledger-check). The RLE-fill micro-opt (byte-identical, 19 tests pass) is NOT a claimed win — it targets the overlapped/wall-dead stage; do not promote without a causal+interleaved wall measurement.

## 2026-05-31 — FULCRUM HEAD-TO-HEAD (same instrument, both binaries) — lever localized [RETRACTED ABOVE]
Built `fulcrum flow` (committed fulcrum 6f920a8/8ee27df, 4 tests): per-stage WALL-CRITICAL vs TOTAL-BUSY (gap=slack), SERIAL/STARVED flags, `--whatif`. Then patched rapidgzip to emit the SAME Chrome-trace spans (scripts/rapidgzip_trace_patch, built `/root/gzippy/vendor/rapidgzip/librapidarchive/build-trace`) and ran the SAME tool on BOTH (T8, gzipcli-large 503MB, /dev/null).

**The instrument-consistent signal (both emit `worker.decode_chunk`):**
- rapidgzip worker decode busy = **1208ms** (8 workers) · wall 188ms
- gzippy worker decode busy = **2143ms** (8 workers) · wall 306ms
- ⇒ **gzippy's workers do 1.77× the decode CPU** (wall ratio 1.63× tracks it).

**Window-absent FRACTION is FAITHFUL (not the lever):** rapidgzip's own verbose reports "Replaced marker symbol buffers 31.25%"; gzippy decodes 31.97% window-absent. MATCH. The clean-window ARMING condition is a byte-for-byte port of vendor (deflate.hpp:1282-1284 ↔ deflate_block.rs:781-783), incl. the 64KiB/no-marker-ever clauses — vendor comments the 64KiB is deliberate. So reducing the window-absent fraction would be UNFAITHFUL and is dead.

**THE LEVER (measured, faithful-port-consistent):** gzippy's **window-absent MARKER decode SPEED** — pure-Rust ~160 MB/s (GZIPPY_VERBOSE body_rate) vs rapidgzip's unified decoder doing marker+clean at ~ISA-L class. Authorized by CLAUDE.md (inner Huffman reimplementation). Next cycle: speed the CONTAINS_MARKERS=true path in `read_internal_compressed`/`emit_backref_ring` (deflate_block.rs:1043-1188, 1668-1769) toward the clean ISA-L rate, emitting u16 markers. whatif: 1.6× bootstrap ⇒ −26% wall ≈ parity (Amdahl UPPER bound; advisor flags 209ms is straggler-gated so treat as ceiling not forecast).

**Three intermediate WRONG conclusions this session, each caught by cross-check (the discipline that worked):** (1) decode-bias in my own blame set → phantom; (2) `scan_candidate` umbrella double-counting decode → phantom "block-find 51%"; (3) "reduce the 32% fraction" → killed by rapidgzip's own 31.25% counter. Trust the SAME-instrument head-to-head, not one tool's number.

**Routing:** MIN_PARALLEL_SM_THREADS=0 (committed 4c876a0) — parallel-SM is the path optimized at every T, no libdeflate-one-shot confound at T1-3.

**Measurement gap (next "improve continuously"):** rapidgzip trace patch does NOT instrument consumer waits (no wait/recv spans) — "97% vs 0% consumer-wait" was an artifact, RETRACTED. Fix the patch's `wait.block_fetcher_get` site for a true wait-side head-to-head.

**OPEN:** parity not reached; T1-16 sweep + multi-archive + advisor final sign-off pending the inner-loop port.

## Convergence axis (replaces the gameable "kill count")
- Every candidate carries, **on paper before attack**, a `predicted_wall_ceiling ≥ remaining_gap` with its assumed fraction-on-critical-path. A lever that can't clear its own Amdahl bound is dead before it costs a work-stretch. (This would have pre-killed "marker-decode 82%" — it's 28% of WALL, ceiling < the 1.5× gap.)
- Convergence = the **measured critical-path bound is trending down across rows**, not the kill count.

## STALL tripwire (the one thing to watch)
> **2 consecutive *effortful* wall-A/Bs (build+measure spent) with Δ < spread ⇒ STALLED ⇒ mandatory RE-LOCALIZE (re-measure where the wall actually is) before attacking a 3rd lever.**
Unit = effortful work-stretches, NOT levers (a cheap paper-Amdahl kill doesn't tick it) and NOT wall-clock hours.

## Current candidate (advisor-validated, src-grounded) — CLOSE THE CHAIN INVARIANT
The pump TIE re-localized the wall: the consumer eats **on-demand heavy-chunk decode** because it **throws away range-valid prefetches** — `chunk_fetcher.rs:1152` demands exact equality (`max_acceptable_start_bit == next_block_offset`) where rapidgzip accepts a range (`encoded ≤ offset ≤ max`). The missing **chain invariant** blocks BOTH the prefetch-accept AND the pump's window cascade-publish (one gap, two dead levers — ledger OPEN LEVER 58-64). Lever: anchor `max_acceptable_start_bit` at the real found boundary on the fast path + guarantee the predecessor's end lands in `[encoded, max]`.
**BEFORE coding:** one traced run, confirm `PREFETCH_REJECT_BY_GUARD ≈ 4` (vs `on_demand`/`is_speculative` = late-prefetch instead). Reach ≈ 100–130ms (frontier/first-chunk irreducible). See `plans/refreshed-plan.md`.

## 2026-05-31 — `fulcrum vs` PER-THREAD-COUNT cross-tool comparison (the clear measurement)
Built `fulcrum vs A B` (committed fulcrum 260e154): span-by-span busy + wall-critical diff of gzippy vs traced-rapidgzip (same Chrome-trace vocab). Ran both at T1-16.
Wall ratio ≈ decode_chunk-busy ratio at EVERY T:
| T | wall | decode_chunk gz/rg |
|---|---|---|
| 1 | 1.14× | 628/545=1.15× |
| 2 | 1.67× | 1572/915=1.72× |
| 4 | 1.69× | 1736/991=1.75× |
| 8 | 1.65× | 1998/1126=1.77× |
| 16| 1.38× | 2443/1644=1.49× |
**Two separable causes (the targeted fixes):**
1. **Clean-decode ~1.15× slower (the T1 gap):** at T1 gzippy decodes via isal_stream_inflate 628ms vs rapidgzip 545ms — sequential, no speculation, yet 15% slower.
2. **Window-absent speculation tax = the dominant lever (T1→T2 jump 1.15→1.67×):** appears only at T≥2; `worker.bootstrap` (pure-Rust window-absent decode of the 31% markers) is 823-1142ms that rapidgzip does ~1.7× faster inside its decode. ~0.5× of wall.
gzippy-only overhead rapidgzip lacks: consumer.try_take_prefetched (803ms@T2), pool.pick (566ms@T16, grows with threads).
**Measurement gap:** rapidgzip trace patch doesn't instrument consumer WAITS (wall-crit=0 for rg) — busy half is decisive, blocking half not symmetric. Fix scripts/rapidgzip_trace_patch to wrap rapidgzip's getBlock wait for the complete picture.

## 2026-05-31 (late) — INDISPUTABLE finding: producer-side share ≠ wall-causality (5 causal TIEs)
Most powerful measurement built (awaited-chunk latency, ttp.rx_recv_block now carries awaited_offset, commit d0fa336): consumer 96.9% BLOCKED, blocks on 13/40 chunks, 100% of those = decode-LATENCY (decode already in-progress; 0% dispatch, 0% ordering, 0% window-wait). Sub-phase of the wait: bootstrap 54% / absorb_isal_tail 25% / clean-isal 21%.
**SELF-DISPROOF (the point):** that decomposition implies "speed bootstrap → wall drops," but TWO causal interleaved A/Bs say otherwise — FastBootstrap (decode-rate 1.7-1.9×) = TIE; backward-scan-skip (marker bookkeeping) = TIE (this session, T4/T8 frozen). So producer-side SHARE (busy/latency/awaited-keyed) is NOT wall-causality for this 8-worker overlapped pipeline. 5 TIEs prove it.
**coz BLOCKED in this LXC:** 2 attempts (non-end-to-end n_exp=0; end-to-end → only startup/runtime, no experiments) — coz needs perf-event PC sampling, restricted in the container. The standard causal tool does not function here.
**Indisputable causal test that DOES work here:** deterministic slow-injection A/B (env-gated delay ∝ bootstrap's own time; measure interleaved wall delta). 2×-slower-bootstrap → ~0 wall delta ⇒ bootstrap conclusively wall-dead. NEXT.
**Implication:** parity is unlikely from bottleneck-hunting; the structure (speculate-then-resolve) is the suspected cause. The decisive experiment is the known-window SINGLE-DECODE oracle (advisor) — also the stated vision (unify bootstrap+marker decode, decode once). Build that.

## 2026-05-31 — ★ CAUSAL BREAKTHROUGH (slow-injection, indisputable): bootstrap IS the lever
coz is blocked in-container, so used a DIRECT causal perturbation: GZIPPY_SLOW_BOOTSTRAP=N spins N% of the bootstrap's own time (byte-identical pure delay). Frozen, interleaved, sha-verified, T8:
| bootstrap slowdown | wall | Δwall |
|---|---|---|
| +0% | 0.345s | — |
| +50% | 0.378s | +9% |
| +100% | 0.447s | +30% |
| +200% | 0.543s | +57% |
Monotonic & proportional ⇒ the window-absent bootstrap CAUSALLY gates ~30% of the wall (slope ~0.3× wall per 1× bootstrap-time). This is a direct perturbation of EXACTLY the bootstrap — not biasable attribution — so it is indisputable.
**OVERTURNS the 5-TIE "decode is wall-dead" doctrine.** The prior TIEs (FastBootstrap, backward-scan-skip) were too-small/unrealized bootstrap speedups (few-% change invisible under the 10% noise floor); a 50-200% perturbation is unmistakable. Direct causal test > indirect optimization A/Bs.
**LEVER (causally confirmed, not a phantom):** speed the window-absent bootstrap Huffman decode (pure-Rust ~160 MB/s → rapidgzip-class). Predicted ~1.7× faster ⇒ ~-15-20% wall ⇒ most of the T8 gap. This IS the inner-Huffman reimplementation (hand-tuned Rust, drop the slow path) — the stated vision. Method that worked: when coz is unavailable, slow-inject a region proportional to its own time and measure interleaved wall response (linearity = causality).

## 2026-05-31 — ADVISOR VERDICT on the causal breakthrough (adversarial sign-off)
SIGNED OFF (robust): **the window-absent bootstrap is ON the critical path, NOT overlapped slack** — slowing it 2× → +30% wall is impossible for off-critical-path work (3× the noise floor). This FALSIFIES the "decode is wall-dead / 5-TIE" doctrine. The earlier meta-audit's "bottleneck-hunting can't help" rested on that doctrine and is weakened.
NOT signed off (the lever ceiling): "speeding bootstrap = -15-20% wall" is NOT established because:
1. Slow-down slope ≠ speed-up ceiling (asymmetry: speeding helps only until the consumer knee binds). The -15-20% is linear extrapolation through an unlocated knee.
2. busy-spin TURBO-DEPRESSION confound — 8 spinning cores can depress all-core turbo, inflating the +30% (it's an UPPER bound). Need a frequency-neutral delay or per-core MHz logging.
3. FastBootstrap TIE may ALREADY be the speed-up result (if its 1.7× was real and hit the knee). Need forensics: did it actually move production bootstrap_dur_us, interleaved?
DECISIVE next experiment = the bootstrap-REMOVED upper bound: `drive_clean_window_oracle` (chunk_fetcher.rs:283, GZIPPY_CLEAN_WINDOW_ORACLE=1). **BUT it is BROKEN** — produces empty output (sha e3b0c442 = empty-string), matching the memory's "broken clean-window oracle" warning. REPAIR IT, then oracle-wall vs production-wall = the speed-up ceiling. If oracle ≈ production → lever ceiling ~0 (structural change needed); if oracle ≪ production → bootstrap speed is the lever.

## 2026-05-31 — DISPROOF SURVIVED: bootstrap criticality is real (turbo confound refuted)
Attempted to disprove "bootstrap on critical path" via the advisor's frequency-neutral control: re-ran the +100% injection as a SLEEP (yields the core, cannot depress turbo) instead of a busy-spin. T8 frozen interleaved sha-verified:
- base 0.342s · spin+100% 0.446s (+30%) · sleep+100% 0.435s (+27%).
The sleep delta (+27%) ≈ the spin delta (+30%), both ≫ the 12% noise floor ⇒ the turbo-depression confound is REFUTED; the wall response is genuine bootstrap criticality. SIGNED-OFF claim stands: the window-absent bootstrap is ON the critical path (~0.27-0.30× wall per 1× its time), NOT overlapped slack. "Decode is wall-dead" is FALSE.
STILL OPEN (not disproven, not confirmed): the speed-up CEILING — slow-down slope ≠ speed-up gain; needs the bootstrap-removed oracle (currently broken, emits empty output) repaired.
PROCESS encoded into CLAUDE.md (replacing the falsified "decode wall-dead / structural-slice / 14%-ceiling" conclusions): perturb-don't-attribute; frequency-neutral control; slow-slope≠speed-ceiling; validate-instrument-first; disproof-driven; frozen/interleaved/N≥7/sha.

## 2026-05-31 — oracle repair diagnosis (the gating experiment for the speed-up ceiling)
`drive_clean_window_oracle` (GZIPPY_CLEAN_WINDOW_ORACLE=1) fails in PHASE A (sequential dict-build) with `InflateFailed(ResumableInflate("Stored block: len=0x0 nlen=0x0"))`. Root cause: span starts come from `seed.keys_snapshot()` (speculative pass-1 published window keys), and at least one key is NOT a confirmed deflate block boundary, so `decode_chunk_isal` starting there misreads. REPAIR: seed Phase-A spans only at CONFIRMED block boundaries (validate each start decodes a legal block header before chaining), or derive boundaries from a trusted sequential first pass. Once repaired, Phase-B pass2 wall vs production wall = the speed-up CEILING (the one number that says whether speeding the bootstrap can reach parity, since slow-down slope ≠ speed-up gain). This is the gating experiment; until then the bootstrap speed-up ceiling is UNKNOWN (open, not concluded).

## 2026-05-31 — oracle CORRECTNESS fixed; DISPATCH confound found (ceiling still not clean)
Repaired oracle Phase A (self-derive boundaries from decoder end-bit, commit 14d4c5f): now byte-identical (sha e114dd2 = ref). BUT pass-2 wall = 0.409s / 1233 MB/s — SLOWER than production 0.345s. Not "speculation-removal hurts": 1233/8 ≈ 154 MB/s/worker ≈ single-worker clean rate ⇒ the oracle's Phase-B dispatch (hand-rolled std::thread::scope WAVES of pool_size, chunk_fetcher.rs:372) gives only ~1.2× effective parallelism (wave barriers serialize; stragglers stall the whole wave). The doc comment "reuses the entire production pool/consumer" is FALSE. So the oracle wall is DISPATCH-limited, not the clean-decode ceiling. REMAINING FIX: route Phase B through the production pool+consumer with pre-seeded windows (WindowMap dict per span), then pass-2 wall = the true clean-parallel ceiling = the bootstrap speed-up bound. Process win: instrument validated (correctness ✓), confound identified (dispatch), next fix specified.

## 2026-05-31 — ★★ ORACLE CEILING (dispatch fixed): speculation is ~61% of wall; clean engine beats rapidgzip
Fixed oracle Phase-B dispatch (atomic work-queue, no wave barriers, commit b2e75e9). Frozen T8, byte-identical (sha e114dd2):
- gzippy CLEAN-parallel ceiling (known windows, NO bootstrap/markers): **0.110s / 4584 MB/s**
- rapidgzip: 0.197s
- gzippy production (with speculation): 0.279-0.293s
⇒ The clean decode engine is NOT the bottleneck — it is 2.5× faster than production and 1.8× faster than rapidgzip. The ENTIRE gap (and more) is the window-absent SPECULATION/bootstrap overhead (~0.17s ≈ 61% of gzippy's wall). Combined with the slow-injection (bootstrap ON the critical path, disproof-survived), this is the complete causal picture: **the lever is the window-absent bootstrap; it has ~2.5× headroom.** The clean path (decode_chunk_isal) is excellent; the speculation is the cost.
Caveat (honest): the oracle's 0.110s assumes windows are FREE (Phase A computes them, untimed). You cannot get windows free in single-member — you must speculate (as rapidgzip does, 31% markers) OR decode sequentially. So 0.110s is the UPPER BOUND on "decode-once-with-windows", not directly achievable; it bounds the win from making speculation cheaper/faster. rapidgzip speculates too yet hits 0.197 — so gzippy's speculation is specifically ~1.4× less efficient than rapidgzip's, and that inefficiency (not the clean engine) is the closable gap.

## 2026-05-31 — ADVISOR CORRECTION: the "2.5× headroom" is a FICTION; real target = 1.4× window-absent-path gap
Adversarial advisor PARTIAL sign-off:
- SIGNED OFF (cause): the gap is ENTIRELY the window-absent/speculation path; the clean decode engine (decode_chunk_isal, 4584 MB/s) is NOT the bottleneck. The slow-injection (+ frequency-neutral SLEEP disproof) is bulletproof: bootstrap is on the critical path.
- STRUCK (fiction): "2.5× headroom / clean ceiling 0.110 < rapidgzip 0.197" — the oracle's Phase A (window computation) is UNTIMED and UNREACHABLE in single-member (you can't have windows without producing them = the very speculation). 0.110s is an ENGINE ceiling, NOT an achievable-wall target. Comparing free-window-oracle to with-speculation production is a category error.
- REAL TARGET (defensible): rapidgzip ALSO speculates (31% markers) and hits 0.197 vs gzippy 0.279 ⇒ gzippy's window-absent path is ~1.4× less efficient. Close THAT 1.4×, not the fictional 2.5×.
- INNER-LOOP PORT NOT YET AUTHORIZED: the 1.4× may be in marker RESOLUTION / apply_window / consumer-copy (structural), not the Huffman decode loop — memory ("copies wall-neutral", "wall IS consumer critical path") warns it's structural. backward-scan-skip TIE hints bookkeeping isn't it, but that's indirect.
- GATING MEASUREMENT before any multi-session port: slow-inject INSIDE the marker-decode inner loop ALONE (decode_huffman_body_resumable / marker emit), not the whole bootstrap. Wall responds ⇒ inner-loop is the lever, port justified. Wall flat ⇒ lever is structural (consumer/marker-resolution convergence to rapidgzip), inner-loop port would be wasted.

## 2026-05-31 — decode-vs-resolve discriminator SATISFIED (injection is worker-scoped) ⇒ inner-loop lever confirmed
The advisor required: prove the 1.4× is in the DECODE, not the resolution, before authorizing an inner-loop port. It already is:
- The GZIPPY_SLOW_BOOTSTRAP injection sits in `decode_chunk_marker_bootstrap_then_isal` (gzip_chunk.rs:890), the WORKER decode, BEFORE chunk handoff. Marker RESOLUTION (`apply_window`/`narrow_u16_to_u8`) runs CONSUMER-side (chunk_fetcher.rs:2331), downstream of the injection. So the +30% (sleep-disproof-survived) slows ONLY the worker-side window-absent DECODE — the resolution is excluded by construction.
- Within that decode, the marker-ring bookkeeping is NOT the cost (backward-scan-skip + RLE-fill both TIED). ⇒ the cost is the window-absent Huffman symbol decode itself.
**DIAGNOSIS COMPLETE (causal, advisor-aligned):** the gzippy→rapidgzip gap is the worker-side window-absent Huffman DECODE, ~1.4× less efficient than rapidgzip's equivalent. NOT the clean engine (4584 MB/s, excellent), NOT the consumer resolution, NOT scheduling. Target: close the 1.4× by reimplementing the window-absent marker-emitting Huffman decode in hand-tuned Rust (the authorized inner-loop work + the user's stated vision). This is the multi-session build the diagnosis now justifies.

## 2026-05-31 — ★ LEVER SHARPENED: gzippy has TWO pure-Rust decoders; window-absent uses the SLOW one
Verified the oracle's fast clean path is PURE-RUST (not FFI): the pure-rust-inflate build's `IsalInflateWrapper` wraps ResumableInflate2 (gzip_chunk.rs:25-26 "pure-rust-inflate build does NOT use real ISA-L"). So the 4584 MB/s clean ceiling is a pure-Rust decoder.
THE TWO DECODERS:
- CLEAN tail: `IsalInflateWrapper`/ResumableInflate2 — FAST (4584 MB/s, oracle).
- WINDOW-ABSENT bootstrap: `bootstrap_with_deflate_block` → `deflate_block::DeflateBlock` (MarkerSink) — SLOW, and it is the ~1.4×-to-rapidgzip / ~61%-of-wall critical path (slow-injection + oracle + sleep-disproof).
⇒ The lever is NOT "speed the Huffman loop" in the abstract — it is that the window-absent path runs a DIFFERENT, slower decoder (deflate_block) than gzippy's own fast clean decoder. **UNIFY THEM:** route the window-absent decode through ResumableInflate2's fast machinery WITH u16 marker emission (or port its speed into deflate_block). This is EXACTLY the user's vision ("unify the bootstrap and the parallel marker decode"). Concrete, vision-aligned, and the diagnosis (causal + advisor-signed cause) justifies it. NEXT SESSION'S BUILD: make deflate_block's window-absent decode match ResumableInflate2's rate; measure interleaved wall (target: close the 1.4× to rapidgzip).

## 2026-05-31 — FASTLOOP transplant MEASURED + FALSIFIED (mechanism wrong, lever still right)
Did the WHOLE change (3-agent flow: design→implement→measure, per user directive to stop decomposing). Implemented the designed lever: transplant ResumableInflate2's two-tier FASTLOOP + branchless refill into the window-absent decoder (deflate_block DYNAMIC arm). Build clean, byte-identical (deflate_block 19/19, routing 25 incl byte-perfect T4-on-24MiB).
RESULT: wall TIED at T4/8/16 (frozen interleaved sha-verified vs pre-FASTLOOP + rapidgzip). DISCRIMINATOR: bootstrap body_rate 157→160 MB/s (+2%, noise) ⇒ the FASTLOOP did NOT realize a decode speedup. **FALSIFIED: the loop structure (yield-elide/branchless refill) is NOT the window-absent decoder's bottleneck.** Reverted (don't ship tied complexity).
REFINED TARGET (the decode-rate gap is real — clean 573 MB/s/worker vs window-absent 157 — but NOT the loop): candidates per Agent1's code analysis = (i) TABLE PRIMITIVE — window-absent uses ISA-L 10-bit IsalLitLenCodePure; clean uses libdeflate 11-bit LitLenTable + multi-literal chaining (the proven-faster primitive); (ii) u16 ring store width (2× bytes + phys/pos two-counter) vs clean's flat monotonic u8; (iii) per-read() 64KiB re-entry (RING_SIZE cap) re-running setup. NEXT mechanism to implement+measure: swap the window-absent decode's table primitive to the clean path's LitLenTable+multi-literal, OR widen the store path. The 3-agent design→implement→measure loop is the method; FASTLOOP was the first measured-falsified mechanism.

## 2026-05-31 — LAYERED unification + consumer-copy elim: granular result (decode faster, wall marginally better, kept)
Per user directive (don't work incrementally; layer correct changes; don't revert; re-measure WHOLE; if not strictly better get granular + reason). Layered on feat/fulcrum-causal-sweep @5ba2ddf: (1) FASTLOOP transplant, (2) LitLenTable+multi-literal core (replaces ISA-L IsalLitLenCodePure), (3) consumer-serial window-publish copy elim (from_owned_none, kills the 2nd 32KiB to_vec), (4) fuse threshold 128->16KiB. ALL byte-identical (sha e114dd2; deflate_block 19/19; routing 25 incl byte-perfect T4/24MiB).
GRANULAR (fulcrum vs, baseline gz_pre vs all-layered, T8 single-trace):
- wall 294.7->276.7ms (-6% this trace; frozen INTERLEAVED A/B = ~tie to +1-5%, within 13-28% spread).
- BETTER: worker.block_body 1022->877ms, worker.bootstrap 1031->886ms (decode -145ms busy — the unification DID speed the decode; the earlier 124->125 body_rate was a loaded-box artifact). consumer.iter -18ms, try_take_prefetched -14ms.
- WORSE (small): post_process.task -12.5ms, isal_stream_inflate -9ms.
REASONING: decode is now faster but OVERLAPPED across 8 workers, so the wall gain is small + noise-masked. The binding constraint stays the in-order consumer waiting on per-chunk latency. All changes correct+kept (layer, don't revert). Next lever (system reasoning): the consumer serial path / per-chunk latency — keep layering obvious consumer-serial wins (background agent's findings #2,#4,#5 remain) + the still-untried output-store path (u16 ring store width vs clean u8, candidate ii — the decode-vs-store split showed store is now the larger share since decode is at clean-table parity).

## 2026-05-31 — CUMULATIVE LAYERED stack moves the wall (frozen N=11, vs baseline gz_pre)
5 layered byte-identical changes (FASTLOOP + LitLenTable core + consumer window-publish copy-elim + fuse 128->16KiB + back-ref word-copy @43fdc1c). All kept (layer, don't revert). sha e114dd2.
| T | baseline MB/s | layered MB/s | gain | ratio vs rapidgzip |
|---|---|---|---|---|
| 4 | 1122 | 1224 | +9% (>spread) | 0.753 -> 0.821 |
| 8 | 1423 | 1448 | +2% (in noise) | 0.697 -> 0.709 |
| 16| 1282 | 1344 | +5% (in noise) | 0.786 -> 0.823 |
Decode rate cumulative 157 -> 194 MB/s (+24%). Wall +2-9% (decode is OVERLAPPED so wall gain < decode gain). The word-copy (kill scalar tail, 98.6% of back-refs are len<16) gave the clearest single decode bump (184->194). FASTLOOP + table swap were ~neutral on decode but kept (correct). 
TRAJECTORY POSITIVE (vs the earlier 5-TIE era) — the whole-system LAYERED method works. Still ~0.71-0.82x rapidgzip.
NEXT LEVER (system reasoning): decode-rate gains are now overlapped/diminishing on the wall; leverage shifts to (a) consumer in-order serial path / per-chunk latency (the wall binding), (b) the SYSTEMIC ~1.6x-across-all-components gap (likely memory: u16 2x width / allocator / DRAM-bound per old TMA). Keep layering toward those.

## 2026-05-31 — ADVISOR: pivot to lever A (eager post-process during stall); memory/u16 DISPROVEN
Advisor (code-grounded) verdict for the next lever now that decode gains are overlapped:
- REJECT B (memory/u16 2x width): DISPROVEN — rapidgzip uses identical u16 MarkerVector (DecodedData.hpp:23), so u16 width is NOT a divergence; the diffuse 1.6x-across-components is CONSUMER-STARVATION (workers idle-gated by the consumer's serial submit), not DRAM. (This also explains why decode micro-opts tied: workers already idle-gated.)
- LEVER A (confirmed in vendor source + my own memory project_wall_is_consumer_critical_path): port rapidgzip's queuePrefetchedChunkPostProcessing (GzipChunkFetcher.hpp:521-581). gzippy submits post-processing ONE-chunk-at-a-time in-order (chunk_fetcher.rs:1451) and leaves ready prefetched SUCCESSOR chunks idle during the consumer stall (the wait at chunk_fetcher.rs:1338). Eagerly apply_window/post-process ready successors DURING the stall → convert stall into useful work → shorten the next blocking chunk's latency.
- CHEAP CONFIRM: ~20-line gated probe — during wait_replaced_markers, submit_post_process for ready prefetched successors (published windows). N=0 (today) vs unbounded; frozen interleaved measure.sh. If wall moves → A confirmed AND it's the impl.
- CAVEAT: a PRIOR consumer-postprocess-pump TIED because it enqueued 0 tasks (cache empty during stall). The probe MUST report how many ready successors it finds — if ~0, the real lever is PREFETCH DEPTH, not eagerness.
- DISPROVE memory cheaply (no perf counters): thread-count per-worker-rate sweep — flat per-worker decode MB/s across T2-16 = consumer-bound (A); falling = memory-bound (B).
- Parity REACHABLE: no proven structural cap (u16 + 31% speculation fraction both equal to vendor).

## 2026-05-31 — early-window-publish (byte-identical) + T-sweep: the CHAIN INVARIANT is the root structural lever
T-sweep (per-worker window-absent decode rate, gz_wordcopy): aggregate 215(T2) -> 210(T4) -> 190(T8) -> 163(T16) MB/s, body_ms 746->986. Decode rate FALLS with threads (-12% T2->T8, -24% T16) + body_ms +32% = a memory-CONTENTION/scaling component (refines advisor's "pure consumer-bound"; the u16-WIDTH sub-hyp stays dead = vendor same, but a general bandwidth/contention bound survives). Single-run, suggestive. ⇒ gap is MULTI-FACTOR: serial window chain + contention.
Early-window-publish (chunk_fetcher.rs run_decode_task): worker publishes its last_32kib_window_vec at chunk_end_bit immediately after decode, GATED on max_acceptable_start_bit==encoded_offset_bits (exact-offset chunks only) + provably-clean tail. Byte-identical (routing 25, deflate_block 19, e2e T1-16 vs gunzip). BUT telemetry: published=2/3/1 vs range_speculative=10/17/28 at T4/8/16 — it fires for only 1-3 chunks; the SPECULATIVE marker-bootstrap chunks (the serial-chain bulk) are EXCLUDED because their boundary is a RANGE (max>encoded), unconfirmed until the consumer reaches them.
ROOT STRUCTURAL LEVER (confirmed from a fresh angle): the CHAIN INVARIANT. gzippy demands exact boundary equality (chunk_fetcher.rs:1152/1194); rapidgzip accepts a confirmed range-chain (chunk_N.end==chunk_{N+1}.start at max). Without it, speculative chunks can't early-publish → the serial resolution chain stands. This is THE remaining structural change for parity (+ the contention component). Early-publish kept (correct, layered) though it fires for few chunks today — it banks once the chain invariant lands.

## 2026-05-31 — CHAIN-INVARIANT design: my premise was WRONG; real lever = speculative-window-slot + promote-on-accept (Design B)
Design agent (vendor-source-grounded) correction: gzippy's speculative range [partition_seed, decode_start] is a PREFIX-GAP range, NOT vendor's ALIAS range. Vendor [encoded,max] = stored-block zero-PADDING (non-data) so every offset aliases the same block → range-accept is byte-safe (GzipChunk.hpp:716-722,827-832; Uncompressed.hpp:85). gzippy's [partition_seed, decode_start) bits are REAL DATA decoded by the PREDECESSOR chunk; the chunk's data starts only at decode_start=max. So accepting S<max would DROP [S,max) bytes = corruption. ⇒ gzippy's current `handoff_at_decode_start` guard (S==max, chunk_fetcher.rs:1205) is ALREADY correct + maximally-safe for its range kind; the chain is ALREADY established (chunk_fetcher.rs:1442-1448 + :973 + set_encoded_offset port). "Relax exact-equality to range-accept" = UNSAFE (would corrupt). My/prior-advisor framing was wrong.
REAL LEVER (Design B): speculative dynamic chunks ARE correct data at decode_start; they just can't EARLY-PUBLISH their window (consumer hasn't confirmed decode_start==predecessor-end yet). Fix: worker early-publishes the chunk's CLEAN-TAIL window (predecessor-INDEPENDENT, no markers) into a SPECULATIVE side-slot keyed at decode_origin_end; consumer PROMOTES speculative->real key on accept (identity-key move, bytes unchanged), EVICTS on reject. Breaks the serial resolution chain WITHOUT touching the correct accept guard. Biggest risk: stale speculative window if evict-on-reject isn't atomic -> resolves successor against wrong bytes; catcher = test_single_member_routing_multithread (byte-perfect T4/24MiB). Design A (true alias ranges for stored blocks) is faithful vendor port but ~perf-neutral on silesia (no mid-stream stored blocks); land its range_is_alias tag alongside for fidelity.

## 2026-05-31 — Design B FIRES on x86 (33/39 chunks) but wall inconclusive; the lever needs the EAGER-POSTPROC pair
Built Design B on x86 (gz_designb @23f9cd8): spec_published=33 promoted=33 evicted=0 (vs arm64's 2-5 — x86's all-39-slow-path makes 33 qualify). Byte-identical (e114dd2). So early window-publish for speculative chunks WORKS at scale.
WALL A/B (gz_wordcopy[no-B] vs gz_designb[+B] vs rapidgzip): INCONCLUSIVE — box NOT quiet (spreads 54-869%, rapidgzip itself 69%). T4 tie, T8 +6% (869% spread=meaningless), T16 -4%. Cannot conclude. Measurement infra (box noise) is now the blocker for few-% levers.
DEEPER ISSUE (theory check): Design B makes the predecessor window AVAILABLE early, but the consumer still serially submits+waits (future_recv) for each chunk's post-process when it REACHES it in order — Design B alone doesn't make it USE the window earlier. The eager-post-process probe (which would consume early windows) hooks the has_predecessor stall = runs 0x (the real consumer wait is future_recv = decode+postprocess RESULT, not the window). ⇒ Design B + EAGER-POSTPROC are a PAIR: each alone inert; together (eager post-process hooked at the future_recv wait, using Design B's now-available promoted windows to resolve successors during the stall) is what could break the chain. Both in tree (Design B committed; eager probe gated) but eager hooks the wrong stall.
NEXT: re-hook eager-postproc at the future_recv/per-iteration wait (not has_predecessor) to submit successors' apply_window during the stall, now that Design B makes their windows available. AND get a quiet box (the 54-869% spreads make wall conclusions impossible). All changes byte-identical + layered + kept.

## 2026-05-31 — eager-pair REFUTED on x86 (critpath); wall is per-chunk in-order DECODE latency, not window/post-process
x86 critpath (gz_eager, T8, noise-robust single-trace):
- eager OFF: consumer wait 287ms = ~85% DECODE (blocked-on scan_candidate 132 + bootstrap 82 + isal 34 = 248ms). future_recv/post-process is NOT the dominant wait.
- eager ON: `consumer.eager_postproc` = 195ms of consumer SELF-WORK on the critical path (per-successor (*arc).clone() + submission runs ON the consumer thread); consumer busy 6.6->200ms. NET LOSS. Fires correctly (23/23 reused, spec_promoted=29, byte-identical) but targets the SMALL part (post-process) and dumps its submission cost on the thread it means to relieve.
⇒ WINDOW-CHAIN / EAGER direction REFUTED. The wall is the PER-CHUNK SPECULATIVE DECODE LATENCY on the IN-ORDER critical path: the consumer blocks waiting for the SPECIFIC NEXT-in-order chunk to finish its speculative decode (scan_candidate) while other chunks are already done. Re-confirms the awaited-chunk-latency finding. Explains the decode-speedup paradox: rate +24% but wall +2-9% because the in-order FRONTIER chunk (not aggregate throughput) gates the consumer.
KEPT: Design B (early-publish, neutral/byte-safe) + eager probe (gated GZIPPY_EAGER_POSTPROC, default OFF, refuted — diagnostic only). Production default unaffected.
NEXT (the real, hard, re-confirmed lever): reduce the IN-ORDER FRONTIER chunk's speculative-decode latency — either (a) prioritize/schedule the next-in-order chunk's decode ahead of speculative successors (the consumer waits on a specific chunk while workers decode others), or (b) cut the speculative re-decode/boundary-search (scan_candidate=132ms, the biggest) cost. Decode-rate micro-opts give diminishing wall returns; the lever is the in-order SCHEDULING of which chunk decodes first, or the speculative-search waste.

## 2026-05-31 — META-REVIEW: method good, but SCOPE DRIFT (T8-single-member-silesia only) + frontier lever refuted + T8-arm64 corruption flag
Opus meta-review verdict: methodology MUCH improved (causal perturbation not attribution; 32 agents delegated; noise→trace pivot; oracle repaired; phantoms retracted) — agent/context/causal concerns SATISFIED. BUT real DRIFT: ~85 of 89 measure cmds are T8; ZERO non-silesia archive types (multi-member/BGZF/incompressible/small = 0% of "every archive type" goal); optimized engine reached via GZIPPY_FORCE_PARALLEL_SM (production T1-3 = libdeflate one-shot, MIN_PARALLEL_SM_THREADS=4). Tunnel-vision on single-member-T8. COURSE-CORRECTION (the #1 action): run ONE frozen interleaved full-matrix sweep T1-16 × {single-member, multi-member, BGZF, incompressible, small} on the ACTUAL PRODUCTION binary (no force-flag) vs rapidgzip+pigz — to find whether single-member-T8 is even the biggest remaining loss + close the archive-type coverage gap + measure production reality.
Frontier-priority lever: REFUTED (near-no-op). Only 1 on-demand frontier decode fires/run; the other 16 are already in-flight prefetches when the consumer reaches them ⇒ decode-THROUGHPUT-bound on prefetches (priority 0), NOT scheduling-bound. Byte-identical/timing-only. Priority bands (postproc -2 > on-demand -1 > prefetch 0) kept (correct, enables future option-b: distance-ranked prefetch priority).
⚠️ CORRECTNESS FLAG: pre-existing DETERMINISTIC T8 output corruption on arm64/pure-rust (T1/2/4/16 clean; x86 T8 = e114dd2 correct all session). Confirmed pre-existing (reproduces on 9181113 baseline). Must run down — likely an arm64 timing/memory-model race in the consumer/window changes; x86 (production target) clean so far. The full-matrix sweep is sha-verified and will catch any x86/production exposure.

## 2026-05-31 — fulcrum vs-sweep (rich cross-tool, per-T) NAMES the lever without code-reading: UNIFY the two-stage decode
Built `fulcrum vs-sweep` (fulcrum branch feat/vs-sweep @cf3de58: per-T × per-role busy+wall-critical, ranked by gzippy-rapidgzip Δwall-critical, cross-T scaling, ★NECESSARY CHANGE headline). Fixed rapidgzip wait instrumentation (gzippy worktree @9fb7aaa, scripts/rapidgzip_trace_patch phase3b: wait.future_recv in BlockFetcher::get + consumer.combine_crc/write_data in ParallelGzipReader::read — the old patch silently no-op'd). Rebuilt traced rapidgzip; sha e114dd2 T1-16.
KILLED A PHANTOM: pre-fix, consumer-write looked like the lever (+300ms every T) — INSTRUMENTATION ARTIFACT (rapidgzip's consumer-write was invisible). Post-fix consumer-write = PARITY (gzippy 294-311 vs rapidgzip 289-313ms, even -2ms T16). Coverage 83-100% both tools.
THE LEVER (rich measurement, frozen, sha-verified T1-16):
- window-absent BOOTSTRAP decode: gzippy +29..+203ms wall-critical, rapidgzip = 0ms (STRUCTURAL — rapidgzip has NO separate window-absent bootstrap decoder; robust fact, not attribution).
- clean-decode (ISA-L tail): gzippy top Δwc +88..+256ms (731ms closable across sweep; attribution-based so weaker signal than the bootstrap=0 structural one).
⇒ NECESSARY CHANGE (measurement-named + goal-aligned): gzippy decodes each chunk in TWO stages (window-absent bootstrap → clean ISA-L tail + absorb_isal_tail handoff); rapidgzip uses ONE unified decoder (emits u16 markers inline when window-absent, u8 when clean). The +10% merge unified the DATA (one DecodedData); the remaining structural change is unifying the DECODE = the original goal "merge marker phase + ISA-L phase, never pay the per-stage CPU tax." Single-member T1-16 gap 0.73-0.91x (T8 worst); x86 byte-correct all T (T8 corruption is arm64-only).

## 2026-05-31 — ADVISOR: decode-UNIFICATION is a PHANTOM (don't build); per-stage tax already measured ~0
The vs-sweep named "unify the two-stage decode," but the advisor (+ our own prior measurements) REFUTE it as a wall lever before the multi-session rewrite:
- per-stage TAX already ~0: absorb_isal_tail copy = wall-NEUTRAL (project_copies_wall_neutral, -0.0%); stage-transition gap = 0.2ms (awaited-chunk breakdown). Unification removes ~0 wall.
- decode-TIME is overlapped/wall-dead: FastBootstrap (1.7-1.9x faster) = TIE; +24% rate from the layered stack = +9% wall. Unification is largely decode-time ⇒ TIES.
- "bootstrap=0ms in rapidgzip" is a structural FACT, not a wall lever (plausibly the consumer-critical-path seen from the other end).
- Advisor's fallback Probe C (consumer eager-postproc) = ALREADY REFUTED this session (eager-pair = +195ms net loss; dominant consumer wait is DECODE not post-process). Probe U (one-pass whole-chunk bootstrap) is confounded (uses the SLOW decoder for 100% → tests slower-decode, not no-handoff).
⇒ DO NOT build the decode-unification rewrite. 
HARD CORE (advisor-validated): the gap is the decode ~1.6x slower BUSY than rapidgzip but OVERLAPPED across 8 workers, so isolated speedups give SUB-LINEAR wall returns. Refuted levers (this session): memory/u16, eager-scheduling, decode-rate-as-primary, range-accept, window-chain, frontier-scheduling, decode-unification, per-stage-tax. The wall is the per-chunk in-order speculative-decode latency where every isolated lever is overlapped/~0. Single-member T1-16 = 0.73-0.91x; x86 byte-correct (T8 corruption arm64-only).
The only un-refuted direction: close the full ~1.6x window-absent-decode busy gap (the slow pure-Rust bootstrap ~194 MB/s vs rapidgzip's faster window-absent-within-one-decoder) — a long inner-loop grind with KNOWN sub-linear wall payoff; not a single lever. Measurement clarity (fulcrum vs-sweep) is now rich enough to name levers; the levers are just mostly overlapped.

## 2026-05-31 — DECODE-BYPASS experiment: decode is the PARALLEL part; the SERIAL consumer/output chain is the floor (REDIRECT)
Implemented gated byte-identical decode-bypass (GZIPPY_BYPASS_CAPTURE/DECODE, commits db8fadd..78c6406): worker memcpy's precomputed chunk instead of decoding. Frozen, interleaved, sha-verified T2-16:
| T | gz_normal | gz_bypass | rg_normal | bypass/normal | bypass/rg |
|---|---|---|---|---|---|
| 2 | 928 | 1392 | 675 | 1.50 | 2.06 |
| 4 | 623 | 1299 | 406 | 2.09 | 3.20 |
| 8 | 510 | 1199 | 348 | 2.35 | 3.45 |
| 16| 501 | 1202 | 390 | 2.40 | 3.08 |
ROBUST (confound-free): removing decode made gzippy SLOWER + collapsed CPUs-utilized 6.36→1.10 (perf-stat T8). ⇒ DECODE is the PARALLELIZED work (~6.4 cores; NORMAL critpath = 90% of consumer wait blocked on decode spans). The NON-decode (in-order consumer + 663MB output materialization + write + CRC + window publish) is ~SERIAL on ~1 core and is the floor. Form-A(clean)≈Form-B(markers) 0.99-1.01 ⇒ marker/window coordination NOT a separable lever.
CONFOUND (honest): can't make a byte-correct decoder's 663MB output free — the prebuilt-memcpy + cold faults (191K→537K) inflate bypass wall. Treat bypass ABSOLUTES as UPPER BOUND; the robust signals are the parallelism-collapse + critpath shift (decode→serial pool.run_task 95.8%).
REDIRECT: the lever is the SERIAL in-order consumer/output chain (parallelize/pipeline output materialization + CRC + window publish — rapidgzip's "lean consumer"), NOT the inner Huffman loop. Corroborates "decode overlapped, sub-linear." 
NEXT (user): SLEEP variant — fixed identical per-chunk sleep in BOTH gzippy+rapidgzip, garbage-output/wall-only (NO materialization confound), realistic timing regime ⇒ CLEAN coordination comparison.

## 2026-05-31 — FIXED-SLEEP experiment OVERTURNS the bypass redirect: coordination is HEALTHY, gap is the (overlapped) DECODE
The decode-BYPASS "serial consumer/output is the floor" redirect (prior entry) was a MEMORY-PRESSURE ARTIFACT — replaying 663MB precomputed buffers (esp. a 3.3GB 5-file merge) OOM'd/swapped the 8GB box, inflating gzippy's wall to 1-2.5s. The sleep agent REPRODUCED it (giant captures → 1-2.5s) and ELIMINATED it (meta-only 6KB captures, sizing metadata only → ~100-190ms). ⇒ RETRACT the "serial consumer is the lever / lean-consumer redirect" — it was OOM, not coordination. (Lesson: the bypass instrument wasn't validated for the box's 8GB RAM; classic box-contamination blind spot.)
CLEAN fixed-sleep result (meta-only, frozen, interleaved N=9, 503MB):
- gzippy's TRUE coordination floor ~100-190ms (critpath T16 ns=0: try_take_prefetched 102ms = healthy consumer-overlapping-on-workers + drain/write_data ~87ms = 503MB output materialization; NO serial coordination stall).
- SLOPE d(wall)/d(sleep-ns): both tools overlap per-chunk sleep ~5-14× ⇒ NEITHER has a latency-overlap defect; the gzippy-rg delta is a FIXED offset, flat in sleep-ns.
- ⇒ COORDINATION IS NOT THE LEVER (healthy, well-overlapped). The ~1.5× gap is the (overlapped) DECODE — re-confirms the pre-bypass conclusion.
CAVEAT (asymmetry): rapidgzip's ChunkData was too coupled to fabricate, so its sleep was injected AFTER real decode (rapidgzip-sleep = real-decode + sleep) while gzippy zero-fills (decode≈0). So the absolute "gzippy 0.41-0.84× rapidgzip / gzippy faster" is NOT a clean coord-vs-coord (gzippy skipped decode). ROBUST signals = (a) gzippy floor ~100-190ms healthy [gzippy-only critpath], (b) slope = no overlap defect either tool, (c) bypass-was-OOM debunk. A truly clean coord-vs-coord needs rapidgzip to also bypass decode (return dummy ChunkData) — not done.
NET: coordination healthy; gap is overlapped decode (sub-linear payoff, as long established). The bypass redirect is dead (OOM artifact).

## 2026-05-31 — CONFOUND-FREE THREE-INSTRUMENT result (bypass re-run + sleep + oracle agree): gap = SPECULATION overhead, NOT decode-rate, NOT coordination
The bypass agent SELF-CORRECTED (meta-only 6KB capture + /dev/null timing, killing the OOM + sha-in-timed-region confounds). Frozen, N=5-7, /dev/null:
| T | gz_normal | oracle(clean,no-spec) | sleep0(no-decode) | rg_normal | norm/rg | oracle/rg |
|---|---|---|---|---|---|---|
| 2 | 787 | 439 | 230 | 495 | 1.59 | 0.89 |
| 4 | 463 | 211 | 166 | 297 | 1.56 | 0.71 |
| 8 | 369 | 144 | 131 | 229 | 1.61 | 0.63 |
| 16| 307 | 115 | 127 | 227 | 1.35 | 0.51 |
THREE instruments agree (sleep-decode, clean-window oracle, critpath). REVERSALS:
1. "coordination floor" (prior bypass) = OOM ARTIFACT (663MB replay on 8GB box). DEAD.
2. "decode overlapped/wall-dead" = WRONG: sleep0 HALVES the wall (decode ~60% of wall, ON critical path; critpath normal ~90% blocked-on-decode, 43% scan_candidate+bootstrap = SPECULATIVE).
3. gzippy CLEAN decode + coordination = at-or-BETTER than rapidgzip (oracle beats rg 0.51-0.89×).
4. GAP = SPECULATION OVERHEAD (gz_norm − oracle ≈ 225ms@T8 = 61% of wall). gzippy's speculation ~2.6× rapidgzip's (both speculate 31%).
THE LEVER (three-instrument-confirmed): reduce gzippy's SPECULATION overhead (the marker-bootstrap path that runs when a predecessor window isn't ready in time) toward rapidgzip's efficiency. The oracle (windows a-priori) = unreachable upper bound (advisor's prior caveat); achievable target = rapidgzip's spec-efficiency (~85ms vs gzippy's 225ms).
TENSION TO RECONCILE: this session REFUTED the obvious spec sub-levers — FastBootstrap (spec decode-RATE) TIED; eager-postproc NET-LOSS; window-absent FRACTION is a faithful vendor port; Design-B early-window fired but didn't bank. So if the gap is speculation overhead but rate/fraction/early-window are refuted, WHAT is the actionable reduction? (→ advisor)

## 2026-05-31 — ADVISOR reconciles the contradiction: spec overhead = DATA-MODEL tax + (maybe) runtime-fraction, NOT decode-rate
The window-absent PATH = 3 serial ops: (1) bootstrap decode→u16 markers [FastBootstrap sped ONLY this], (2) ISA-L tail+absorb, (3) apply_window/replace_markers [2nd pass] + narrow [3rd pass], gated on window publish. FastBootstrap sped the smallest term → tied. But this session's slow-injection (+30% wall for 2× slower bootstrap, sleep-disproof-survived) shows term(1) IS on the critical path at ~0.3 wall-sensitivity — and +24% rate→+9% wall is EXACTLY that 0.3 slope. ⇒ FastBootstrap's "1.7×→TIE" was the CONFOUNDED measurement (speedup unrealized); term(1) is a real SUB-LINEAR lever, not wall-dead. Contradiction RESOLVED: "decode overlapped" and "decode 60% of wall" are both ~right (sub-linear 0.3 sensitivity).
The 2.6×-vs-rapidgzip = (c) DATA-MODEL TAX: gzippy moves the marker buffer ~3× (decode-write + replace_markers read+write + narrow read+write) vs a fused decoder's ~1× = 2.4-2.6× memory traffic (matches TMA dram_bound 14.4 vs 6.1, 11× L3). AND/OR runtime window-absent FRACTION > static 31% (windows publish LATE on the consumer-serial path ⇒ more chunks decode window-absent before their window arrives; would also explain Design-B fired-but-didn't-bank = published too late to unblock the successor's DECODE).
DECISIVE MEASUREMENT (existing scaffolding, the one to run): runtime window-absent fraction = at each chunk's Phase-1 start, was its predecessor window already published? If >31%/>rapidgzip ⇒ lever = eager-publish-to-unblock-DECODE (publish clean-tail window off the consumer-serial path the moment a worker proves it). If =31% ⇒ lever = apply_window/replace_markers FUSION (collapse 3× buffer moves to 1×). Plus measure apply_window+narrow wall-share (the data-model tax). These distinguish the two live levers + reconcile all refuted ones (FastBootstrap=term1, eager=wrong-phase, Design-B=too-late).

## 2026-05-31 — DECISIVE MEASUREMENT RAN (causal trace, T4/T8/T16): runtime window-absent = 95% ≫ 31% static, and it is SELF-INFLICTED OVER-DISPATCH (not key-mismatch, not fusion)
Fulcrum `causal` view + gzippy `causal.*` trace (decode_decision@decode-start + window_publish@consumer, branch feat/fulcrum-causal-sweep @0e29a9f; traces /tmp/causal_T{4,8,16}.json). 40 chunks, 38 window-absent at EVERY T. Wall 485/352/307ms T4/8/16. I re-joined the events myself (the agent's "97% key-mismatch" HEADLINE was WRONG — refuted below); advisor-validated (a46169fb).
- **Runtime window-absent fraction = 95% (38/40), flat across T4/T8/T16** — vs the 31% STATIC boundary floor. The 64-pt gap = recoverable waste.
- **Cause = LATE-PUBLISH, not key-mismatch.** Strict join (immediate-predecessor window published BEFORE the chunk's decode-ts, exact-get missed): only **1–2 of 38** at any T (noise). The other 36–37 had **NO predecessor window published at all** when they decoded. The agent's seed≠real-boundary keys DO differ (33554432 seed vs 33571303 publish) but that is NECESSARY-NOT-SUFFICIENT: a boundary-tolerant lookup hits nothing because nothing is published near that stream position yet. Key-misalignment is downstream of the real cause.
- **THE discriminator — prefetch-depth-ahead-of-resolved-frontier histogram:** window-absent chunks decode a MEDIAN of **2 (T4) / 7 (T8) / 13 (T16)** chunks AHEAD of where the consumer has resolved+published windows. Clean decode happens ONLY at depth ≤ −1 (at/behind the frontier). **Depth SCALES with thread count while the 95% fraction stays flat** ⇒ adding workers DEEPENS speculation (all window-absent + 3-pass tax), it does NOT add clean throughput. THIS is gzippy's scaling defect vs rapidgzip.
- Timing proof (T8): first 8 chunks decode @2.3ms, first window publishes @29.1ms; a late chunk decodes @241.8ms, its predecessor window publishes @313.8ms (72ms LATER). Workers race the in-order consumer and always win → window-absent.
- rapidgzip ~88% clean ≈ its 31% static floor inverse: it eats only the irreducible ~12% speculative window-absent. It bounds prefetch depth to the resolved frontier (FetchingStrategy/splitIndex) AND resolves markers worker-side off the serial consumer path (queuePrefetchedChunkPostProcessing).
LEVER (advisor-framed, non-circular — reframes "fusion vs eager-publish" → over-dispatch): drive runtime window-absent toward the 31% static floor by (a) BOUNDING prefetch depth to the published frontier [pure dispatch policy] AND/OR (b) WORKER-SIDE EAGER apply_window — resolve a speculatively-decoded chunk's markers in parallel the moment its predecessor window arrives, OFF the consumer's serial 3-pass path. (a)-alone risks worker starvation (deep spec is what keeps workers busy); (b) is the fundamental one — it makes the speculation NOT wasted. Prior eager-postproc hooked the has_predecessor stall (ran 0×) = WRONG site; the right hook is "predecessor window just published → clean this already-decoded chunk on a worker." Reconciles all refuted spec sub-levers: FastBootstrap=sped term1 only; fraction-is-faithful-port=FALSE (95% runtime, not 31%); Design-B fired-but-didn't-bank=published too late + nothing consumed it eagerly.

## 2026-05-31 — CONSUMER SERIAL-SPAN DECOMPOSITION (from existing causal trace) → crossover + combine_crc lever
Decomposed the consumer (tid1) serial span on /tmp/causal_T*.json (advisor a46169fb + pivot-advisor). CAVEAT: my python self-time double-counts nested same-name spans (Σself 350 > span 308 @T16) — fix with a direct idle-gap span before trusting subtractive wall predictions. Robust (inclusive / direction / flat-value) facts:
- consumer span ≈ wall (485/353/308ms T4/8/16).
- block_finder_get (WAIT for next chunk's decode result): 212/142/98ms — SHRINKS with threads (parallel part scales healthily; deep speculation keeps results flowing).
- marker-resolution COMPUTE on the serial path (window_publish_marker + resolve_markers): ~126/134/155ms, GROWS as share 26%→38%→50%. At T16 ~HALF the consumer span is serial marker-resolution (the data-model tax, on critical path, NOT overlapped).
- combine_crc = FLAT ~62ms at ALL T (62.7/62.4/62.4) ≈ 503MB CRC32 computed SERIALLY on the consumer (~8GB/s). Associative ⇒ movable to workers.
- process_prefetches 31-71ms.
CROSSOVER: wait-bound at low T (block_finder_get dominates), COMPUTE-bound on serial marker-resolution at high T. So fusion (cheapen the 3-pass tax) IS a real lever at T16 (partially overturns the "fusion=TIE" prior), but bound-depth (cut 95%→31% window-absent = fewer chunks paying resolution) attacks the root + changes fusion's denominator.
LEVER ORDER (advisor-set): (1) combine_crc OFF the consumer (worker-side per-chunk CRC + folded combine) — highest confidence, associative, no re-exposed wait, no correctness landmine, T-flat ~60ms; (2) bound prefetch depth (95%→31%); (3) fuse residual 3-pass tax. (1) isolates cleanly (doesn't touch the window/resolution chain). Range-accept/chain-invariant lever = DEAD (window is ABSENT not mis-keyed; range-lookup finds nothing). Eager-postproc = already in tree (GZIPPY_EAGER_POSTPROC) + TIE'd (ac41ef0: confirmed-window is an irreducible serial dep).

## 2026-05-31 — CORRECTION: the entry above used a BUGGY self-time script; combine_crc=62ms is a PHANTOM. Real consumer serial = OUTPUT-WRITE + NARROW + DECODE-WAIT
The "combine_crc flat 62ms" + "marker-resolution 126/134/155ms growing to 50%" numbers above came from a python script that DOUBLE-COUNTED nested same-name spans (Σself > span). A corrected stack-based self-time pass on the SAME /tmp/causal_T*.json gives the real consumer (tid1) serial self-time:
| consumer tid1 SELF-time | T4 (span 486) | T8 (span 354) | T16 (span 307) |
| consumer.write_data (503MB output materialization/write) | 115ms | 126ms | 123ms |
| ttp.rx_recv_block (WAIT for next chunk's decode result) | 246ms | 106ms | 66ms |
| wait.block_fetcher_get (WAIT) | 74ms | 72ms | 64ms |
| consumer.write_narrowed (u16→u8 NARROW tax) | 48ms | 45ms | 47ms |
| consumer.window_publish_marker (marker RESOLUTION on consumer) | 0.4ms | 0.5ms | 1.2ms |
REVERSALS vs the buggy entry:
- Marker RESOLUTION is NOT on the consumer serial path (0.4-1.2ms) — it is OVERLAPPED on workers. The "3-pass data-model tax on the consumer" framing for the RESOLVE pass is WRONG. (The NARROW pass, write_narrowed ~45ms, IS on the consumer.)
- combine_crc is NOT a top serial term (the 62ms was the script bug). combine_crc lever = WITHDRAWN.
- The REAL consumer serial cost = output-write (~120ms, ~irreducible 503MB) + narrow (~45ms) + DECODE-WAIT (rx_recv_block + block_finder_get = ~180ms@T8, ~130ms@T16 = blocked on worker decode results).
- The decode-wait SHRINKS with T (246→106→66 rx_recv) = parallel decode scaling; it is the term bound-prefetch-depth attacks (clean chunks decode ~3× faster → consumer waits less + produce u8 directly → less narrow).
LEVER (re-grounded on the CORRECTED trace): bound prefetch depth (95%→31% window-absent) attacks decode-WAIT (~180ms) + narrow (~45ms), NOT a resolution tax. combine_crc WITHDRAWN. Fulcrum needs a CORRECT consumer-span decomposition view (self-time + idle-gap, busy+idle=span validated) so this class of script bug can't recur — see 2026-05-31 user directive.

## 2026-05-31 — BOUND-PREFETCH-DEPTH rejected via NAMED worse-measurement (D6 starvation), arm64 mechanism; x86 wall pending make ship
Lever: GZIPPY_MAX_PREFETCH_DEPTH=k clamps prefetch frontier to k chunks ahead of the consumer (port of rapidgzip FetchingStrategy bound). Branch feat/bound-prefetch-depth @899868c (frontier-relative guard fires 0× = amount-clamp already anchored at last_fetched; byte-identical, kept/layered). Measured via the NEW Fulcrum `consumer` decomposition view (PR #6, double-count-free, RECONCILE residual 0.000µs — the instrument that killed the combine_crc=62ms phantom: combine_crc is really 39.56µs).
arm64 release pure-rust, silesia 40MB, k-sweep {unbounded,8,4,2,1}, sha e5b7e686 identical at every k (D1 HELD):
- D2 clean-decode fraction RISES 6.7→10→16.7→26.7→40.0% as k drops (cap bites; HELD direction).
- D6 (the named worse-measurement that justifies rejection per the methodology bar): consumer-span wall WORSENS monotonically 36→40→53→73→91ms; `wait.block_fetcher_get` EXPLODES 4→15→23→36→53ms, `ttp.rx_recv_block` 7→23ms. Bounding depth STARVES the in-order consumer — the clean-decode win is dwarfed by re-introduced decode-WAIT.
VERDICT: bound-depth ALONE is rejected (valid per the bar — named worse-measurement, not a bare TIE). This CONVERGES with the prior advisor framing (entry ~289: "(a) bound-depth alone risks worker starvation — deep spec is what keeps workers busy"). The deep over-dispatch is NOT pure waste; it keeps the consumer fed. So the 95%-window-absent "waste" is partly load-bearing.
CAVEATS (do not over-claim): (1) x86 PRODUCTION wall unconfirmed — this box is arm64 w/o ISA-L (routing prints path=IsalParallelSM but runs pure-Rust); D4 needs `make ship`/`make bench-sm` on neurotic with the k-sweep. On x86 the clean-decode compensation is STRONGER (fast multi-symbol ISA-L clean vs slow pure-rust), so arm64 may UNDERSTATE bound-depth's benefit — but the D6 starvation direction is arch-independent. (2) Rejection is of bound-depth ALONE; pairing with worker-side eager resolution (so freed capacity isn't idle) was always the prior plan (289).
NEXT-LEVER (PENDING ADVISOR — do NOT re-derive a corpse): the agent proposed queuePrefetchedChunkPostProcessing/eager-apply_window-during-the-stall. That is ALREADY BUILT (eager_postprocess_prefetched, GZIPPY_EAGER_POSTPROC) and ALREADY REFUTED on x86 (entry 208-214: +195ms consumer self-work, clone+submit ON the consumer thread = NET LOSS). Open question for advisor: is the refuted version (submit ON consumer) distinct from the un-built "clean on a WORKER the moment predecessor window publishes" (289)? AND reconcile with entry 212's conclusion that the wall is the IN-ORDER FRONTIER chunk's decode latency (a SCHEDULING lever: decode next-in-order first), which is distinct from both bound-depth and eager-postproc.

## 2026-05-31 — ADVISOR adjudicates bound-depth verdict + next lever (3 rejections WITH mechanism; lever = window-absent decode SPEED)
Advisor (abca15276) ruled on the bound-depth result + the agent's proposed next lever:
1. BOUND-DEPTH rejection is SOUND on arm64 SIGN alone — do NOT spend a make-ship. Starvation is arch-independent in SIGN: x86's stronger clean-decode only shrinks the BENEFIT term, not the re-introduced consumer-WAIT (block_fetcher_get) term; for the sign to flip, x86 clean-speedup would have to exceed the ENTIRE re-introduced wait, which is serialization-dominated not decode-dominated. Record SIGN-only (not magnitude). The clean-decode "win" is partly an accounting artifact: bounding depth converts speculative-decode-latency into in-order-WAIT-latency (consumer blocked long enough for the predecessor window to arrive).
2. EAGER-APPLY_WINDOW (agent's proposal) — the worker-side variant IS structurally distinct from the refuted consumer-side eager_postproc (dodges the +195ms consumer self-work), BUT it is INERT for a prior+stronger reason: the as-built probe logged EAGER_PROBE_SUBMITTED=0 / "0 ready successors during stalls" — there is NOTHING to eagerly post-process because the work is DEPENDENCY-blocked on the same frontier the consumer waits on; moving it to a worker can't manufacture work that doesn't exist. Reject WITHOUT building.
3. TRIANGULATION — (i) bound-depth starves (can't cut in-flight count) + (ii) eager inert (no off-critical-path work; awaited work is dependency-blocked) + (iii) wall = in-order FRONTIER chunk's intrinsic decode latency — all point to ONE place: the window-absent frontier chunk's DECODE SPEED. rapidgzip's mechanism is SPEED not scheduling: its prefetcher ALSO speculates breadth-first (no in-order priority); it wins because its window-absent path is its FULL-SPEED inflate engine, so every chunk incl. the in-order frontier is ready in time without reordering. gzippy's frontier is slow because its window-absent bootstrap is the ~194MB/s single-symbol pure-Rust path. SCHEDULING-priority is a WEAKER lever than it looks (frontier chunk is already in-flight under saturated workers; reprioritizing doesn't make it start sooner). 
HIGHEST-PROBABILITY UN-REFUTED DIRECTION: speed up the window-absent/bootstrap speculative decode toward multi-symbol (clean-decode parity). ~15% BOUNDED (structural to the speculative-marker datamodel — carry the bound honestly; it shrinks but won't fully close the x86 1.5×). Consistent with project_t8_saturated_pool_diag ("only live lever = bootstrap marker DECODE speed").
DO NOT BUILD: bound-depth (D6 starvation), eager-postproc worker-side (EAGER_PROBE_SUBMITTED=0, dependency-blocked), in-order scheduling priority (likely inert — confirm via the A-vs-B split first).
Q4 DISCRIMINATOR STILL OPEN (my no-code attempt was TOO COARSE — same bug class as combine_crc): I split consumer block_fetcher_get into (A) start-delay vs (B) decode-duration, but my test checked whether ANY decode starts in the stall window (near-certain with 40 chunks / 2 stalls), NOT whether the SPECIFIC AWAITED chunk starts then. Also this arm64 causal trace lacks the scan_candidate/bootstrap spans the (B) term needs (x86-only), and the dominant consumer wait is rx_recv_block (106ms) not block_fetcher_get (72ms). VERDICT NOT RECORDED from it. To settle SPEED-vs-SCHEDULING needs awaited-chunk attribution: tag each block_fetcher_get stall with the AWAITED chunk's decode-start vs the stall-start (one timestamp diff). Until then the advisor's SPEED conclusion rests on the x86 scan_candidate/bootstrap=214ms-are-(B)-terms finding (entry 212), not on this arm64 run.

## 2026-06-01 — MODEL PARAMS measured both tools (pinned x86) — but "+0.0% residual" is a TAUTOLOGY, not a confirmed prediction (advisor a689769)
fulcrum `model` view (PR #7) populated the wall-model params for BOTH gzippy + rapidgzip from CPU-pinned (8 P-core) x86 runs, silesia 503MB, sha-verified e114dd2b. Table (gz/rg):
| param | gz T8 | rg T8 | gz T4 | rg T4 |
| N | 36 | 39 | 34 | 39 |
| window-absent f | 90.5% | 97.4% | 83.3% | 97.4% |
| d_w (window-absent decode/chunk) | 37.5ms | 28.6ms | 32.7ms | 28.1ms |
| L_resolve (MEAN inter-publish gap) | 12.80 | 7.74 | 17.89 | 13.21 |
| frontier | 19.5 | 17.1 | 19.1 | 17.0 |
| tail | 68.2 | 81.6 | 45.4 | 22.6 |
| wall_pred/obs/resid | 535.6/535.6/+0.0% | 392.9/392.9/+0.0% | 654.8/654.8/+0.0% | 541.6/541.6/+0.0% |
HONEST VERDICT (advisor-corrected — do NOT record "model CONFIRMED"):
- **+0.0% at every cell = TELESCOPING IDENTITY, not a prediction.** publish-chain+tail = frontier + (Σ inter-publish gaps) + (EOF−t_pub(last)) = t_pub(0)+(t_pub(last)−t_pub(0))+(EOF−t_pub(last)) = EOF = wall, BY CONSTRUCTION (L_resolve=mean gap re-sums the wall's own timeline). publish-chain bound in ALL cells ⇒ the non-tautological worker-bound branch was NEVER exercised. The decomposition is valid BOOKKEEPING; predictive content UNTESTED.
- **L_resolve is a CONSEQUENCE variable, NOT a lever/knob** (a chunk can't publish faster than it decodes). Don't say "cut L_resolve."
- **The REAL levers (two, superimposed in the gap):** (a) d_w window-absent decode 1.31× slower (gz 37.5 vs rg 28.6 — the bootstrap-decode-speed lever, ~15% bounded); (b) publish SCHEDULING / eager apply_window (the consumer-critical-path lever). The gap ratio 1.65× > d_w 1.31× ⇒ (b) is NON-TRIVIAL (scheduling absorbs the residual beyond raw decode).
- **Cleanest MODEL-INDEPENDENT fact:** rg has HIGHER window-absent fraction (97% vs 90%) yet LOWER decode AND lower gap ⇒ rg's window-absent pipeline is simply faster per chunk, full stop (isolates from "gz unlucky with fewer clean chunks").
- d_c DROPPED (n≈3, chunk-0 cold-start dominated — gz d_c=45>d_w=37 is the artifact; also poisons the worker-bound branch's d_w_eff).
- Thermal caveat: i7-13700T 35W throttles under 8-P-core load (wall spread 40-51% @T8); within-run trace-ratios robust, absolute 535.6/535.6 is spurious precision.
THE ONE FALSIFIABLE PERTURBATION (next): GZIPPY_SLOW_BOOTSTRAP k-sweep {1.0,1.5,2.0}×{T4,T8,T16}, spin+sleep controls, interleaved N≥7, sha-verified — checks (1) d_w tracks k [instrument control], (2) gap-vs-k SLOPE separates lever (a) decode-gated from (b) scheduling-gated, (3) the T16 KNEE (model's only falsifiable claim: publish-chain binds T4/T8 ⇒ wall moves ~proportional; worker-bound takes over T16 ⇒ response flattens). Report as within-run ratios not absolute ms.

## 2026-06-01 — CLEAN FROZEN-CLOCK NUMBERS (neurotic, verified lock) + causal lever test — advisor-audited
FINALLY a low-variance measurement. neurotic IS the bare-metal Proxmox host (i7-13700T "T" SKU, base 1.4GHz). Lock: no_turbo=1 pins cores at base=1.4GHz (zero turbo headroom ⇒ naturally flat, verified ratio=14 flat ±0 droop under 8-core stress, aperf/mperf=0.993); uncore MSR 0x620=0x1e1e=3.0GHz flat; C-state floor pinned; 6 guests frozen (105/111/166/170/211/212, transmission was the residual) → box 98.8% idle. silesia-large 503MB, N=10 interleaved, iter0 dropped, sha-verified both tools byte-match gzip ref. Box RESTORED to baseline after (turbo on, freq 4.8G, uncore 0x82b, all guests running) — verified.
WALL (all sd<5%, gate passed):
| Cell | gzippy med (sd) | rapidgzip med (sd) | ratio |
| T4 (0,2,4,6) | 1116ms (1.2%) | 995ms (0.6%) | 1.12× |
| T8 (0,2,4,6,8,10,12,14) | 688ms (2.2%) | 581ms (1.7%) | 1.18× |
| T16 (0-15 SMT) | 746ms (2.1%) | 486ms (0.8%) | 1.54× |
**gzippy LOSES at every T, worsening with threads; T16 NEGATIVELY scales (746 > T8's 688) while rapidgzip keeps scaling (581→486).**
RECORD (established, survived-disproof):
1. LEVER TEST (causal perturbation, GZIPPY_SLOW_BOOTSTRAP inflates window-absent bootstrap by +N%; self-test +0%≡clean): window-absent bootstrap WORK is ON the critical path at T4 (slope 1.94≈linear) and T8 (slope 2.27, SUPERLINEAR); T16 has a worker-bound KNEE (flat +1.0% @+50%, then +14.7% @+100%). CONTROLS PASSED: busy-spin≈sleep-yield within ~2ms (turbo-artifact killed); SMT-at-T8 slope 2.76 ≈ T8-noSMT 2.45 and shows no knee ⇒ T16 knee is WORKER-BOUND, not SMT port-contention. The T8 SUPERLINEARITY (>2.0, impossible for an isolated serial region) = d_w is on-path VIA the publish chain: slowing bootstrap delays publish → lengthens L_resolve → lengthens downstream block_fetcher_get waits = amplification. So "d_w" and "publish-chain" are the SAME lever from two ends.
2. STRUCTURAL (decomposition as bookkeeping): publish-chain (frontier + Σ L_resolve in publish order) BINDS at T8/T16 (596/592ms) ABOVE worker-bound (478/339ms). Consumer 94-96% WAIT, 81-83% in wait.block_fetcher_get (493/516ms); consumer serial COMPUTE negligible (12-27ms). Worker side is SLACK. The wall is the IN-ORDER resolve/publish SERIAL CHAIN, not decode throughput. T16 negative scaling = more workers shrink worker-bound (339) but NOT the serial publish-chain (592); the 257ms gap is wasted slack (swallows the +50% inject). rapidgzip keeps scaling because its per-chunk L_resolve is small + it overlaps resolve via queuePrefetchedChunkPostProcessing.
DOWNGRADE (do NOT record as confirmation): fulcrum "model CONFIRMED residual +0.0%" is a TELESCOPING IDENTITY (L_resolve ≡ publish_span/N ⇒ frontier+N·L_resolve+tail = EOF = wall BY CONSTRUCTION), vacuous, NOT a prediction. Keep the decomposition as bookkeeping; the evidence for "publish-chain binds" is publish_chain>worker_bound (non-trivial), NOT the +0.0%.
LEVER the clean data points to = PUBLISH-CHAIN / L_resolve SERIALIZATION (port rapidgzip's queuePrefetchedChunkPostProcessing = eager successor apply_window during the consumer stall so resolve OVERLAPS instead of serializing). Corroborates [[project_wall_is_consumer_critical_path]] with a frozen-clock + controlled measurement.
NOT the lever: (a) window-absent FRACTION f — if rapidgzip is ALSO ~97% window-absent yet wins, f is DEAD (gzippy's 90.5% is not the differentiator; reconcile "31% replaced-marker SYMBOLS" [symbol-level] vs "~97% window-absent" [chunk-level] = DIFFERENT metrics). (b) raw d_w speed — NOT YET PROVEN: slope≠ceiling (Rule 3), and T16 negative scaling argues AGAINST it (more decode parallelism makes it WORSE).
THE ONE EXPERIMENT STILL OWED (before committing to any decode lever): a KNOWN-WINDOW ORACLE (decode at ISA-L/known-window speed, SAME publish pipeline) to measure the speed-up CEILING. If the oracle barely moves the wall ⇒ lever is unambiguously the publish CHAIN, not decode. This single A/B disambiguates d_w from L_resolve — the clean data, for all its rigor, cannot.

## 2026-06-01 — STANDING GOAL (user-set): close the T16 negative-scaling, holistically
MILESTONE GOAL: gzippy scales MONOTONICALLY T4→T8→T16 (today T16 746ms > its own T8 688ms = NEGATIVE scaling; ratios vs rapidgzip 0.86/0.75/0.61x). The structural cause: more workers shrink the worker-bound term but NOT the in-order publish-chain (frontier + Σ L_resolve), which serializes — so adding workers past T8 adds contention without shortening the binding term. Fix the publish-chain serialization (the lever the clean data points at: overlap/shorten per-link resolve, e.g. rapidgzip's queuePrefetchedChunkPostProcessing), reasoning over the WHOLE model (both tools' independent params) — NOT an isolated win. Then pursue full parity (wall ≤ rapidgzip at all T).
DISCIPLINE (the golden rule): think about the ENTIRE gzippy system; speeding one stage may shift the binding term (publish-chain ↔ worker-bound) — speed each piece AND tune the structure to exploit the fast pieces; re-measure the WHOLE after each change. Loop: measure-agent → design-agent (review or implement) → measure-agent.
MEASURED ONLY via scripts/bench/clean_bench.sh (RUN_TRUSTWORTHY, sd<5%, frozen-clock neurotic). No make-ship, no hand-scripts, no tautological +0.0% fulcrum "confirmations".

## 2026-06-01 — INDEPENDENT params kill the tautology AND falsify "worker-bound/d_w" — the real term is HEAD-OF-LINE blocking (advisor a618e152)
Fulcrum model rewritten so L_resolve = consumer publish-span DURATION (independent), NOT publish_span/N. The +0.0% tautology is DEAD: independent L_resolve = 8-35µs/chunk (gzippy), 400-1200× SMALLER than the inter-publish gap the old model mislabeled "L_resolve" — proof the +0.0% was telescoping. wait_replaced_markers totals 0.02-0.03ms ⇒ resolve WORK is genuinely slack. Both verified on the clean harness (frozen-clock, gated, N=9, SD 0.6-1.1%, box restored match-proof).
BUT the agent's replacement conclusion ("both tools WORKER-BOUND, lever = d_w_eff decode latency") is UNSOUND — falsified by its OWN table (advisor-adjudicated):
- wall_pred = frontier + (N/T)·d_w_eff + tail UNDER-predicts observed by 20/20/41% (gz T4/8/16). Absolute miss GROWS with T: 62→115→394ms. A term that only shrinks with T (N/T) cannot have a residual that grows with T ⇒ the model is MISSING a term that scales UP with workers.
- gz T16 NEGATIVELY scales (obs 688→746 T8→T16) but wall_pred DROPS 573→352 — worker-bound predicts the OPPOSITE direction. A worker-bound system mathematically CANNOT negatively scale. Falsified.
- Model misses 53% of the T16 wall (352 of 746). Cannot name a lever from a model explaining <half the wall. The agent mistook "largest MODELED term" for "binding term."
RECONCILES the two runs (they measure DIFFERENT things, no contradiction): L_resolve (resolve WORK, 8-35µs, slack) ≠ block_fetcher_get (consumer WAIT on the in-order-next chunk's DECODE RESULT, 81-83% — prior run). The unmodeled residual is a THIRD term invisible to both (N/T)·d_w AND L_resolve: IN-ORDER HEAD-OF-LINE COMPLETION LATENCY — consumer blocked on chunk i while i+1..i+k already done; GROWS with T (more in-flight → more out-of-order completion). Consistent with BOTH runs.
RECORD NO LEVER YET — model is mis-specified. Leading hypothesis = HoL blocking; the lever it implies = resolve-ahead (rapidgzip queuePrefetchedChunkPostProcessing), NOT d_w, NOT L_resolve.
THE ONE DISCRIMINATING MEASUREMENT (next): Σ HoL_i = Σ max(0, p_i − max(c_i, p_{i−1})) from chunk-completion ts c_i + publish ts p_i, at T8+T16. If Σ HoL_i ≈ residual (62/115/394) AND grows with T ⇒ model becomes frontier+(N/T)·d_w+ΣHoL+tail, residual→0, lever = resolve-ahead. If small ⇒ pivot to confound. MEASURE HoL directly, do NOT infer from residual (circular = the tautology trap again).
MANDATORY CONTROL FIRST: T16 negative scaling is ALSO the SMT-oversubscription signature [[project_sm_scaling_tma_2026_05_29]] flagged at T16. Re-run T16 PROPERLY PINNED before attributing to HoL. If negative scaling survives clean pinning ⇒ structural (HoL); if it vanishes ⇒ the 394ms T16 residual was substantially a pinning artifact and the durable case rests on the T4→T8 residual growth (62→115ms).

## 2026-06-01 — HoL measurement attempt #1 FAILED (broken join); corrected design (advisor ad718b0c). NO HoL number recorded.
The HoL agent (branch measure/hol-latency @89aaf15) captured traces but its c_i/p_i join was INVALID: worker.decode_chunk is keyed by start_bit (partition SEED), consumer.window_publish_* by end_bit (REAL boundary) — INCOMMENSURABLE (same key-mismatch that's bitten the campaign before). My salvage join fell back to POSITIONAL pairing → Σ HoL = 594ms > publish-span-total 268ms = IMPOSSIBLE (HoL is idle WITHIN the span). Also 42 decode spans vs 36 publishes (speculative re-decodes). The number is GARBAGE — NOT recorded. (Also: the agent died before restoring; box was left locked — I force-restored + verified baseline. HARNESS GAP: when the controlling process dies mid-run, restore waits on the watchdog TTL instead of firing immediately; worth a fix.)
The cells were ALSO noisy despite the gate passing: T4 gz sd 34.7%, rg sd 44.5% — gate verified clock-flat but WALL spread was huge. (Per-chunk trace timestamps are robust to inter-run noise; the wall cells were not. Investigate why the gate passed but cells were noisy — possibly the 162MB corpus is too short, or a residual.)
CORRECTED DESIGN (advisor, for attempt #2):
1. STOP joining on bits/chunk_id (chunk_id is u64::MAX sentinel for speculative). Use partition_idx (the consumer's monotonic in-order ordinal, already computed chunk_fetcher.rs:1038, already in trace args as partition_idx).
2. PREFER the consumer-side DIRECT HoL metric over any cross-span join: integrate wait.block_fetcher_get duration where (ready_ahead>0 AND !i_done) — emit wait.hol{partition_idx, ready_ahead, i_done} at wait-entry (chunk_fetcher.rs:1288), via a GZIPPY_TIMELINE-gated per-partition completion-timestamp registry workers write + consumer reads. No cross-span join = no key-mismatch trap. (This is a conservative LOWER bound on HoL — good, won't manufacture a phantom.)
3. Speculative 42-vs-36: add explicit chunk.accept{partition_idx, decode_complete_ts} instant at the acceptance point (don't infer acceptance post-hoc).
4. New `fulcrum hol` view (NOT a /tmp hand-script — user constraint).
5. DISCRIMINATING (disproof-driven, not HoL-confirming): in the SAME trace decompose consumer-thread wall into (i) wait.hol integral [HoL idle, term A] vs (ii) time INSIDE consumer.window_publish_* spans [serial consumer WORK = output-write B + apply_window C] vs (iii) handoff. If residual = (i) ⇒ HoL; if residual = (ii) ⇒ serial consumer work (write/apply_window), HoL is a red herring → split (ii) write-vs-apply_window. Rule out memory-bandwidth cheaply: T8-vs-T16 mean worker.decode_chunk DURATION (BW contention inflates decode duration, not consumer idle).
STILL the open question: which term explains the residual that grows 62→115→394ms with T + the negative T8→T16 scaling. Attempt #2 must DISCRIMINATE, not just confirm HoL.

## 2026-06-01 — HoL FALSIFIED + "decode speed" verdict is STILL the attribution trap; real lever = PLACEMENT (advisor a1e3f627, vendor-grounded)
HoL agent (measure/hol-latency, c_i/p_i joined by nearest-start_bit 36/36) DISPROVED head-of-line as the term: Σ HoL = 2/29/4ms T4/8/16 — tiny, non-monotonic, nowhere near residual 62/115/394ms. Out-of-order DEPTH grows 4→12→18 (symptom, not idle slack). The agent's REPLACEMENT verdict ("lever = gating-chunk decode SPEED") is ALSO unsound — advisor caught it as the same critical-path-occupant trap, now grounded in VENDOR CODE:
- rapidgzip's in-order frontier carries CHEAP applyWindow (marker replace); expensive decodeChunk runs in BlockFetcher PREFETCH threads AHEAD of the frontier (GzipChunkFetcher.hpp:520-583 queueChunkForPostProcessing/queuePrefetchedChunkPostProcessing; BlockFetcher.hpp:289-315 prefetch pipelining). gzippy's frontier is gated on DECODE RUNNING. ⇒ the gap is PLACEMENT (gzippy decodes ON the frontier; rapidgzip prefetched it AHEAD so only applyWindow remains), NOT per-byte decode rate.
CONFOUND-FREE HEADLINE: T8-noSMT gzippy 1.47× slower than rapidgzip (cleanest cell, no SMT). 
T16 NEGATIVE SCALING IS SUBSTANTIALLY SMT, NOT STRUCTURAL: rapidgzip (which CANNOT have HoL/placement issues, resolves ahead) regresses IDENTICALLY T8→T16 (rg +22.7%, gz +20.8%); pure-SMT cost +13.1%; gating bootstrap span doubles 60→128ms under load. ⇒ DISCARD T16 as structural evidence. (This partly FALSIFIES the standing "close T16 negative-scaling" goal premise — T16 regression is mostly SMT contention shared with rapidgzip, not a gzippy structural defect. The durable structural gap is T8-noSMT 1.47×.)
UNEXPLAINED-BY-SMT: T4→T8 residual grew 62→115ms at constant per-byte rate (both ≤8 phys cores, no SMT) ⇒ points at PREFETCH-DEPTH/PLACEMENT (decode not keeping ahead of the frontier), NOT inner-loop rate (which is T-invariant per byte).
TWO LEVERS, now crisp: (A) inner-loop: window-absent decode faster per byte (~15% bounded). (B) STRUCTURAL placement: prefetch/decode the in-order gating chunk AHEAD so only applyWindow is on the frontier (port rapidgzip BlockFetcher prefetch + queuePrefetchedChunkPostProcessing). Data points at B (vendor frontier=applyWindow not decode; residual grows at constant rate; gating span doubles under load = placement/contention not fixed rate).
THE ONE CEILING EXPERIMENT (rule-3 compliant, discriminates A vs B): FRONTIER-PLACEMENT ORACLE — leave per-byte decode rate EXACTLY as-is, but force the in-order gating chunk fully decoded BEFORE the consumer reaches it (eager-prefetch next-unprocessed chunk off the frontier; consumer pays only applyWindow). Interleaved wall: drops toward rapidgzip ⇒ PLACEMENT (Lever B, port vendor prefetch); doesn't drop ⇒ per-byte SPEED (Lever A) is the ceiling, earned. Run THIS before any inner-loop grind OR the known-window oracle (which conflates A+B). Do NOT start inner-loop work first.

## 2026-06-01 — GOAL RETARGETED (user-set): close the confound-free T8-noSMT 1.47x gap via PLACEMENT
SUPERSEDES the "close T16 negative-scaling" milestone (2026-06-01 earlier) — that was partly a confound: T16 regression is substantially SMT contention rapidgzip ALSO pays (regresses identically +22.7% vs gz +20.8%), not a gzippy structural defect.
NEW MILESTONE GOAL: close the CONFOUND-FREE gap = T8-noSMT, where gzippy is 1.47× slower than rapidgzip (cleanest cell, no SMT, frozen-clock harness). LEVER (vendor-grounded, placement not rate): get decode OFF the in-order frontier — prefetch/decode the gating chunk AHEAD so the consumer pays only applyWindow, as rapidgzip does (port BlockFetcher prefetch-ahead + queuePrefetchedChunkPostProcessing; GzipChunkFetcher.hpp:520-583, BlockFetcher.hpp:289-315). 
DISCIPLINE unchanged (golden rule): whole-system — getting decode off the frontier may re-expose worker-bound or BW terms; speed each piece AND tune structure; re-measure the WHOLE after each. Loop: measure→design→measure. Measured ONLY via scripts/bench/clean_bench.sh (RUN_TRUSTWORTHY, sd<5%; T8-noSMT pin 0,2,4,6,8,10,12,14 is the headline cell; T16 reported but SMT-flagged, not a structural target).
NEXT EXPERIMENT (before any port): FRONTIER-PLACEMENT ORACLE (rule-3 ceiling test, discriminates placement-B vs speed-A) — rate unchanged, force gating chunk decoded AHEAD of the consumer; wall drops toward rapidgzip ⇒ placement is the lever (port vendor prefetch); doesn't ⇒ inner-loop speed is the ceiling.

## 2026-06-01 — FRONTIER-PLACEMENT ORACLE: causal TIE ⇒ placement (Lever B) REFUTED; + measurement-completeness definition (advisor affb8011)
ORACLE (rule-3 REMOVAL test, branch feat/frontier-placement-oracle @9ae9048, byte-correct sha==gzip both ON/OFF): force the in-order frontier chunk decoded AHEAD (placement changed, per-byte RATE unchanged). neurotic x86 frozen-clock, N=9 interleaved, RUN_TRUSTWORTHY=true, sd<1.5%:
| T | oracle/base wall | ratio gz/rg |
| 4 | 0.992 TIE | 0.861/0.854 |
| 8 noSMT (headline) | 1.012 TIE | 0.739/0.747 |
| 16 SMT | 0.997 TIE | 0.608/0.606 |
All removal deltas within sd ⇒ TIE. Oracle BIT (not no-op): frontier_already_inflight 34/36=94% — gzippy ALREADY decodes the frontier ahead off the consumer thread, like vendor BlockFetcher. Consumer frontier-wait = 92.9% of wall and is the frontier chunk's OWN decode time; pool.pick.wait dropped 525→420ms but did NOT convert to wall.
⇒ PLACEMENT (Lever B) CAUSALLY REFUTED — porting vendor prefetch-ahead is redundant (gzippy already does it). The gap is per-byte DECODE work on the in-order frontier chunk. Refutes my/advisor's earlier vendor-code-inferred "placement" verdict (that was source-reading, not measurement — exactly the trap the user flagged).
BUT NOT YET "decode RATE = inner-loop lever": the oracle proves NOT-placement, it does NOT prove speeding the inner loop pays. The advisor (completeness review) warns: "busy decoding" on the frontier may be MEMORY-BANDWIDTH/page-fault STALLED, not compute-bound — and the uncore lock does NOT remove BW CONTENTION (only MC clock). The 17-41% model residual is NAMED by prior work as page-fault/allocator/syscall (project_inflate_not_bottleneck, project_three_way_perf: gap is memory stalls, instructions ~equal). So the live question is now COMPUTE-bound (inner-loop Lever A real) vs MEMORY-bound (neither placement nor inner-loop helps; it's allocator/BW).

## 2026-06-01 — MEASUREMENT COMPLETENESS definition (advisor affb8011) — what ends the guessing
The disagreements (HoL/placement/rate, all attribution-then-overturned) prove the instrument is CAUSALLY incomplete, not descriptively. Corrected completeness (5 clauses):
1. Critical-thread wall = labeled spans + an EXPLICIT untraced-gap bucket attributed by perf-on-critical-TID. residual→0 is UNACHIEVABLE (spans can't see page-fault/alloc/syscall/futex = the named residual); target = residual<noise with remainder NAMED, not zero.
2. Per-stall START-TIME PAIR (S1) = start_time(decode_i) vs earliest free worker with chunk i ADMISSIBLE — the clean placement-vs-rate discriminator (NOT occupancy: "96% in-flight" = started, not started-as-early-as-admissible). [The ORACLE above already answered placement causally = the perturbation form of S1.]
3. "busy worker" qualified by PRODUCTIVE-vs-STALLED (IPC/stall-cycles per span) — raw busy mislabels BW-stall as rate. Generator only; perturbation is the verdict.
4. Cross-tool = per-segment SUBTRACTION of MEASURED quantities (min: rapidgzip stall-time worker-occupancy at matched operating point) — NO source-code inference in the verdict loop.
5. BINDING CLAUSE (the user's 3-part def omitted it): descriptive completeness (sum-to-wall) NEVER implies causal completeness (a region can be 100% of critpath and wall-NEUTRAL, per project_copies_wall_neutral). Every surfaced lever needs a CAUSAL PERTURBATION. Attribution proposes; perturbation disposes.
NEXT (resolves compute-vs-memory): S2 = explicit untraced-gap bucket + perf-on-critical-TID on a clean neurotic run — converts the 17-41% residual into NAMED buckets (page-fault X% / alloc Y% / futex Z% / decode-compute W%). + S3 IPC-per-decode-span (productive vs memory-stalled). If the frontier decode is memory-stalled ⇒ inner-loop grind is ALSO a dead lever and the real target is allocator/page-faults/output-materialization (the long-standing 33% memmove/clear_page finding). If compute-bound ⇒ inner-loop multi-symbol decode is finally earned. Build S2/S3 into Fulcrum (user wants it IN the tool).

## 2026-06-01 — Fulcrum TOTAL steps 1-3 landed; convergence is HALF-real (advisor a969d00b + code-read)
BUILT (Fulcrum feat/profile-bundle, on origin/main 9b6b901..acd56f3, no collision w/ copy-edit agent; gzippy residual instrumentation PR #121 byte-identical, 31/31 routing, sha-verified): ProfileBundle keyed (tid,region,partition_idx) with per-thread timestamp-containment join (anti-naive test passes); `fulcrum schedule` (S1) + `fulcrum decompose` (getrusage residual tier); perf-spike PASSED (guest-side per-thread perf record + PEBS-mem work after host paranoid=1; S3 buildable). Box restored baseline.
TRUSTWORTHY (bank it): PLACEMENT is causally DEAD — frontier-placement oracle TIE (rule-3 removal) + S1 `schedule` RATE-100% AGREE. The consumer waits on the frontier chunk's OWN decode finishing, NOT on ready-work-unused. (S1 first mis-reported PLACEMENT 54.6% from a concurrent-idle-worker summing artifact — fixed w/ interval-UNION + decode-start-deferral + regression test.)
NOT TRUSTWORTHY (the trap, caught before recording): `fulcrum decompose` "minor page-faults = 36.9% of wall (207,230 faults)" is the CPU-SUMS-LIE. Code (decompose.rs:233 `100*modeled_us/wall`): modeled_us = 207k faults × ~1µs midpoint = ~207ms, but those faults are SUMMED ACROSS 8 WORKER THREADS and divided by SINGLE-THREAD wall ⇒ ~26ms/core inflated to "36.9% of wall". NO critical-path filter (counts faults in every region on every worker). The 2026-05-31 critical-path trace already ruled worker-side memory ~37× OVERLAPPED SLACK. AND decompose-faults likely DOUBLE-COUNTS with S1-RATE: minor faults happen INSIDE the decode span (first-touch of 503MB fresh output pages) that S1 attributes to RATE — same time at two resolutions, not two findings.
⇒ "RATE-dominant" = decode WALL-TIME on the critical path, mechanism-AGNOSTIC. Does NOT license inner-loop decode-speed (the decode span = compute + its OWN faults + mem-stalls). Z2 blanket pre-fault already FALSIFIED (-300ms, serialized faults); only untried page-fault lever = per-thread REUSE POOL (rapidgzip's rpmalloc-with-pthread-hooks faults ~½: 79k vs 169k, project_allocator_framework) — but only worth it if faults are ON the critical-path frontier decode, not slack.
THE ONE DISCRIMINATOR (buildable today, perf-spike passed): S3 per-TID retiring-vs-CYCLE_ACTIVITY.STALLS_MEM_ANY SCOPED TO THE FRONTIER-DECODE SPANS the consumer blocks on (NOT summed across workers — that's decompose's slack-trap). High retiring/IPC ⇒ compute-bound ⇒ inner-loop lever (~15% bounded). High mem-stall/page-walk ⇒ allocator/reuse-pool lever. CHEAP precheck pair first (an afternoon): (1) [DONE] confirmed decompose is CPU-sum-over-wall not critical-path — demote the 36.9%; (2) measure rapidgzip's fault count at the SAME operating point (have 79k vs 169k from 2026-05-28; refresh on the clean harness). Do NOT start inner-loop OR allocator rework until S3-scoped-to-frontier names compute-vs-stall.

## 2026-06-01 — ALLOCATION instrumentation design (advisor a317ace) — premise may be ALREADY FIXED; shim would be BLIND
User wants allocation FULLY observable in one run (timing/pressure/cause/source) + the S3 frontier-scoped mem-stall discriminator. Advisor CODE-READ found the design's core assumption FALSE:
- gzippy ALREADY has a custom per-thread rpmalloc binding (src/decompress/parallel/rpmalloc_alloc.rs) with ensure_thread_initialized()→rpmalloc_thread_initialize() (the "MISSING" fix from feedback_z_allocator/project_allocator_framework is PRESENT in the typed path). Hot 503MB buffers (U8/U16 chunk/marker/output types) route through THIS, BYPASSING Rust #[global_allocator] (which is rpmalloc only behind non-default `global-rpmalloc`, else system glibc). dump_global_stats() already wraps rpmalloc stats.
- ⇒ A #[global_allocator] shim OR LD_PRELOAD malloc shim is BLIND to the hot path (rpmalloc-sys calls mmap directly, not malloc). Would produce an authoritative-LOOKING dataset blind to the mechanism — worse than the CPU-sum lie.
STEP 0 GATE (before building ANYTHING): re-measure today's binary on the clean harness — confirm path=IsalParallelSM, confirm rpmalloc_thread_initialize FIRES PER WORKER (guard at rpmalloc_alloc.rs:271 — verify per-thread not once-global), and confirm the 169k-vs-79k page-fault gap STILL EXISTS. If the typed path already closed it ⇒ allocation investigation is OVER, pivot to S3-only (compute-vs-memory of the frontier decode).
CORRECTED signal set (if gate passes): KEEP per-(tid,region) {alloc count, total bytes, peak outstanding} + rpmalloc SPAN-CACHE REUSE RATE (centerpiece, from rpmalloc_thread_statistics — the rapidgzip-delta signal) + MADV_DONTNEED/munmap return-to-OS churn + THP-backed fraction of output region (/proc/self/smaps_rollup AnonHugePages delta — first-touch multiplier: sub-2MiB buffers miss THP = 512× more clear_page) + RUSAGE_THREAD minflt delta. CUT: per-alloc event stream (observer effect across 8 threads), per-alloc backtraces (call sites already known), sub-2MiB detail, brk/arena frag, NUMA (unless numactl=2 nodes), the shim entirely. CAPTURE: instrument rpmalloc_alloc.rs directly (thread-local counters, GZIPPY_TIMELINE-gated, byte-identical off) + region-boundary snapshots — aggregate-at-boundary NOT per-event ring (kills Heisenberg).
CRITICAL-PATH FILTER (not the CPU-sum trap): window by CONSUMER-STALL INTERVALS — "alloc/fault on chunk-K's decoder thread WITHIN the consumer's stall blocked on K", reported as fraction-of-stall-explained, never summed absolute ms. (Frontier identity moves over the run; static frontier-span filter would miss chunks or re-sum.)
DISCRIMINATOR ORDER: S3 FIRST (per-TID retiring-vs-STALLS_MEM scoped to the blocking decode) — high-IPC/retiring ⇒ COMPUTE-bound ⇒ inner-loop lever, allocation DEAD, no perturbation needed (bundle KILLS the hypothesis cheaply). If mem-stall-bound ⇒ alloc+THP+reuse LOCALIZE faults to output-buffer vs back-ref-gather (both look identical in S3 — only alloc-detail distinguishes). THEN mandatory PERTURBATION (pre-faulted/warm-reuse buffer oracle, decode rate unchanged, interleaved wall): drops ⇒ reuse-pool lever; flat ⇒ faults were overlapped slack (copies-wall-neutral / Z2 precedent). 
HONEST CEILING: the bundle makes allocation fully DESCRIBABLE + can KILL the hypothesis if compute-bound + pre-LOCATE the one oracle worth running if mem-bound. It can NEVER confirm the reuse-pool moves the wall without the oracle (counterfactual = perturbation by definition). "Impossible to not know something" = achievable for description, NOT for "is it the lever".
RUNS: separate — (A) alloc-clean+wall, (B) PEBS-mem (perturbs, ratios-only), join on ratios/identities NEVER splice PMU times into wall; (C) oracle if A+B survive. Never one run (Heisenberg).

## 2026-06-01 — FULCRUM FALSE-CONFIDENCE AUDIT (agent afed9036, 4 failing tests at fulcrum-audit branch audit/false-confidence) + memory cleanup
4 CONFIRMED false-confidence sources (each demonstrated by a FAILING test, not just asserted):
- M1 CRITICAL — model.rs:255-278 residual is a TELESCOPING TAUTOLOGY (the +0.0% bug REINCARNATED): L_resolve=Σgaps/(N-1), Σgaps=last_pub-first_pub ⇒ publish_chain+tail=observed_wall EXACTLY for ANY publish timing; main.rs:856 prints "MODEL CONFIRMED" unconditionally when publish-chain binds. Test: 3 wildly different publish patterns, same endpoints → residual=0 all three. The field doc BRAGS "N·mean reconstructs that span exactly" — saw the reconstruction, didn't see it kills falsifiability. FIX: predict wall from an INDEPENDENT quantity, or drop "CONFIRMED" for the binding term (worker-bound branch is NOT tautological). ⇒ "MODEL CONFIRMED" must NEVER corroborate a convergence/TIE claim — it's unfalsifiable.
- D1 HIGH — decompose.rs cross-thread CPU SUM / single-thread wall: 16thr×2000faults×1µs=32ms over 10ms wall = 320%-of-wall printed as the VERDICT (this is exactly the CPU-sum-lie I caught manually; the caveat is in the header but the >100% number is still the headline). The ×1µs/×50µs/×3µs cost constants are FABRICATED (count→time). FIX: attribute to consumer/critical-path thread ONLY, or report per-thread rate "not wall-additive at T>1", never ÷wall.
- D2 MED — decompose.rs:96 named_residual_frac().min(1.0) CLAMP HIDES D1 (renders 320% as "100% named = success"). FIX: drop the clamp, frac>1.0 IS the over-count signal.
- S1 MED, THREATENS THE LIVE VERDICT — schedule.rs:236-255: a stalled chunk with NO worker.decode_chunk{chunk_id=i} span ⇒ decode_start=+INF ⇒ whole stall classified PLACEMENT. A missing/renamed decode span (measurement gap) becomes a confident PLACEMENT verdict. FIX: dec.is_none() ⇒ COVERAGE-GAP bucket (or default RATE), never PLACEMENT.
5 read-confirmed gaps: bundle.rs:95 `concurrency` field documented but NEVER computed (stays 0.0=false purity); region_hw.rs:607 reconcile auto-PASSES on 0% perf (silent-empty=agreement); main.rs:1437 clock-splice (perf-stat intervals hardcoded start=0 vs absolute µs trace ⇒ counters silently dropped); main.rs:1424 clock_base mismatch is a stderr WARNING not an abort; region_hw.rs:263 (bl2+dram).min(1.0) clamp masks intentional double-count.
SOUND (audited clean): trace.rs B/E pairing; bundle.rs per-thread timestamp-containment Joiner (anti-naive tests test real failure modes); schedule.rs overlap_union (the >100% artifact fix is genuine); consumer.rs (busy+idle==span assert, single-thread so %-of-wall legit).
**LIVE-VERDICT IMPACT:** "PLACEMENT-dead via S1 RATE-100%" is AT RISK from the S1 missing-decode bug — RATE is sound ONLY IF every stalled chunk has a matching worker.decode_chunk span with that exact name+chunk_id. VERIFY the production trace emits that span for bootstrap/speculative chunks before trusting RATE-100%; re-run with the coverage-gap bucket. (The converged-oracle TIE is a CAUSAL perturbation, NOT one of these views ⇒ NOT undermined — but must NOT be cross-validated by the model-view residual.)
MEMORY CLEANUP (same task): deleted 31 stale state-snapshot memories (perf-number/CPU%/gap-ratio/"lever is X" headlines that this session or later memories CONTRADICTED — e.g. sm_scaling "1.10× parity" refuted by clean 1.47×; wall_is_consumer_critical_path "lever=resolve-ahead" causally refuted by oracle TIE; inflate_not_bottleneck/memmove "33% CPU" = the CPU-sum framing we reject; plus 12 dated orphan snapshots). Kept process rules, still-true falsifications, current wins, references. Index now 27/27, links resolve, no orphans.

## 2026-06-01 — THE ANSWER: gap is TLB/PAGE-WALK stalls, NOT compute, NOT simple-reuse (alloc+S3 run, frozen T8-noSMT)
gzippy vs rapidgzip, frozen, T8-noSMT:
- instructions 10.90B vs 10.84B = EQUAL (1.005×) — NOT a compute/algorithm gap.
- cycles 7.17B vs 5.06B (1.42×), wall 0.273 vs 0.186s (1.47×) — the gap is STALLS.
- DTLB STORE-walk-active 123M vs 38M (3.26×!), DTLB load-walk 155M vs 83M (1.86×), iTLB-walk 7.6M vs 3.8M (2×). gzippy walks the page tables ~3× more.
- 99.6% of minor page-faults are inside worker.decode_chunk; THP=0 (4KB base pages, no huge pages); rpmalloc maps 3.4GB for 503MB output (huge_alloc path), unmapped_total=0 (NOT munmap-churn).
- SLAB warm-reuse oracle (GZIPPY_SLAB_ALLOC=1, retain huge blocks resident): minflt UNCHANGED (~220k both), wall UNCHANGED (within noise), sha MATCH. ⇒ simple retain-the-allocation reuse-pool REFUTED as a lever (exactly as the "description can't confirm a lever, needs perturbation" rule predicted; converges with Z2/copies-wall-neutral precedent).
MECHANISM: gzippy's decode writes into freshly-mapped, NON-THP (4KB) pages → massive TLB/page-walk pressure (3.26× store-walks) on first-touch → memory-stall cycles that are the 1.47× gap. NOT inner-loop decode speed (instructions equal), NOT the simple reuse-pool (SLAB tie). 
LEVER this points at (UNTRIED, distinct from everything refuted): THP / huge-pages for the decode/output buffers (collapse 512 4KB first-touch faults → 1 2MB fault; cut the page-walk pressure). rapidgzip's rpmalloc spans are THP-friendly; gzippy's buffers sized near 2MiB LARGE_SIZE_LIMIT may MISS THP (segmented_buffer.rs:9-11). NEEDS a causal perturbation: THP-on (madvise(MADV_HUGEPAGE) on the buffers, or transparent_hugepage=always) → does DTLB-walk drop AND wall drop? (NOTE: this is allocation-shaped but it's about PAGE SIZE not reuse — the SLAB-reuse refutation does NOT pre-empt it.) [pending advisor + perturbation; do NOT commit the THP rework without the THP-on A/B showing wall moves.]

## 2026-06-01 — DECODE VISUALIZER design (advisor a8b0c06c) — domain-aware verdict panel, 3 blocking prereqs
User wants an HTML viz of the parallel-SM decode (threads, flow, time, weight, blocking) for BOTH tools, to "ask better questions" — domain-aware NOT a generic flamegraph. Advisor-validated design w/ 3 BLOCKING corrections (a viz lies more persuasively than a number):
1. WEIGHT must encode WALL-RELEVANCE not CPU-sum: the CONSUMER SPINE (single in-order thread) IS the wall (its waits+publishes+iter tile end-to-end = wall by construction); worker decode boxes that finish BEFORE their consumer turn = SLACK → render FOLDED/thin-grey, NOT fat (invert the flamegraph convention, label the inversion). A fat overlapped worker box read as the bottleneck = the CPU-sum-lie, visually.
2. BLOCKING-ARROW primitive is currently UNBUILDABLE: worker.decode_chunk span has NO partition_idx arg (bare SpanGuard::begin), and the trace has NO causal flow edges (no ph:s/f). PREREQUISITE: add partition_idx to worker.decode_chunk + emit Chrome flow events (ph:s on worker publish, ph:f on consumer consume, id=partition). Until then "critical path" is an unproven OVERLAP HEURISTIC — must be LABELED "heuristic (no causal edges)" not drawn as fact. The rate-vs-placement (S1) distinction = per-stall glyph: at wait{K} start, was K's decode E-closed (◆ ready=placement) or still open (● running=rate)? + a rate/placement histogram = the deliverable that picks the lever.
3. CROSS-TOOL side-by-side manufactures a PHANTOM gap from instrumentation asymmetry (gzippy ~15 span names, rapidgzip patch coarser): MUST canonicalize BOTH to 6 phases (dispatch/decode/resolve/publish/output/wait) before rendering + show per-tool COVERAGE-% (uninstrumented wall = grey-hatched "UNKNOWN", NEVER blank=idle). 
HONESTY GUARDS (load-bearing UI): wall-reconciliation banner (viz wall vs clean_bench wall, red if >spread); B/E-mismatch counter (dropped E = fake giant bar); sampled counters (faults/TLB) as "~" badges on the FOLDED worker track NOT the wall spine (they're CPU-region facts, decode is wall-slack). TECH: static HTML + inline JS + SVG (DOM-inspectable, not canvas), Python reducer reusing timeline_analyze.py load_events/pair_spans → small derived JSON → presentational HTML. Custom builds the VERDICT PANEL (spine + glyphs + histogram + 6-phase bars ×2 tools); Perfetto handles raw 40×16 timeline exploration (don't rebuild zoom/pan).

## 2026-06-01 — STRUCTURAL MECHANISM nailed (alloc+S3 agent, fulcrum `alloc` view shipped): huge-path buffers bypass span-cache ⇒ first-touch TLB-walks
Step-0 gate PASSED: fault gap is OPEN and WIDER — gzippy 223k vs rapidgzip 87k minor-faults = 2.55× (was 2.15×). rpmalloc_thread_initialize DOES fire per-worker (rpmalloc_alloc.rs:261-273, thread_local guard) — the "missing thread-init" memory is FALSE for the typed path, does NOT explain the gap.
S3 ANSWER (frozen T8-noSMT, whole-process TMA + PMU): instructions EQUAL (10.90B vs 10.84B = 1.01×) ⇒ COMPUTE/inner-loop lever FALSIFIED outright (no work gap). Cycles 1.42× = pure stalls. TMA memory_bound 26.3% vs 14.8% (+11.5pp), core_bound only +2.2pp, retiring 25.9% vs 33.2% (gzippy retires LESS). Stalls = TLB page-walks: dtlb_store_misses.walk_active 3.26×, dtlb_load 1.86×, L3-miss-loads 7.8×.
LOCALIZATION (fulcrum alloc view): worker.decode_chunk carries 99.6% of minor faults; THP backing = NONE (every 503MB output page = separate 4KB fault + TLB walk).
**THE STRUCTURAL DIVERGENCE (the actionable mechanism, not just "non-THP"):** gzippy's hot buffers are >2MiB ⇒ take rpmalloc's HUGE path ⇒ BYPASS the span-cache ⇒ fresh OS mapping every chunk ⇒ first-touch on never-reused non-THP pages. rapidgzip uses 128KiB SUB-BUFFERS ⇒ span-cached ⇒ WARM-REUSED ⇒ 2.55× fewer faults, 1.9× lower DRAM-bound. gzippy maps 3.4GB for 503MB output, unmapped_total≈0 (first-touch of fresh content, NOT munmap-refault).
PERTURBATION: SLAB warm-reuse oracle (GZIPPY_SLAB_ALLOC=1) = TIE (faults ~218k→223k, wall ~0.262→0.270s, sha MATCH) ⇒ the existing retain-list reuse-pool is wall-neutral (retains the >2MiB ALLOCATION but pages still first-touch). Simple-retain refuted.
THE LEVER (un-refuted, reframed): make gzippy's decode/output buffers SPAN-CACHEABLE + WARM-REUSED like rapidgzip — i.e. SEGMENT the >2MiB monolithic buffers into sub-2MiB chunks that hit rpmalloc's span-cache (warm pages, no re-fault) AND/OR THP-back them (madvise MADV_HUGEPAGE: 512 4KB faults → 1 2MB fault). NOT inner-loop (instructions equal), NOT simple-allocation-retain (SLAB tie). This is the structural buffer-segmentation lever (earlier project_global_optimum anticipated it; advisor refuted the v1 segmentation as a vendor no-op but that was pre-this-mechanism). 
NEEDS A PERTURBATION before commit: a THP-on / warm-page / sub-buffer-segmented oracle — does dtlb_store_walk DROP and wall DROP together? Description (this run) LOCATES the mechanism; it cannot confirm the lever moves the wall. The page-size/segmentation oracle is the next experiment. CODE SHIPPED: gzippy feat/alloc-instrument (rpmalloc_stats.rs, byte-identical), fulcrum feat/alloc-bundle `fulcrum alloc` view (anti-CPU-sum test, locates-never-claims-a-lever).

## 2026-06-01 — LEVER CHOICE (reuse vs THP vs both) — advisor aa90a0b3 corrects the plan + a MANDATORY disproof owed first
USER Q: reuse, huge-pages, or combine? ADVISOR verdict:
- THP-on is a CONFOUNDED oracle, NOT faithful: THP attacks TLB-WALKS only; reuse attacks FAULTS+ZEROING; both are elevated in our data ⇒ a THP-on TIE does NOT exonerate reuse (zeroing-bound case survives). DO NOT run THP-on first.
- SLAB-retain TIE correctly read: retaining the VIRTUAL allocation ≠ retaining the PHYSICAL commit. Vendor rpmalloc source: 128KiB = MEDIUM class, retain=2/overflow=8 ⇒ freed pages stay COMMITTED → warm-reuse skips faults. LARGE class retain=1 + glibc both DECOMMIT/munmap → big-buffer reuse re-faults. ⇒ "reuse" must = segment to ≤128KiB BACKED BY A RETAINING POOL (small+retaining-allocator), NOT merely small buffers.
- RIGHT lever = SEGMENT+REUSE-POOL (rapidgzip's model; wins with THP=NONE ⇒ reuse alone sufficient). THP = conditional residual top-up, NOT co-equal, NOT build-both-speculatively.
- LIVE CONTRADICTION to resolve (load-bearing): alloc agent observed gzippy dumping RPMALLOC stats (3.4GB mapped, rpmalloc_thread_initialize fires per-worker) ⇒ gzippy uses rpmalloc for hot buffers. Advisor reasoned from old memory that gzippy links GLIBC (project_allocator_framework: global-rpmalloc falsified). Can't both be true — the live rpmalloc stats dump suggests the TYPED path uses rpmalloc directly (the OLD note was about the GLOBAL allocator). MUST verify which allocator backs the hot >2MiB buffers + its size-class retain policy before trusting the segmentation mechanism.
- MANDATORY DISPROOF OWED FIRST (cheapest ceiling, ~0 code): the faults are 99.6% in worker.decode_chunk, and prior findings (project_copies_wall_neutral −0.0% wall; "worker side ~37× overlapped slack") warn the page cost may be WALL-NEUTRAL slack. Before building ANY lever: slow-inject the fault/decode cost in decode_chunk + frequency-neutral sleep control, measure interleaved wall. Flat ⇒ ALL THREE levers DEAD (page direction is overlapped slack). Moves ⇒ proceed. This, NOT THP-on, is the true cheapest ceiling oracle.
CORRECTED ORDER: (0) fault-cost slow-injection disproof [is it on the wall at all?]; (1) resolve rpmalloc-vs-glibc + size-class retain; (2) shrink-buffer fault probe (win⇒proceed, tie⇒inconclusive on glibc); (3) thread-local ≤128KiB reuse POOL = the ship code; (4) per-buffer madvise(MADV_HUGEPAGE) LAST + host-neutral (NEVER transparent_hugepage=always host-wide on the shared box — perturbs other guests). DESCRIPTION located the mechanism; only the perturbation confirms a lever.
VISUALIZER shipped (feat/decode-visualizer, tools/decode_viz/: tested reducer + SVG verdict panels gz/rg T4/8/16; consumer-spine=wall, folded-slack workers, coverage/reconciliation banners, blocking edges labeled HEURISTIC pending flow-event instrumentation). REVEALS: T4/T8 rate-bound (T8: 26 rate vs 10 placement), but T16 FLIPS to placement-bound (3 rate / 33 placement) ⇒ THE LEVER DIFFERS BY THREAD COUNT (T4/8=decode-rate/page, T16=consumer ordering). 6-phase bars show gzippy decode wider = RATE not extra machinery (same 38/4 window-absent/clean shape).

## 2026-06-01 — GOAL (user-set): THOROUGHLY EXPLORE the performance of adjusting page-walks
The full exploration matrix (each cell = a CAUSAL PERTURBATION on the clean harness — frozen-clock, T-sweep, interleaved N>=7, sha-verified, measuring BOTH dtlb_store/load_walk_active AND wall, so we never confuse "walks dropped" with "wall moved"):
E0 [RUNNING, agent acd776] — FAULT-COST AMPLIFICATION go/no-go: GZIPPY_FAULT_AMP=K re-faults decode buffers K× (madvise DONTNEED + re-touch) + sleep control. Wall ∝ K ⇒ on critical path; flat ⇒ overlapped slack ⇒ WHOLE page direction dead. GATES everything below.
E1 [RUNNING, agent acd776] — ALLOCATOR RESOLUTION: rpmalloc vs glibc for the hot buffers + size-class retain/decommit policy. Determines whether sub-2MiB segmentation warm-reuses alone or needs an explicit pool.
IF E0 = on-the-wall, the exploration sweep (else document slack + pivot to consumer chain):
E2 — THP per-buffer: madvise(MADV_HUGEPAGE) on the decode/output buffers (host-neutral, NOT transparent_hugepage=always host-wide). Measure: does dtlb_store_walk collapse (expect ~512× fewer entries) AND wall drop? Verify THP actually GRANTED (/proc/pid/smaps AnonHugePages) — a tie could mean not-granted. Isolates the TLB-COVERAGE axis.
E3 — SUB-2MiB SEGMENTATION + REUSE POOL: per-worker thread-local pool of ≤128KiB buffers (rapidgzip's model: small + retaining). Measure faults (expect →rapidgzip's 87k) + walks + wall. Isolates the FAULT/ZEROING axis. (Plain segmentation w/o pool first, to test if the allocator retains; then +pool.)
E4 — COMBINE: THP + reuse-pool together. Does it add over the better single one, or is one dominant? (rapidgzip wins with THP=NONE ⇒ expect reuse dominant, THP marginal — but MEASURE.)
E5 — THREAD-COUNT shape: the visualizer showed T4/T8 rate-bound but T16 placement-bound. Run E2/E3/E4 across T4/T8/T16 — does the page lever's payoff differ by T (does it even help at T16 where the bottleneck flipped to consumer ordering)?
E6 — CROSS-TOOL confirm: after the winning lever, re-measure gzippy vs rapidgzip walks+faults+wall at T8-noSMT — did we close the 2.55× fault / 3.26× walk / 1.47× wall gap?
Each cell: perturbation + frequency-neutral control + interleaved sha-verified RUN_TRUSTWORTHY; record walks AND wall (decoupled); a lever "confirmed" only if BOTH the walk count drops AND the wall drops together (descriptive walk-drop alone ≠ wall lever — the binding rule). E0 is the gate; do not run E2-E6 if E0 says slack.

## 2026-06-01 — E0 = GO (faults ON the wall) + E1 (rpmalloc HUGE path munmaps; ≤128KiB span-cached) — page-walk direction CONFIRMED live
E0 FAULT-COST AMPLIFICATION (agent acd776, branch experiment/fault-amp-disproof, GZIPPY_FAULT_AMP=K re-faults decode buffers K× via madvise(DONTNEED)+re-touch, byte-identical off): amplifying the per-chunk fault/zero/TLB-walk cost moves the interleaved wall MONOTONICALLY: K=2/4/8/16 → +2.6/+23.5/+75.5/+176.7% (busy-spin); SURVIVES the frequency-neutral SLEEP control (+20/+24/+50/+78%) ⇒ NOT a clock artifact. Instrument positive-control-validated: minor-faults scaled linearly 190k→2.47M, dtlb-store-walks 2.55M→41.6M, output byte-identical (e114dd2) every K. Two self-test bugs caught+fixed pre-verdict (unaligned-madvise no-op; whole-page-zero corruption).
⇒ VERDICT: page faults are ON the critical path. project_copies_wall_neutral slack precedent does NOT generalize (a 212ms COPY was slack; per-chunk FAULTS are not — different costs). HONEST CEILING (rule 3): slow-down slope proves criticality, does NOT bound speed-up — E3 reuse-pool removal-oracle (faults→rapidgzip's 87k) sets the actual ceiling.
E1 ALLOCATOR RESOLUTION (reconciles the contradiction — BOTH were right, different allocators): pure-rust-inflate enables arena-allocator ⇒ hot U8 buffers (ChunkData::data, narrowed) = Vec<u8, RpmallocAlloc> → rpmalloc-sys 0.2.3 DIRECTLY (typed path); #[global_allocator] is glibc but the typed path BYPASSES it. rpmalloc size classes (span=64KiB): SMALL≤1KiB, MEDIUM≤31.5KiB, LARGE 31.5KiB–3.937MiB, HUGE >3.937MiB. The ~7–12MiB chunk `data` buffers take the HUGE path → free calls _rpmalloc_unmap(release=full) = IMMEDIATE munmap, NO span-cache, NO retain → every chunk re-mmaps + first-touch faults (matches 3.4GB mapped / unmapped≈0 = fresh-content not churn). MEDIUM/LARGE ARE thread-span-cached (400/100 spans).
⇒ sub-≤128KiB segmentation falls in span-cached classes ⇒ can WARM-REUSE on the same worker WITHOUT a hand-rolled pool (unlike SLAB-retain which kept the HUGE alloc and still first-touched). Explicit retaining pool only as fallback if the per-thread cache flushes. E3 = segment the flat huge data/narrowed U8 → measure faults→87k + walks + wall.
NEXT: E3 segment+(maybe-pool) build = the live lever. Then E2 THP-per-buffer (TLB-coverage axis, host-neutral madvise), E4 combine, E5 T-sweep (does it help at T16 where bottleneck flipped to consumer ordering?), E6 cross-tool confirm vs rapidgzip 2.55×/3.26×/1.47×.

## 2026-06-01 — DO NOT build E3 yet (advisor a59541c3, tree-read) — cheaper oracles + a CONTRADICTION to resolve first
Advisor read the actual tree and found 3 things that reshape the page-walk plan:
1. A RETAINING per-worker buffer pool ALREADY SHIPS (chunk_buffer_pool.rs, MAX_POOLED=8, take_u8/return_u8_to_worker, active under pure-rust-inflate) — yet the 40%→17% fault gap PERSISTS. CONTRADICTION with E1's "HUGE class munmaps every chunk, no reuse": the pool should hand back a warm ≥3.937MiB Vec, but rpmalloc HUGE-free munmaps it on drop BEFORE it reaches the pool — OR the pool is missing (capacity<min, MAX_POOLED overflow, cross-worker drop). MUST resolve which before building.
2. MADV_HUGEPAGE is ALREADY IMPLEMENTED + ALREADY FALSIFIED −38% (chunk_buffer_pool.rs:113-119,224-228, gated GZIPPY_HUGEPAGE). ⇒ E2 is NOT a cheap ceiling oracle, it's a re-test of a known loss; needs a written mechanism to re-attempt (rule #7) which we don't have. Also THP can't touch the ZEROING sub-cost (same bytes zeroed). DEMOTE E2.
3. segmented_buffer.rs (E3 SegmentedU8, 128KiB granule) ALREADY SCAFFOLDED but UNWIRED into ChunkData::data. E3 = "wire it in + fix marker/narrowed paths", cheaper than multi-day-from-zero, but STILL must not be wired without a ceiling number.
CHEAPEST CORRECT ORACLES (rule-#3 cost-REMOVAL, ~0.5-1 day each, NOT builders) — run BEFORE any E3 wiring:
(a) POOL HIT/MISS COUNTER on take_u8 (hit vs miss, and on miss whether capacity sufficient) — explains WHY the existing retention isn't closing the gap. If pool already hits warm ~90% ⇒ E3 is MOOT and the HUGE-munmap story is wrong. Run FIRST.
(b) NEVER-FREE / PRE-SIZED warm-pool oracle (forget-on-drop OR huge MAX_POOLED + max-size take so faults→one-time) swept T4/T8/T16 — REMOVES the fault cost. Wall drops toward rapidgzip's 87k ⇒ ceiling real, build E3. Flat ⇒ faults are overlapped slack despite E0's slope (slope proved criticality NOT ceiling, rule #3) ⇒ E3 DEAD before wiring. THE gate.
(c) DECOMPOSING AMPLIFIER: MADV_DONTNEED+touch (fault+zero+walk) vs MADV_FREE+touch (walk+fault, lazy-zero) vs resident-touch (walk only). Δ splits fault/zero/walk ⇒ tells us E3(fault+zero) vs THP(walk) is the right axis. (Prior: fault+zero likely dominates given 2.55× faults + steep K-response, but MEASURE — advisor priors 3-5× off.)
T-SWEEP belongs in oracle (b), BEFORE the build — the viz showed T16 flipped to placement/consumer-ordering, so E3 may be a T8-only win; confirm payoff is/ isn't T-dependent before committing.
CORRECTED ORDER: (a) counter [why isn't existing pool working] → (b) never-free oracle T4/8/16 [ceiling + T-dependence; GATE] → (c) decomposing amplifier [fault/zero/walk split] → ONLY if (b) shows real ceiling AND (c) says fault+zero: wire segmented_buffer into ChunkData (E3), measure vs the oracle ceiling → THP top-up only w/ a mechanism beating −38% → cross-tool confirm. Will NOT commit E3 wiring without (b)'s ceiling.

## 2026-06-01 — PAGE-WALK EXPLORATION COMPLETE: E3 = NO-GO (faults are overlapped SLACK), advisor-confirmed airtight. GOAL met as a valid negative result.
Three independent removal oracles (frozen, interleaved N>=9, gate N=21, sha-verified) + a 4th prior instrument all agree: REMOVING the page-fault cost does NOT move the wall.
- (a) POOL CONTRADICTION RESOLVED: the shipped per-worker pool (chunk_buffer_pool.rs, MAX_POOLED=8) DOES retain (137/138 returns kept — "HUGE-munmap-before-pool" story is WRONG), but warms only 62% of takes; the 38% fault-takes are RETURN-LATENCY (in-flight depth ~28 outruns the per-worker LIFO → take-before-return → fresh HUGE alloc → first-touch). Not capacity-miss, not cross-worker.
- (b) THE GATE = NO-GO: warm-pool oracle (uncap+pre-size) cut dtlb-store-miss 9-15% + minor-faults 3-8% → paired wall TIE T4 -1.7%/T8 -1.1%/T16 +0.0% (within stdev 1.9-5.9pp). True never-warmup ceiling (DECODE_REPEAT=N, faults→~104k near rapidgzip 87k): N=21 fully-warm wall T8 -1.43%/T16 +1.52% = SLACK. (N=15 -5.5% blip collapsed to -1.4% at N=21 = noise.)
- (c) sub-cost split (8× amp): zeroing +73.4% (dominant), fault-handler +13.2%, walk +5.7% — so the on-path-WHEN-AMPLIFIED cost is first-touch zeroing; confirms MADV_HUGEPAGE falsification (walks only 6%).
RECONCILIATION (E0-GO vs E3-NO-GO, advisor-confirmed CORRECT, rule#3 slope≠ceiling): real overlapped-slack→in-order-consumer physics. AMPLIFY binds (slowing every worker's faults past the consumer's slack propagates head-of-line into wall, MAX over a saturating resource). REMOVE doesn't (faster workers just finish earlier and WAIT — slack, invisible to wall; the binding constraint is the serial chain not worker throughput). Both true. 104k≠87k does NOT weaken it: the verdict rests on MONOTONE ELASTICITY (9-15% walk cut → 0% wall, NO steepening toward a knee), decisive independent of the absolute floor.
CAVEATS for the record (honest NO-GO, not over-claimed): (1) b1/b2 oracles carry TIE-BIASING confounds (pre-size working-set bloat; warm-pass cache/TLB pollution) — both bias TOWARD the null, so a real win could be masked; the claim is bounded as "no win detectable up to 9-15% walk removal / down to ~104k faults". (2) E3 proves FAULTS AREN'T THE LEVER — it does NOT prove the consumer chain; the chain is the SURVIVING hypothesis (from M0.3 ~86%-structural + the OPEN LEVER), not a thing E3 demonstrated. Don't manufacture a phantom.
WHY THIS MATTERS / NOT UNEXPLAINED: page-faults were always a CORRELATE/symptom (faults/sec ~equal across gzippy's own engines; rapidgzip faults 2.55× less BECAUSE its buffer-lifecycle/window-resolution structure is leaner — same structural cause, both downstream). Killing the correlate was NECESSARY (user mandated thorough page-walk exploration) and now DONE. The 1.47× is NOT unexplained — it REDIRECTS to the structural in-order consumer window-resolution chain.
PAGE-WALK GOAL: COMPLETE (valid negative — adjusting page-walks does NOT move the x86 T8/T16 wall; the cost is overlapped slack). E3 segment+reuse-pool CLOSED, not built (saved the multi-day wiring). MADV_HUGEPAGE stays falsified. 
NEXT (redirect, NOT part of the page-walk goal): the in-order consumer serial window-resolution chain — concretely chunk_fetcher.rs:1152 exact-equality-vs-range prefetch-reject invariant (OPEN LEVER). ONE next measurement: instrument consumer waits (wait.block_fetcher_get) in BOTH tools + confirm PREFETCH_REJECT_BY_GUARD frequency on a traced run, then causally slow-inject the resolution publish step (same E0/E3 discipline). Instrumentation from this exploration committed on experiment/pagewalk-oracles + experiment/fault-amp-disproof (byte-identical off, sha-verified).

## 2026-06-01 — RETRACTION: the page-walk NO-GO was OVERCLAIMED (user pushback correct, advisor ab93040b confirms)
The 3 removal oracles (GZIPPY_NEVERFREE_POOL uncap+presize, DECODE_REPEAT warm-pass) only WARMED + PRE-SIZED the still-MONOLITHIC 7-12MiB `data` buffer — they NEVER SEGMENTED it to 128KiB. So they proved "warm pre-sized monolith ≈ 0% extra wall" (fault-timing + realloc are slack), which is NOT the same as "rapidgzip's 128KiB-chunked structure ≈ 0%". Rule #7: a TIE on intervention X (warm monolith) is NOT a refutation of direction Y (segmentation). The NO-GO is RETRACTED; the segmentation lever is RE-OPENED and UNTESTED.
WHY the granule could still matter (the UNTESTED mechanism): a warm monolith still has the SAME resident-page working set (~3000 4KB pages for 12MiB) — warming stops re-FAULTING but does NOT shrink the TLB footprint or make the write-target L2-resident. A reused 128KiB segment = 32 hot pages that stay in dTLB+L2 across chunks. This maps DIRECTLY to the campaign's named gap (dtlb_store_walk 3.26×, memory-bound 33%) — you can have ZERO faults and still pay page-walks on a cold-in-TLB 3000-page write target. The vendor's STATED cliff cause ("overallocations and resizes") WAS tested-slack by our oracle; the UNSTATED residency cause was NOT. Lever survival hinges on the residency mechanism (vendor comments are often wrong about WHY their code is fast).
This is the ONE massive faithful-port divergence: gzippy data=monolithic rpmalloc HUGE (munmap-on-free), data_with_markers=monolithic GLIBC (doubly unfaithful), narrowed=separate 3rd buffer; vendor = all 128KiB span-cached + in-place-resolve-reuse-swap (1 buffer). SegmentedU8 scaffolded (segmented_buffer.rs:34) but UNWIRED.

## 2026-06-01 — OPTIONS to align gzippy's memory model with vendor + PRE-MORTEMS (advisor ab93040b)
O1 wire SegmentedU8(128KiB) into ChunkData::data only. PRE-MORTEM: TIEs because it segments the CHEAP clean suffix, leaves the 2×-byte u16 data_with_markers glibc monolith (the likely residency cost) untouched ⇒ "segmentation TIE" falsely kills the whole direction. Plus back-refs reaching 32KiB back must read ACROSS 128KiB segment boundaries → split-memcpy/byte-loop deopt in the hot emit_backref_ring → could REGRESS. Risk: med-high.
O2 O1 + segment data_with_markers + move it glibc→rpmalloc 128KiB. PRE-MORTEM: the u16 buffer is write-once-read-once-streamed (short life) → segmenting may only change fault-timing (already slack) not steady residency → TIE; but it's the highest-faithfulness-gain + biggest-byte buffer. Risk: med. BEST faithfulness/byte.
O3 in-place resolve + reuse-swap (kill the separate narrowed). PRE-MORTEM: DEAD/lowest-priority — (a) its primary benefit is copy-elimination which is ALREADY-FALSIFIED-slack (project_copies_wall_neutral); (b) vendor's std::swap(u16↔u8 reinterpret) is UB in Rust (transmute Vec<u16>→Vec<u8> = wrong Layout on free = heap corruption); faithful translation is same-allocation raw-pointer resolve (output≤input bytes) + ManuallyDrop+hand-built Layout — fiddly correctness minefield. Only live justification = residency (same as O1/O2).
O4 FULL faithful port (O1+O2+O3). PRE-MORTEM: multi-week; if it TIEs you can't tell which of 3 changes was the dud (violates one-variable); inherits O3 UB. Only justified AFTER a ceiling oracle says granule pays ≥X%.
O5 cap chunk size <rpmalloc huge threshold (~3.94MiB) so data lands in span-cached LARGE class, no SegmentedU8. PRE-MORTEM: 3.94MiB = ~960 pages, still blows L2 + TLB ⇒ captures the munmap/realloc win (already slack) but NOT residency ⇒ likely TIEs for the SAME reason warm-monolith did; also smaller chunks = more chunks = more consumer-reorder overhead (wall=consumer-chain) → could regress. LOW build value, but HIGH as a discriminating oracle.
THE FAITHFUL PORT = O4 (with O3 as raw-pointer-resolve, a documented justified deviation since the swap has no safe Rust form). Reach it incrementally O2→O1→O3, layered+re-measured. BUT first:
CHEAP CEILING ORACLE (run BEFORE any rework — the granule-specific test we should have run instead of the warm-monolith one): S1 = reused thread-local 128KiB scratch, decode each chunk in 128KiB slices into scratch then memcpy out into the existing monolith. Forces write-target working set to 32 HOT pages while keeping everything downstream byte-identical. It ADDS a copy (a cost) ⇒ LOWER BOUND: if wall DROPS even WITH the extra copy ⇒ residency mechanism real ⇒ GO build O2→O1→O3. If TIEs/regresses AND dtlb_store_misses.walk_completed doesn't move ⇒ mechanistically-grounded NO-GO (residency too weak, vendor cliff was realloc-driven=slack). ~1 afternoon, byte-safe (pure copy, no UB), pair with the store-walk perf counter. Sequence: retract NO-GO (done) → build S1 → measure (measure.sh interleaved sha-verified T8 + store-walk counter) → GO/NO-GO. Do NOT commit O1/O4 without S1's ceiling.

## 2026-06-01 — S1 oracle was INVALID (null instrument), NOT a NO-GO (advisor af1c8529). Residency hypothesis still UNTESTED. Targeted the WRONG BUFFER.
S1/Form A (post-hoc restripe `data` slice→scratch→back) is DOUBLY invalid for the residency hypothesis (do NOT record its +3-7% wall / flat store-walks as a NO-GO — that's the 3rd overclaim of the same shape):
1. WRONG STAGE: the decode already wrote the 3000-page monolith; the restripe ran AFTER, so the write-target was NEVER shrunk. Flat store-walks (T8 1.002) = TAUTOLOGICAL (didn't move the independent variable); +3-7% wall = pure added-memcpy cost. Form A's ONLY true fact: post-hoc touching the clean `data` suffix can't cut walks + costs a memcpy. Zero bearing on the hypothesis.
2. WRONG BUFFER (the stronger point, confirmed by the data model): S1 restriped `data: U8` — the CLEAN suffix written by the FAST known-window/ISA-L path, good locality already. But the 3.26× store-walk gap almost certainly lives in `data_with_markers: Vec<u16>` — the GLIBC-allocated (not even rpmalloc), 2×-byte, TRIPLE-passed (window-absent decode-write + replace_markers re-read + narrow→`narrowed` U8) bootstrap buffer (the 90%-of-faults / 2×-memmove buffer prior notes finger). Segmenting `data` was never going to move walks sourced from the u16 marker buffer.
HONEST STATE: residency hypothesis remains UNTESTED — TWO oracles now measured the wrong thing (warm-monolith: warmed but didn't shrink working set; S1: post-hoc + wrong buffer). 
THE CHEAP VALID ORACLE NOT YET RUN (advisor (c), the one S1 gave up before trying): measure rapidgzip's OWN dtlb_store_walk-per-byte at DECODE time, native-128KiB vs a forced-large-buffer rapidgzip build. Varies ONLY the write-target size inside the fixed REFERENCE engine. If native-128KiB walks are low and forced-monolith SPIKES them ⇒ residency real, win bounded by the delta = a real GO ceiling. If forced-monolith does NOT spike ⇒ the 3.26× is gzippy-engine-specific (the u16 double-write data model), NOT residency-per-se ⇒ SegmentedU8 port is MISDIRECTED, target the u16 buffer instead. Either outcome decisive + cheap. (A valid NO-GO from (c) IS recordable — the rule isn't "never conclude NO-GO", it's "only conclude from an instrument that moved the actual variable"; S1 didn't, (c) does.)
BACK-REF SEAM cost correction: the 32KiB-back-ref-across-128KiB-boundary is a BOUNDED two-part-copy sub-problem (98.6% back-refs len<16, seam-straddle rare), ~1-2 days + fuzzing — NOT the hard part. The hard part is the BLAST RADIUS: rewiring data/data_with_markers/narrowed + their 5 consumers (chunk_data, apply_window, replace_markers, chunk_fetcher, CRC pass, recycler) off flat contiguous slices. Don't conflate the two when costing the port.
NEXT: run oracle (c) [rapidgzip native-128KiB vs forced-monolith store-walks] BEFORE any port-build fork. If (c) says residency real → the port targets data_with_markers FIRST (the u16 buffer, where the walks are), not `data`. If (c) says not-residency → pivot the whole investigation to the u16 triple-pass data model.

## 2026-06-01 — ORACLE (c) GO is CONFOUNDED — do NOT commit the port (advisor a0a05f74, vendor-source-grounded). Residency still not cleanly shown on gzippy.
Oracle (c) (rapidgzip native-128KiB vs forced-monolith ALLOCATION_CHUNK_SIZE: 2455→12503→116646 walks/MB = 1.0×/5.09×/47.5×, wall 1.0×/1.49×/2.73×) LOOKED like an airtight GO. It is CONFOUNDED:
1. WRONG-BUFFER + UNFAITHFUL MODEL: ALLOCATION_CHUNK_SIZE governs ONLY dataWithMarkers (u16) — DecodedData.hpp:282-288 puts the clean `data` suffix in a SINGLE monolithic reserve() ALWAYS (vendor comment: 128KiB on `data` "makes no sense", ISA-L takes over after 32KiB). So rapidgzip's clean buffer is ALREADY monolithic-and-fast; bumping the constant monolithizes only the marker buffer — NOT a faithful model of gzippy's monolithic `data`.
2. ALLOCATOR-CLIFF ARTIFACT: ChunkData.hpp:20-63 is explicit that the span-sizing exists BECAUSE rpmalloc has slab-reuse collapse with large/varying allocs (the 1MiB→370MB/s collapse in their table is an rpmalloc-allocator cliff on the bgzip-index path, NOT a TLB law). Forcing 12/64MiB drives rapidgzip's rpmalloc OFF its span-cache cliff (≤128KiB cached vs >3.937MiB munmap) — re-triggering the EXACT pathology that code avoids. The near-linear 5→47× walks + super-linear wall = allocator-cliff fingerprint, OVER-states the granule effect. gzippy-on-glibc never had that span cache to fall off, so it can't recover that penalty by segmenting.
3. NON-SYMMETRIC CEILING: "rapidgzip loses 1.49× going monolithic ⇒ gzippy gains 1.47× segmenting" is INVALID — the 1.49× is partly the rpmalloc cliff (gzippy can't recover it), the u16 TRIPLE-PASS data model is monolith-INDEPENDENT (u8-escape already failed −60%), and the airtight 0%-wall fault-removal NO-GO redirected to the in-order CONSUMER window-resolution chain which segmentation doesn't touch. Honest bankable ceiling: well under 1.47×, PLAUSIBLY ~0 if the consumer chain is the real gate.
4. WALKS=6% PER OUR OWN DECOMPOSITION: the decomposing amplifier sized walks at 6% (zeroing 73/fault 13/walk 6). For walks to be worth 1.47× wall while faults (86%, co-located in decode_chunk) are 0% wall, walk-cycles would have to be near-perfectly serial on the consumer chain while faults perfectly overlap — extraordinary, and oracle (c) showed it on wrecked-allocator-rapidgzip, NOT on gzippy.
5. PORT TARGET UNPROVEN: GO named data_with_markers (biggest/most-unfaithful) but never showed its WRITES source the store-walks; the bulk clean/narrowed writes likely dominate.
RECONCILIATION (faults-slack vs walks-on-path): logically sound (different sub-costs) but does NOT supply gzippy-side evidence — our own decomposition says walks are 6%, not 147%.
REQUIRED GATE BEFORE ANY COMMIT (cheap, gzippy-side, the INVERSE of oracle c): cap GZIPPY's chunk/buffer granule so its write buffers land in a span-cached size class (or pre-chunk to 128KiB), measure dtlb-store-walks AND interleaved wall together. Walks drop ~3× AND wall improves ⇒ residency real ON GZIPPY ⇒ confirm target + commit. Walks drop but wall FLAT (likely, given the airtight 0%-wall NO-GO) ⇒ confound confirmed, port DEAD before week one. Hours not weeks; same toolbox.
STATE: residency hypothesis STILL not cleanly shown on gzippy. 3 oracles now (warm-monolith, S1, oracle-c) each measured the wrong thing or a reference-engine artifact. The gzippy-side granule-cap oracle is the one valid cheap test remaining; if IT is flat, the page/granule direction is genuinely exhausted and the wall is the consumer window-resolution chain.

## 2026-06-01 — ISA-L vs PURE-RUST resolved (advisor af374531) + memory-model port verdict INVALID (wrong decoder build)
FEATURE→DECODER MAPPING (confirmed against source — kills the recurring confusion):
- `parallel_sm` cfg (chunk pipeline/prefetcher/consumer/window-map + the new memory-model port) compiles under EITHER `isal-compression` OR `pure-rust-inflate`. Only the INNER WINDOWED DECODE differs.
- `--no-default-features --features pure-rust-inflate` ⇒ inner decode = PURE RUST (IsalInflateWrapper@inflate_wrapper.rs:654 wraps Inflate<Clean,Generic,Streaming>/ResumableInflate2; or pure bulk w/ GZIPPY_ISAL_PURE_BULK). NO real ISA-L FFI in the decode graph. = THE CANONICAL PRODUCTION/MEASUREMENT path per CLAUDE.md GOAL ("pure-Rust DEFLATE engine as the SOLE production decode path, C-FFI off the decode graph") + Measurement Rule 6.
- `--features isal-compression` ⇒ inner windowed decode = REAL ISA-L C FFI (IsalInflateWrapper@:162, isal_raw::inflate_state). = LEGACY/oracle comparator, NOT the optimization target. (Enabling BOTH ⇒ pure wins the not(pure-rust-inflate) guard.)
- Window-absent marker BOOTSTRAP (deflate_block.rs) is pure-Rust in BOTH builds (emits the u16 markers).
- ISA-L writes into gzippy's OWN segmented chunk.data (gzip_chunk.rs:380-389 hands ISA-L a spare slice carved from chunk.data) — NOT its own buffer; so the memory-model port engages under both. (asterisk: x86 shipping artifacts historically still ISA-L per project_pure_rust_inflate_not_shipped; pure-rust is the DIRECTED goal + campaign baseline.)
MEMORY-MODEL PORT (PR #123, feat/rapidgzip-memory-model): the WHOLE faithful port BUILT + byte-correct (silesia byte-identical T1/4/8/16 both arches via path=IsalParallelSM; seam-differential vs flate2 passes; 830 tests, 3 pre-existing fails match base). BUT its measured verdict (T4/8/16 +10.7/+10.3/+24.2% REGRESSION, dtlb_store_walk +38%) was taken on `--features isal-compression` = THE WRONG DECODER. INVALID as a verdict on the production-goal build because:
1. pure-rust-inflate is the canonical path (Rule 6); isal-compression is legacy.
2. MECHANISTIC CONFOUND: the store-walk mechanism the port targets is buffer residency DURING THE DECODE WRITES. Under isal-compression the writer is ISA-L bulk (~340MB/s multi-symbol); under pure-rust it's ResumableInflate2 (slower single-symbol). The store-walk PATTERN differs between builds — the isal number can mask or invert the effect. The advisor: re-run is "not just fidelity, mechanistically necessary."
⇒ The port's NEGATIVE result does NOT yet count. RE-RUN the before/after on --no-default-features --features pure-rust-inflate (GZIPPY_DEBUG=1→path=IsalParallelSM, GZIPPY_FORCE_PARALLEL_SM=1, interleaved N>=9, sha-verified, T4/8/16, + dtlb_store_walk + minor-faults) BEFORE concluding the memory-model lever. (Note: the byte-correctness + the WHOLE-port build stand; only the perf verdict is invalid.)

## 2026-06-01 — MEMORY-MODEL PORT = NO-GO on the CORRECT decoder too (pure-rust-inflate). Residency/segmentation EXHAUSTED on both decoders.
CORRECTED before/after (PR #123 feat/rapidgzip-memory-model @1778651 vs reimplement-isa-l @f7d6277), BOTH --no-default-features --features pure-rust-inflate (Rule 6 production decoder), both path=IsalParallelSM, both isal_inflate dynsyms=0 (genuine pure-Rust, the prior isal-build confound GONE), silesia-gzip.tar.gz 67MB, frozen-clock gate PASS, N=9 interleaved sha-verified:
| cell | BEFORE med(sd) | AFTER med(sd) | Δwall | vs rapidgzip before→after |
| T4 | 0.5008(1.1%) | 0.5316(1.6%) | +6.15% (min +4.3%) | 0.902→0.865 |
| T8 | 0.4171(31%!) | 0.4362(30%!) | +4.58% (min +5.7%) | 0.786→0.744 [T8 sd>5% = box-contention burst, NOT trustworthy on absolutes; verdict rests on clean T4+T16] |
| T16 | 0.2103(4.5%) | 0.2366(7.4%) | +12.51% (min +14.4%) | 0.933→0.816 |
MECHANISM (T8 counters, n=3, per-process robust to wall noise): dtlb_store_walk 2.41M→2.94M = +21.9% (rapidgzip 0.74M) — moved AWAY from rapidgzip, NOT toward; minor-faults +3.7% flat; cycles +9.9%.
VERDICT: NO-GO — the faithful segmented memory-model port REGRESSES the wall AND INCREASES store-walks on the production decoder. The residency hypothesis is INVERTED by the port's access pattern (per-segment LUT rebuilds + boundary branches + contiguous re-materialization for the window builder + segment-walking accessors in segmented_markers.rs cost MORE than residency saves). DECODER DOES NOT FLIP THE SIGN: pure-rust regresses ~half the isal magnitude, identical direction. Do NOT merge PR #123 for perf (it IS byte-correct + a faithful port — keep as the record/negative result).
⇒ RESIDENCY / SEGMENTED-BUFFER direction EXHAUSTED on BOTH decoders. Corroborates [[project_wall_is_consumer_critical_path]]: the wall is the in-order consumer/publish serial chain, NOT buffer residency. Remaining live lever = rapidgzip's queuePrefetchedChunkPostProcessing (eager successor apply_window / resolve-ahead), NOT buffer residency. The whole page-walk/allocation saga (which the user rightly forced us to test exhaustively rather than dismiss) is now DECISIVELY closed by building the real thing.

## 2026-06-01 — USER CHALLENGE: disprove the analysis with FULCRUM numbers. Honest answer: Fulcrum STRUCTURALLY CAN'T measure C2 — that's WHY it never surfaced it (advisor a6994e87).
The user is right to distrust analysis Fulcrum didn't produce. The disproof splits:
- C1 (pure-rust retires ~1.005× rapidgzip's instructions ⇒ gap is STALLS not work): SOLID + FULCRUM-INDEPENDENT. Whole-process perf stat is concurrency-immune; equal-instructions + 1.42× cycles = stalls BY ARITHMETIC (cycles=instr/IPC). Needs ONLY the provenance witness (proof the binary was pure-rust). Routing it through Fulcrum region_hw would ADD smear to a clean fact — do NOT. (Caveat: C1-the-fact is bulletproof; the leap "therefore inner-decode isn't the lever" is C2, does NOT follow — equal instructions are consistent with stalls INSIDE the decode loop.)
- C2 (the cycle-excess is in the consumer/publish chain NOT decode): UNPROVEN-BY-FULCRUM, and UNPROVABLE by region_hw. region_hw.rs:37-42,711-720 says concurrent regions "are not separable by time alone… for exact separation capture -p1." But the consumer-chain stall is a T8-ONLY phenomenon that VANISHES at -p1 (one worker, no cross-chunk serialization to wait on). FUNDAMENTAL TENSION (real, not a worry): clean per-region counter attribution needs -p1; the bottleneck only exists at T8. WORSE: a consumer WAIT is a LATENCY/HOLE — zero PEBS samples, zero retired instructions — a sample-and-counter profiler is blind to a hole by construction. region_hw attributes SAMPLES to regions; the thing C2 claims is the bottleneck produces NO samples. ⇒ THIS IS WHY FULCRUM CHASED THESE NUMBERS FOR WEEKS AND NEVER SURFACED THE CONSUMER CHAIN — the bottleneck is invisible to the measurement Fulcrum does. The user's distrust is JUSTIFIED about region_hw specifically. I OVERSTATED C2 as if Fulcrum-grounded; it was advisor-reasoning + critpath/consumer (latency views) + the refuted-lever ledger, NOT a region_hw decomposition.
THE VALID DISPROOF (per our own PROCESS #1 perturb-don't-attribute): (1) DEMONSTRATE Fulcrum can't see C2 — its own concurrency-purity (>0.5 smear at T8) + a CONSERVATION-GAP check (per-region attributed cycles vs whole-process total; the unattributed HOLE = the invisible consumer-wait, so a big gap is EVIDENCE FOR C2 by absence). (2) TEST C2 causally: GZIPPY_SLOW_BOOTSTRAP-style slow-injection at T8 into consumer/publish vs decode, interleaved wall response + frequency-neutral sleep control. Proportional wall response to consumer-slow + flat to decode-slow ⇒ C2 causally confirmed (survives the smear; perturbation needs no clean counters). (NB: the +13.7% back-ref-copy-inline win is in MILD TENSION with "decode wall-dead" — resolve it.)
FULCRUM IMPROVEMENTS (minimal, NOT rebuild): (a) PROVENANCE WITNESS — every bundle self-labels gzippy build flags + isal_inflate dynsym count (0=pure-rust, >0=ISA-L) so no run is interpretable without knowing which decoder (answers the user's flag-clarity demand + prevents the memmodel-wrong-decoder fiasco recurring). (b) FAIL-CLOSED SMEAR GATE — region_hw already COMPUTES concurrency + warns; make it REFUSE to emit a per-region split (or stamp UNRELIABLE + block any "confirmed" verdict) when concurrency>0.5. This is the anti-phantom fix that would have caught the model-tautology + decompose-CPU-sum. (c) self-checks: per-region cycles sum to total within 5%, region instructions match whole-process within 5%, + a region_hw POSITIVE-CONTROL self-test on a known-ground-truth workload (PROCESS #4) BEFORE trusting any T8 run. OUT OF SCOPE: per-thread/hole-aware T8 attributor (research problem; the perturbation sidesteps it).

## 2026-06-01 — THE PIPELINE-GAP MEASUREMENT (never done before): SHIPPED ISA-L gzippy LOSES to rapidgzip ⇒ gap is ARCHITECTURAL, NOT pure-Rust-self-imposed
Built reimplement-isa-l @3ff3c9b `--features isal-compression` (the SHIPPED x86 release config = REAL C ISA-L inner decode, proven via linked libisal.a defining isal_inflate/decode_huffman_code_block_stateless + igzip strings; the dynsym=0 check was a false alarm — release binaries are STRIPPED, use the static-lib check). path=IsalParallelSM, sha=gzip ref e114dd2 every trial. Frozen-clock, N=9 interleaved, sd<5%, RUN_TRUSTWORTHY=true.
silesia-large (162MB→503MB), gzippy-isal vs rapidgzip 0.16.0:
| T | gz med (MB/s) | rg med (MB/s) | RATIO gz/rg |
| 4 | 1.0388 (496) | 1.0125 (508) | 1.02× near-TIE |
| 8 | 0.7433 (714) | 0.6120 (853) | 1.19× LOSS |
| 16| (654) | (947) | 1.45× LOSS (SMT) |
COUNTERS /MB (T8, n=5): gzippy walk_completed 4608 vs rg 2382 (1.93×); walk_active 144239 vs 71065; INSTRUCTIONS 21.59M vs 22.53M = gzippy ~0.95× (FEWER); cycles 9.65M vs 9.42M.
VERDICT — the "self-imposed gap" reframe is REFUTED: the SHIPPED product with the IDENTICAL fast C ISA-L decode is NOT at parity (1.19× T8 / 1.45× T16). 
- gzippy executes FEWER instructions/MB than rapidgzip yet LOSES the wall ⇒ inner decode is CONCLUSIVELY NOT the lever (kills "inner decode"; both use the same C engine, gzippy is leaner per byte).
- gzippy still pays ~1.9× dtlb store-walks with the same decoder ⇒ the gap is ARCHITECTURAL (pipeline / consumer chain / memory residency), present in the SHIPPED PRODUCT, not pure-Rust-specific.
- T-DEPENDENT: near-parity T4 (1.02×) → 1.19× T8 → 1.45× T16-SMT. Same store-walk mechanism CLASS as the pure-rust 3.26×, here at 1.9×. Pure-Rust adds EXTRA cost (3.26 vs 1.9) ON TOP OF a real architectural gap.
RECONCILES the campaign question: we have NOT been grinding a purely self-imposed gap. The consumer-chain/memory-model lever matters for the ACTUAL SHIPPED product. CORRECTS my earlier "shipped-isal may already be at parity / 1.47× is pure-rust-cost" framing (is-this-possible.md option 2) — it's NOT at parity; relaxing clause A (keep ISA-L) does NOT get parity either. The architectural gap is shared and real on both decoders.
NOTE the residency tension: the memory-model SEGMENTATION port REGRESSED (residency-via-segmentation exhausted), yet store-walks ARE 1.9-3.26× higher AND correlate with the wall loss across both decoders + scale with T. So the walk DIFFERENCE is real but not fixable by the segmentation we tried — consistent with "it's the consumer/publish chain ordering, not the buffer granule." The un-refuted lever remains queuePrefetchedChunkPostProcessing (resolve-ahead off the serial publish path). (← the running disproof agent a001c98 is causally testing exactly this consumer-chain claim C2.)

## 2026-06-01 — Design D (worker-side resolve-ahead) REFUTED at design stage by our OWN causal data (advisor a5cefcca). DO NOT BUILD.
Proposed fix (port queuePrefetchedChunkPostProcessing: worker resolves+publishes chunk i ahead of the consumer, off the serial thread). DISPROVEN before any code by three independent campaign measurements:
1. KILL SHOT — the FRONTIER-PLACEMENT ORACLE already tested D's exact hypothesis (force gating chunk decoded+resolved AHEAD of consumer) = TIE T4/8/16 (0.992/1.012/0.997, sd<1.5%). It BIT (frontier_already_inflight 94% — gzippy ALREADY resolves ahead like vendor); pool.pick.wait dropped 525→420ms but did NOT convert to wall. D is the implementation of a hypothesis its own ceiling-test FALSIFIED.
2. WRONG MECHANISM — store-walks (the actual 3.26× difference) live in the DECODE WRITES: 99.6% of faults in worker.decode_chunk; resolve/apply_window is 8-35µs genuine SLACK (wait_replaced_markers 0.02-0.03ms). D moves resolve TIMING, not the decode write granule ⇒ INERT on the measured mechanism. Also copies-wall-neutral trap: moving overlapped slack earlier = 0% wall (proven by the 212ms-copy-elim=0% precedent).
3. RENAME WALL — eager_postprocess_prefetched (chunk_fetcher.rs:1856) already ports the structure + was REFUTED (+195ms consumer-side). D's worker-side variant escapes the +195ms but lands on the documented 0×-fires (predecessor window rarely published early) / no-idle-worker (decode is the occupant, 94% in-flight) walls. T-dependence: D adds WindowMap publish contention that worsens with T ⇒ neutral-to-negative at T16.
The decode-vs-resolve counter-mechanism ("publish chain CAUSES the decode wait via window-absent decode") is HAND-WAVING — refuted twice: placement oracle forced the frontier ahead = TIE; HoL measured = 2/29/4ms, nowhere near the 62/115/394ms residual.

## 2026-06-01 — THE LIVE CONTRADICTION to resolve before ANY granule build (the honest open question)
Two campaign results appear to conflict and BOTH can't be the last word:
- ORACLE (c): forcing the REFERENCE rapidgzip engine to gzippy's ~12MiB write granule (one compile-time var, engine fixed) = 5.09× store-walk spike + 1.49× wall ⇒ the GRANULE alone over-explains the gap ⇒ segmentation is the GO lever.
- BUT the faithful MEMORY-MODEL SEGMENTATION PORT (PR #123, both decoders) REGRESSED the wall +6-12% and store-walks +21.9% (wrong direction).
ADVISOR RECONCILIATION (the testable claim): the port segmented the WRONG way — SLAB-retain kept the VIRTUAL allocation but rpmalloc's LARGE class (>2MiB-ish) DECOMMITS the physical pages on free, so reuse still first-touch-faults; AND the port added per-segment bookkeeping (LUT rebuilds, boundary branches, re-materialization) that cost more than it saved. The UN-tested specific lever: segment data_with_markers (the u16 GLIBC monolith, 2 bytes/elem, triple-passed — the biggest buffer) into ≤128KiB sub-buffers backed by a RETAINING rpmalloc MEDIUM-class pool (≤128KiB = MEDIUM, retain=2, stays PHYSICALLY COMMITTED → warm-reuse skips first-touch). That is a DIFFERENT intervention than what regressed (physical-commit-retention, not virtual-alloc-retention; on the marker buffer, minimal bookkeeping).
⇒ IS THIS REAL OR ANOTHER PHANTOM? ORACLE (c) itself was later flagged as possibly confounded (it drove the reference rpmalloc off ITS OWN span-cache cliff — an allocator artifact, not a pure TLB law). So we have: ORACLE(c) says granule matters (maybe confounded), port says segmentation regressed (maybe wrong-buffer/wrong-retention). NEITHER cleanly establishes the granule lever on gzippy. The one VALID gzippy-side test never cleanly run: a MEDIUM-class physically-committed retaining pool for data_with_markers, measured store-walks AND wall, interleaved sha-verified — does it drop BOTH? If flat ⇒ granule exhausted on gzippy too ⇒ the gap is genuinely diffuse/unlocated and the campaign's honest state is "no located lever".
HONEST CAMPAIGN STATE: gap is REAL + ARCHITECTURAL + shared across decoders (shipped ISA-L loses 1.19/1.45× with FEWER instructions). Inner-decode DEAD. Resolve-ahead/placement DEAD. Page-fault-removal = 0% wall. The granule is the ONLY direction with any positive signal (ORACLE c) but it's confounded + the one port regressed. Next valid step = the MEDIUM-class retaining-pool granule A/B on data_with_markers (NOT Design D, NOT the regressed segmentation port). If THAT is flat, the gap is unlocated and we should say so.

## 2026-06-01 — DISPROOF via Fulcrum (user challenge): the analysis was OVERSTATED. C1 half-confounded, C2 REFUTED. (agent a001c98, frozen-clock, pure-Rust dynsyms=0 confirmed)
The user demanded the analysis be disproven with FULCRUM numbers + Fulcrum hardened until it could. Result: it PARTLY REFUTED the prior analysis — distrust was justified.
- C1 (instructions equal ⇒ gap is stalls): WORK-PARITY CONFIRMED (instr ratio pure/rapidgzip = 0.965, gzippy does LESS work, concurrency-immune whole-process counter). BUT the cycle ratio = 1.196 NOT the 1.42× I'd been citing — the "1.42×" was a CLOCK/TURBO CONFOUND. The stall gap is ~20%, not ~42%. (Arithmetic "equal work + more cycles = stalls" holds; the magnitude was contaminated.)
- C2 (gap is the consumer chain, decode is wall-dead): REFUTED by causal slow-injection + frequency-neutral sleep control: consumer-slow 0→100% = +69% wall; decode-slow = +21% wall. BOTH regions ON the critical path. Decode is NOT wall-dead (resolves the back-ref-copy-inline +13.7% tension — decode IS live). Consumer is the LARGER lever (~3.3×) but "consumer-only / inner-decode-is-a-red-herring" — stated repeatedly this campaign — is FALSE.
- WHY FULCRUM NEVER SURFACED C2, proven with its own numbers: hardened region_hw ran positive-control self-test PASS, then TRUST GATE = UNRELIABLE on the real T8 trace (consumer concurrency 95-98%, decode 100% smear); cycles conserve but cannot be per-region separated ⇒ the T8 per-region counter split is a FICTION. Counter-attribution is structurally blind to concurrent regions; Fulcrum now FAILS CLOSED instead of emitting a phantom.
CORRECTED CAMPAIGN PICTURE (supersedes the "1.47× / consumer-chain-only / instructions-equal" framing): the pure-Rust T8 stall gap is ~1.20× (cycles), work is at parity, and it splits across BOTH decode (~+21% lever) AND consumer (~+69% lever) — consumer dominant but decode live. The shipped-ISA-L architectural gap (1.19/1.45× T8/16) stands separately. Inner-decode is NOT dead (refuted my own claim); resolve-AHEAD/placement IS dead (oracle TIE); granule is confounded+regressed. So there are TWO live levers per the causal test: the consumer serial chain (bigger) AND decode speed (smaller but real) — NOT consumer-only.
FULCRUM HARDENED (durable, the user's "improve Fulcrum" ask): provenance witness (every run self-labels ISA-L-vs-pure-Rust via isal_inflate dynsym count — kills the wrong-decoder fiasco); fail-closed smear/conservation TRUST GATE (refuses "confirmed" when concurrency>0.5 — the anti-phantom fix that would've caught the model-tautology + decompose-CPU-sum); positive-control self-test (PROCESS #4). 16 tests, fulcrum branch fulcrum/provenance-smear-gate @ee69218 on origin/main. gzippy GZIPPY_SLOW_CONSUMER knob added (PR #125). The verdict came from CAUSAL PERTURBATION not attribution — the only method that survives the smear.

## 2026-06-01 — STANDING METHOD: intuition-QUARANTINED perturbation protocol (advisor a7228875, 7 amendments)
PERTURBATION (def): change one region's speed by a KNOWN ABSOLUTE amount (inject K ms, or oracle-remove) holding the shared substrate constant, read the INTERLEAVED WALL response, report as a [sleep,spin] INTERVAL — wall-criticality PROVEN by wall movement, never inferred from occupancy.
"Intuition-zero" is UNACHIEVABLE (it relocates intuition somewhere less auditable). Target = intuition-QUARANTINED: every judgment enumerated + attackable. The 7 seams + fixes:
1. Calibrate knobs in ABSOLUTE ms, not fraction-of-smeared-region-time (the fraction inherits the measurement bias the protocol exists to escape).
2. Sleep-control only BRACKETS confounds (kills turbo+core-steal — the 1.42× miss — but NOT memory-bandwidth/cache; gap is 33% mem-bound). Report elasticity as [sleep,spin] INTERVAL; "indeterminate" when it straddles the threshold; NEVER a single confirmed point.
3. Single-region elasticity is MATHEMATICALLY INVALID on this feedback pipeline (slow decode→longer consumer wait→queue backs up→stalls decode). The "two live levers" (consumer +69%, decode +21%) are almost certainly ONE COUPLED CONSTRAINT measured from both ends. Use a 2^k FACTORIAL + interaction terms, NOT per-region sweeps. Ranking them separately builds the wrong lever + watches the bottleneck migrate (the campaign's recurring failure).
4. The consumer WAIT is STRUCTURALLY UNPERTURBABLE by slow-inject (a wait is the ABSENCE of work; spin models "more work" not "longer hole"). You can only perturb its CAUSE (decode) — which is WHY decode+consumer aren't two independent data points. Enumerate unperturbable regions as explicit blind spots per bundle.
5. WHICH regions have knobs is itself intuition (a region nobody suspected has no knob → can never be found). Enumerate the knobbed-region set as an assumption.
6. Removal oracles need a POSITIVE CONTROL admitted before the ceiling: zero-execution witness (R's entry counter reads 0 in the oracle build — would've caught the clean-window oracle re-running bootstrap) + work-conservation check (total CPU drops ~R's cost — would've caught warm-monolith).
7. Per-bundle ASSUMPTIONS MANIFEST (region partition, knobbed set, threshold, TIE band, workload, oracle-feasibility-is-SEPARATE-and-intuited) with AUTO-INVALIDATION: a verdict dies when any listed assumption is later falsified (the absence of this rule = why the clean-window-oracle falsification didn't auto-kill its dependent conclusions = weeks lost).
Protocol steps: S1 provenance gate (build flags + dynsym ISA-L/pure witness + frozen-clock proof or DISCARD) → S2 baseline (frozen, N≥9, sha, RUN_TRUSTWORTHY, T4/8/16, labeled-decoder vs rapidgzip) → S3 FACTORIAL elasticity (2^k knob cross + interaction, [sleep,spin] intervals) → S4 ceiling by REMOVAL (positive-controlled oracle) → S5 rank by measured ceiling (coupled regions ranked JOINT) → S6 build top, re-run S2 whole-system layered → S7 every verdict carries elasticity+ceiling+provenance+assumptions; trust gate BLOCKS smeared/unprovenanced.
NEXT MEASUREMENT (the diagnostic, not a build): {decode, consumer} 2×2 FACTORIAL — GZIPPY_SLOW_BOOTSTRAP × GZIPPY_SLOW_CONSUMER at {0, high} = 4 cells, frozen-clock, N≥9 interleaved, sleep+spin bracket, pure-rust build (dynsym=0 witness). Read the INTERACTION term: strong NEGATIVE interaction ⇒ ONE coupled constraint ⇒ real lever = the HANDOFF/QUEUE latency between them (what queuePrefetchedChunkPostProcessing addresses), ceiling oracle = "remove the handoff wait" NOT speed-either-endpoint. Near-ZERO interaction ⇒ genuinely independent ⇒ rank separately. This MUST run before building any lever.

## 2026-06-01 — RESULT: the {decode,consumer} 2×2 FACTORIAL ran. INTERACTION ≈ 0 ⇒ INDEPENDENT, NOT one coupled constraint. (trainer i7-13700T, frozen-clock 111+105, T8-noSMT pinned 0,2,4,6,8,10,12,14, pure-Rust dynsym isal_inflate=0, path=IsalParallelSM, N=11 interleaved, sha-verified == rapidgzip oracle, box restored + baseline-match-proof verified)
KNOBS (both fire, byte-identical): added ABSOLUTE-µs mode — GZIPPY_SLOW_BOOTSTRAP_US=K (decode, gzip_chunk.rs per-chunk) + GZIPPY_SLOW_CONSUMER_US=K (consumer, chunk_fetcher.rs per-iteration; cherry-picked PR#125's percent knob and added the µs mode the protocol mandates). K=5000µs calibrated (single-region ~+40% at warm baseline, clearly above noise). NB blind-spot #4: SLOW_CONSUMER perturbs the consumer's BUSY work only — a consumer WAIT is unperturbable — so the consumer elasticity is a LOWER bound.
THE TABLE (median s, T8-noSMT, silesia-large RAW=503627776):
        | spin 00/D0/0C/DC            | sleep 00/D0/0C/DC
 median | .3731 .3889 .5214 .5365     | .3749 .3886 .5147 .5261
 sd     | .0137 .0185 .0098 .0173     | .0103 .0178 .0075 .0052
 decode effect   (D0-00): spin +15.8ms(+4.2%)  sleep +13.7ms(+3.7%)
 consumer effect (0C-00): spin +148.3ms(+39.7%) sleep +139.8ms(+37.3%)
 INTERACTION (DC-D0-0C+00): spin −0.7ms(−0.2%)  sleep −2.3ms(−0.6%)   [sleep,spin]=[−2.3,−0.7]ms
 pooled per-cell sd (noise band): spin 14.8ms  sleep 10.2ms
VERDICT: interaction (|0.7|,|2.3|ms) ≪ noise band (10–15ms) in BOTH spin and sleep ⇒ NEAR-ZERO INTERACTION. Decode + consumer are INDEPENDENT ADDITIVE levers (DC = D0+0C−00 within noise). NOT one coupled constraint; NOT super-additive. The "two live levers are almost certainly ONE coupled constraint measured from both ends" hypothesis (STANDING METHOD #3) is FALSIFIED for these two regions at T8-noSMT on this workload. ⇒ the next step is NOT "build the handoff/queue lever (queuePrefetchedChunkPostProcessing) because the endpoints are coupled" — that premise didn't survive. Rank the two endpoints SEPARATELY by ceiling (protocol S5 independent branch).
RANKING (by elasticity; ceiling-by-removal S4 still owed before building): CONSUMER is the dominant lever — ~+40% wall for K=5000µs/iter, ~9× the decode effect (+4%). Decode is LIVE but small here (resolves nothing about back-ref-copy tension beyond "decode is on-path but a minor share at T8-noSMT under this baseline"). 
CAVEAT (adversarial, honest): the interaction's diagnostic strength is bounded by decode's SMALL measured effect here (+16ms vs the +21% seen in the prior single-region run at a different/cold baseline — decode is heavily overlapped across 8 workers so most of it is off the critical path at this baseline). A near-zero interaction with a small decode arm cannot rule out coupling that would only appear if decode were a LARGER share (e.g. T16-SMT, or a higher-K decode injection). Independence is established AT THIS OPERATING POINT (T8-noSMT, K=5000µs, ~0.37s baseline). Before building: (a) S4 ceiling-by-removal oracle on the CONSUMER (positive-controlled) to bound the speed-up — the consumer is the ranked #1; (b) optionally re-run the factorial with a LARGER decode-K to stress the interaction where decode is a bigger arm. The consumer-WAIT blind spot (#4) means even +40% UNDERSTATES the consumer; the consumer chain is the lever to bound next.
ASSUMPTIONS MANIFEST (auto-invalidating): region partition = {decode=window-absent bootstrap in gzip_chunk.rs:890; consumer=in-order serial chain in chunk_fetcher.rs consumer_loop}; knobbed set = {decode busy, consumer busy} (consumer WAIT unknobbed — explicit blind spot); workload = silesia-large T8-noSMT; K=5000µs; TIE band = pooled per-cell sd 10–15ms; operating point = ~0.37s frozen baseline. Verdict dies if: decode arm shown to be a larger critical share at the tested point, or the consumer effect is shown to be the WAIT not the busy-work (would flip "consumer busy is the lever").

## 2026-06-01 — 2×2 FACTORIAL: decode & consumer are INDEPENDENT (interaction ~0), refutes my coupled prior. Consumer = dominant lever (~40%, 9× decode).
T8-noSMT, frozen-clock, pure-rust dynsym=0, N=11 interleaved, sha==rapidgzip oracle, K=5000µs absolute-µs knobs (GZIPPY_SLOW_BOOTSTRAP_US / GZIPPY_SLOW_CONSUMER_US — the consumer knob cherry-picked from PR#125 + µs mode added; both byte-identical pure-delay):
| effect | spin | sleep |
| decode-slow D0−00 | +15.8ms (+4.2%) | +13.7ms (+3.7%) |
| consumer-slow 0C−00 | +148ms (+39.7%) | +140ms (+37.3%) |
| INTERACTION DC−D0−0C+00 | −0.7ms | −2.3ms |  (noise band 10-15ms)
VERDICT: INTERACTION ≈ 0 in BOTH spin and sleep ⇒ decode + consumer are INDEPENDENT ADDITIVE levers, NOT one coupled constraint (refutes my strongly-negative prior — the protocol's interaction term overruled my intuition, which is the point). The "build the handoff/queue lever because endpoints are coupled" premise is FALSIFIED at this operating point. Rank endpoints SEPARATELY: CONSUMER #1 (~40%, ~9× decode's ~4%); decode live but minor here.
CAVEATS (do not over-claim): (1) GZIPPY_SLOW_CONSUMER perturbs the consumer's BUSY work ONLY — a consumer WAIT is structurally unperturbable — so +40% is a LOWER BOUND and the finding is about consumer COMPUTE not its wait. (2) Independence holds AT THIS OPERATING POINT ONLY (T8-noSMT, decode arm small +16ms heavily overlapped across 8 workers); cannot rule out coupling at larger decode share (T16-SMT / higher decode-K).
NEXT OWED (protocol S4, before any build): CEILING-BY-REMOVAL on the CONSUMER — a positive-controlled oracle (zero-execution witness + work-conservation) that REMOVES consumer work to bound the speed-up (slow-down slope ≠ speed-up ceiling). Only the removal bounds the achievable win. THEN decide build. Knob edits are LOCAL/uncommitted in the worktree.

## 2026-06-01 — S4 CONSUMER-NULL CEILING = GO (the campaign's first positive-controlled GO). Lever = the consumer's single-core 503MB OUTPUT-WRITE memcpy.
Oracle GZIPPY_NULL_CONSUMER_WORK=1 (branch consumer-null-oracle @593819d, byte-identical OFF): nulls the consumer thread's per-chunk write_all(output) + crc.append, keeps pipeline-structural work (window-publish/marker-wait/rx.recv — deliberately NOT nulling publish, which would force window-absent decode = the clean-window-oracle trap). POSITIVE CONTROL passed (the discipline past oracles lacked): consumer_work_executed=0 ON, nulled_chunks=36, bytes_skipped=503627776==expected (conservation_ok), no FAKE warning. pure-rust dynsym=0, path=IsalParallelSM, frozen, T8-noSMT, N=11-15 interleaved, OFF sha==rapidgzip e114dd2.
CEILING (median, removal):
| T | OFF | ON | ceiling | OFF/rg | ON/rg |
| 4 | 0.461 | 0.376 | +18.6% | 1.46× | 1.19× |
| 8 | 0.373 | 0.273 | +26.9% | 1.81× | 1.32× |
| 16| 0.408 | 0.291 | +28.6% | 2.28× | 1.63× |
MECHANISM PINNED (3 adversarial checks ⇒ not a phantom): (1) it's the OUTPUT WRITE not CRC — crc.append is O(1) combine_crc32 (36 calls, µs); the ~100ms is the consumer single-core serial write_all of all 503MB into BufWriter (io.rs:148), a memcpy of freshly cross-core-decoded data. (2) consumer-thread-LOCAL — worker.decode_chunk busy identical OFF/ON (1488 vs 1502ms), not systemic contention. (3) ORTHOGONAL to the granule/residency lever — dtlb_store_walk only −9% (store-walks live in decode writes, worker-side). Busy-vs-wait: wait.future_recv did NOT grow when compute nulled (27→24ms) ⇒ the write was real critical-path work, not converted-to-wait; the consumer WAIT (~27ms, unperturbable by this oracle) is a separate smaller residual.
VERDICT: GO — build the consumer output-write (writev/vectored gather, a dedicated writer thread/double-buffer, or eliminate the BufWriter intermediate copy). Predicted ceiling ~100ms ≈ HALF the remaining T8 gap. NECESSARY-NOT-SUFFICIENT: ON is still 1.32× rapidgzip, so layer it + re-measure the whole (the ~27ms consumer-wait + residual architecture remain). This is the FIRST positive-controlled GO of the campaign — and it's a lever NO prior attribution/intuition named; the removal-oracle protocol surfaced it. (NB the segmentation port + granule levers are still DEAD; this is orthogonal/additive to them.)
NEXT (S5/S6): rank — this is #1 (consumer output-write, ~27% ceiling). Build it; the natural design is to get the 503MB write OFF the in-order consumer's critical path (worker-side or dedicated-writer-thread streaming, like rapidgzip doesn't serialize the materialization on its consumer). Then re-measure whole-system; the residual lever board = consumer-wait (~27ms) + the shipped-ISA-L architectural gap.

## 2026-06-01 — COPY AUDIT (rapidgzip vs gzippy pure-rust, doc plans/copy-audit-rapidgzip-vs-gzippy.md @afca118 origin/docs/copy-audit): gzippy ~3× vs rapidgzip ~2× touches; D1+D2 are ONE convergence target
COUNT (per-chunk data, decoded→fd): rapidgzip ~2× (1 worker decode-write + 1 consumer writev) vs gzippy ~3× (2 worker + 1 consumer BufWriter-memcpy).
gzippy's 2 STRUCTURAL EXTRA copies vs rapidgzip:
- D1 = separate `narrowed` u8 buffer (replace_markers.rs:128-155 materializes markers→new buffer); rapidgzip resolves IN-PLACE + swaps u16-storage as u8 views (DecodedData.hpp:325-388, the reuse-swap). D1 is WORKER-SIDE ⇒ SLACK today (matches absorb_isal_tail copy-elim = 0% wall, which the audit re-confirmed is swap-not-copy at gzip_chunk.rs:1113). NOT an independent GO — layer-it candidate, validate by perturbation AFTER D2 (copies-wall-neutral law: worker-side copy elim ~0% wall until the consumer bottleneck shifts).
- D2 = consumer BufWriter output memcpy (chunk_fetcher.rs:3040-3079); rapidgzip writev/vmsplice (ChunkData.hpp:794 + FileUtils.hpp:765) = the S4 +27% GO, BUILD IN FLIGHT (agent ac67978).
KEY: D1+D2 are the SAME convergence target — rapidgzip's resolve-in-place + iovec-gather + writev collapses BOTH into its 2-touch shape. The writev build's gathered iovecs would be the in-place-resolved buffers ⇒ D1 (eliminate `narrowed`, resolve in-place) is the natural 2ND step on the SAME port, composes with D2. absorb_isal_tail = swap-not-copy confirmed (not a copy lever). So D2 (consumer write) is the ONLY large pure-consumer-side copy today; D1 is the second structural copy to delete via the same in-place+writev port, but only banks once D2 shifts the bottleneck to it (re-measure, don't assume).
LEVER BOARD (copy-elimination): D2 writev (consumer, +27% ceiling, building now) → then D1 in-place-resolve (worker, layer + re-measure) → both = rapidgzip's 2-touch shape. Everything else (granule/segmentation, placement, HoL, inner-decode) stays DEAD.

## 2026-06-01 — writev = FLAT (campaign's 1st build-driven wall test); reframes the target (advisor a4c27600). Mechanism UNPROVEN, COMPUTE gap is the bigger lever, writer-thread ceiling UNMEASURED.
writev zero-copy consumer output (PR #126) = FLAT: in-binary GZIPPY_DISABLE_WRITEV on/off ±0.3-2.6% all T/builds (inside 3-5% spread), pure-rust AND shipped-isal, frozen N=11 sha-verified, strace=36 writev (one/chunk, engages+coalesces). Did NOT hit the S4 +18-25% T8 prediction. FLAT VERDICT SOUND (properly-controlled negative).
WHY: writev removes only the userspace BufWriter GATHER memcpy; the dominant write cost is NOT the gather. (Mechanism NOT YET established — ≥3 fitting models: gather-was-never-on-the-wall; tmpfs-write-is-memory-BW-bound; page-cache-dirtying. We measured the REMOVAL ceiling, never the gather cost alone. Do NOT record "single-threaded kernel materialization" as settled.)
THE REFRAMING DECOMPOSITION (from the /dev/null-vs-tmpfs discriminator, T8 min): total tmpfs gap 1.46× = COMPUTE gap 1.33× (gzippy /dev/null 0.267 vs rapidgzip 0.201, write removed) × SINK cost (gzippy +0.129s = 1.65× rapidgzip's +0.078s). TWO SEPARABLE GAPS. (/dev/null short-circuit RULED OUT: harness uses `-d -c >/dev/null` = real fd, both tools fully decode; verified rapidgzip.cpp:649-668 count-skip needs no -o/-c.)
TWO CORRECTIONS to my read (advisor):
1. S4's +27% is the REMOVAL ceiling, NOT the writer-thread ceiling. A writer-thread RELOCATES the write (overlap), doesn't remove it — its ceiling = the OVERLAP FRACTION, UNMEASURED. Building a writer-thread on +27% = the intuition-leap the methodology forbids. writer-thread is bounded (0, +27%] and necessarily < writev's failed overlapped fraction.
2. BY THE NUMBERS the COMPUTE gap (1.33×) is the BIGGER lever: a PERFECT writer-thread caps the tmpfs win at ~0.13× (1.46→1.33×, achievable fraction less), landing on the 1.33× compute FLOOR which then dominates. The sink is a polish on the smaller half. We've spent the campaign on the consumer/sink chain; /dev/null reveals the bigger gap is UPSTREAM of the write.
- The 2×2 factorial's "consumer dominant ~40%" was measured WITH the write in the path ⇒ does NOT transfer to /dev/null (where the consumer's dominant component, the write, is gone). Must RE-decompose the 1.33× compute gap at the /dev/null sink.
writev: KEEP quarantined behind GZIPPY_DISABLE_WRITEV (layer-don't-revert; load-bearing only IF a writer-thread uses it). REVERT if the overlap-oracle comes back flat (then it's dead surface justified by a disproven lever). Do NOT cite "we built writev" as momentum toward the writer-thread.
NEXT = MEASUREMENT not build (methodology: ceiling-before-build, numbers drive): (1) consumer write-time-vs-next-chunk-wait instrument (fills the factorial's consumer-WAIT blind spot; discriminates write-bound-with-headroom vs starved-no-headroom = the writer-thread overlap ceiling). (2) RE-RUN the decode/consumer perturbation factorial AT THE /dev/null SINK to decompose the 1.33× compute gap (decode vs marker-resolve vs serial-drain) — names the real bigger lever. (3) throwaway async-write spike for the writer-thread overlap ceiling ONLY if (1) shows headroom. (4) 3-sink decomposition (null / pre-faulted anon buffer / tmpfs) to pin the write mechanism before recording it. Do (1)+(2) first.

## 2026-06-01 — M1+M2 reconciled; a 9-THREADS-ON-8-CORES OVERSUBSCRIPTION CONFOUND found (advisor a4cb0918, source-confirmed) — potentially contaminates T8 campaign numbers
M1 (T8 tmpfs): consumer write=227.7ms, next-chunk-wait=454.6ms (~all block_fetcher_get), write/wait=0.50 ⇒ "consumer STARVED on decode, writer-thread has ~0 overlap headroom, DON'T build it". M2 (/dev/null 3-knob factorial): decode +33ms(5%), CONSUMER-NON-WRITE +103ms(16% DOMINANT), marker-resolve +33ms(5%), sub-additive interactions.
THE CONFOUND (source-confirmed, not speculation): at T8 gzippy spawns 8 WORKER threads pinned cores 0-7 (chunk_fetcher.rs:306 pool_size=min(par,physical); thread_pool.rs:66 i%ncores) + the CONSUMER runs on the calling thread UNPINNED (single_member.rs:33), and the harness `taskset -c 0-7` confines all 9 to 8 cores ⇒ the consumer is a 9th thread TIME-SLICING against a worker, STEALING decode cycles. NOT a clean 8-worker decode with a free consumer core.
RECONCILES EVERYTHING (M1 & M2 consistent, both partly confounded by this):
- M1-wait-dominant & M2-compute-dominant = SAME phenomenon two ends: the consumer's work sits on the critical path AND steals a decode core → decode falls behind → consumer waits. M2's +103ms compute elasticity is partly real-critical-path + partly "slowed consumer holds the stolen core longer → decode falls further behind" (can't separate from M2 alone).
- block_fetcher_get-454 vs Σ-HoL-tiny(2-29ms) vs decode-only-5%: NOT contradictory. Tiny HoL = not waiting on a laggard-while-others-ready. 454ms block_fetcher_get = waiting because NOTHING ready = LATENCY-OF-THE-RIGHT-CHUNK supply starvation (per-chunk decode fast enough at 5%, but the bounded prefetch window [block_fetcher.rs:712 prefetching_len+1>=parallelization] + a stolen core can't keep the in-order consumer fed). Decode is latency-of-the-right-chunk bound, NOT throughput bound.
⇒ DO NOT build the consumer-non-write removal oracle next — TRIPLE-CONFOUNDED (true-critical-path + freed-core-decode-speedup + wait-absorbed-compute); it would show a phantom ceiling. Also re-opens the writer-thread: if the consumer is stealing a core, OFFLOADING the write (= freeing the core) could speed decode + shrink the wait, so M1's "writer-thread dead" verdict may be a 9-on-8 artifact.
POTENTIAL CONTAMINATION: every prior T8 number measured at `CPUS=0-7` had this 9-on-8 oversubscription. The relative gzippy-vs-rapidgzip ratios may still hold IF rapidgzip has the same consumer+worker topology (it does — same parallel-SM shape), but absolute T8 walls + the per-region splits are suspect. FLAG for re-confirmation.
NEXT = THE DISCRIMINATING MEASUREMENT (one variable, not a build): interleaved T8 `CPUS=0-8` (consumer gets its OWN 9th core) vs `CPUS=0-7` (current 9-on-8), compare wall / block_fetcher_get / consumer-compute. TRISECTS: (a) wall drops + block_fetcher_get collapses ⇒ OVERSUBSCRIPTION ARTIFACT, lever = pinning/offload (and the writer-thread is NOT dead — freeing the core is what offload does); (b) wall flat + wait stays ~454 ⇒ DECODE-SUPPLY bound, lever = prefetch depth (GZIPPY_BURST_PREFETCH exists, chunk_fetcher.rs:512), NOT consumer-compute NOT inner-decode; (c) wall flat + wait shrinks + consumer-compute now dominates smaller wall ⇒ genuine CONSUMER-COMPUTE bound, THEN the removal oracle is justified (with 9-on-8-vs-9-on-9 as its positive control). Uses existing infra (taskset CPUS, M1 trace points, BURST_PREFETCH). Run this BEFORE any oracle/build.

## 2026-06-01 — TRISECTION: oversubscription confound CLEARED (T8 not contaminated); decode-supply verdict, but the "contradiction" = wrong-target perturbation (advisor a3ecd13). ONE discriminating measurement left before any floor call.
TRISECTION (frozen, pure-rust dynsym=0, sha-verified, N=13-21, ONE variable=consumer core placement, 8-worker pinning proven byte-identical between arms via /proc/tid/Cpus_allowed_list):
| arm | wall median | bfg wait | vs rapidgzip |
| A 9-on-8 (consumer floats in 8-P-core cpuset) | 0.3799s | 161.5ms | 0.665× |
| B 9-on-9 (consumer→cpu15 free HT sibling) | 0.3812s (+0.3% TIE; tight N=21 −0.8% med/−2% min, inside ~9% spread) | 164ms | 0.663× |
| C 9-on-8 + GZIPPY_BURST_PREFETCH | +1.3% TIE | flat | 0.657× |
| rapidgzip same mask | 0.2527s | — | (gzippy 1.50×) |
⇒ NOT oversubscription (B=TIE) — PRIOR T8 ABSOLUTES NOT CONTAMINATED (9-on-8 costs ~0% wall). NOT prefetch-cap (C=TIE). bfg wait FLAT at exactly count=4 stalls ~57ms each in every arm.
ADVISOR RESOLVES the "decode-supply bound vs decode-5%-elastic vs resolve-ahead-dead" contradiction: it's NOT a contradiction and NOT a floor-proof — it's a WRONG-TARGET PERTURBATION. SLOW_BOOTSTRAP slows EVERY chunk uniformly = perturbs the OVERLAPPED BULK worker-throughput term (37× slack → 5%-elastic, near-flat). The bfg-wait is a LATENCY on the SERIAL publish chain = a DIFFERENT model term (frontier + N·L_resolve). Wall = MAX(worker-bound, publish-chain); the 5%-injection hit the NON-binding term. So decode-5%-elastic says nothing about the serial edge-latency the consumer actually waits on. The loop is NOT logically exhausted.
THE ONE DISCRIMINATING MEASUREMENT LEFT (unrun; both outcomes change the decision — NOT loop-padding): add the model-specified per-chunk `window.published` consumer span (plans/parallel-sm-model.md names it as the missing instrument), trace gzippy AND rapidgzip (scripts/rapidgzip_trace_patch) at the SAME 9-on-8 point, answer: (1) are the 4 bfg-stalls STARTUP-CLUSTERED [frontier-ramp t_publish(0..4), largely IRREDUCIBLE — can't consume chunk 0 before it decodes] or SPREAD through the run [recurring L_resolve = live bankable lever]? (2) does rapidgzip pay the SAME ~160ms consumer-wait [SHARED ⇒ gap is diffuse BULK memory-stalls/IPC per real_gap_pinned 1.5×-cycles ⇒ surface STRUCTURAL FLOOR to user] or FAR LESS [gzippy-specific cheaper pipeline-FILL ⇒ located lever = first-chunk decode / initial prefetch]?
DECISION TREE: rapidgzip-wait≈gzippy's AND startup-clustered ⇒ STOP, surface to user (locatable levers exhausted; residual 1.3-1.5× = diffuse bulk + irreducible frontier-ramp; needs rapidgzip-grade rewrite not a lever). rapidgzip-wait≪gzippy's ⇒ located fill lever, pursue it. DO NOT name/record/floor-call until this trace lands; DO NOT re-slow-inject decode (proven wrong-target). DO NOT re-run a placement/resolve-ahead oracle (already TIE'd = loop-padding).

## 2026-06-01 — LOCATED the wall (consumer-wait gzippy-specific) but the proposed FIX is HALF-UNSAFE/HALF-ALREADY-SHIPPED — NO parity GO (advisor a00762ae). Gate: counter-attribution first.
THE MEASUREMENT (sound, decisive — WHERE the wall is): cross-tool per-chunk consumer-wait trace, T8 9-on-8, frozen, sha-verified all==e114dd2. gzippy block_fetcher_get = 167ms (35% of 472ms wall), 4 stalls SPREAD: chunk 0 @0% 19ms, chunk 11 @8.7% 56ms, chunk 41 @38% 50ms, chunk 54 @57% 42ms. rapidgzip wait.future_recv = 18ms (5%), ~all in ONE startup stall. Delta 149ms OVER-explains the 124ms wall gap ⇒ NOT a diffuse floor, a LOCATED lever; chunk-0 (19≈18ms) = SHARED irreducible first-chunk floor, only the 3 mid-run stalls (~148ms) are gzippy-specific. gzippy on-demand-decodes the awaited chunk during the stall; rapidgzip keeps it prefetch-ready.
THE FIX CONFLICT (advisor, source-re-confirmed): the agent's "close range-accept guard 1152 + re-prefetch-during-wait" CONFLATES two orthogonal mechanisms:
- SAFE half = re-issue prefetch on the 1ms wait-poll (vendor BlockFetcher.hpp:314). ALREADY SHIPPED as gzippy Lever H (chunk_fetcher.rs:1167 try_take_prefetched_pumping, the pump closure). ⇒ it is RUNNING DURING the measured stalls ⇒ it CANNOT be the unbanked 148ms lever.
- UNSAFE half = relax chunk_fetcher.rs:1152 exact-equality to range-accept. RE-CONFIRMED corrupting (line 230): gzippy's [encoded,max) is a PREFIX-GAP range of real predecessor data (NOT vendor's stored-block ALIAS/zero-padding); accepting S<max DROPS [S,max) = corruption. Stays refuted.
WHY gzippy on-demand-decodes (must read COUNTERS, not assume): either (a) prefetch arrived but THROWN AWAY by the guard [PREFETCH_REJECT_BY_GUARD, chunk_fetcher.rs:1228] ⇒ only the UNSAFE range-accept captures it ⇒ safe ceiling ~0, parity UNREACHABLE without corruption; or (b) pool SATURATION crowds out the next-in-order frontier chunk [thread_pool_saturated, block_fetcher.rs:710] ⇒ a SAFE frontier-priority-dispatch lever exists (submit next-in-order ahead of far-ahead speculatives), captures the saturation FRACTION. BURST_PREFETCH=TIE rules out DEPTH (different knob from ordering+re-issue) but WEAKLY favors the guard-reject (unsafe) explanation.
CEILING HONESTY: the agent's 148ms→parity is an OVERCLAIM — safe half already shipped; irreducible ~40-56ms decode latency per heavy chunk; removing the wait MIGRATES the bottleneck to worker contention (saturated pool). Realistic safe ceiling = a worker-contention-bounded FRACTION of 148ms, or ~0 if guard-rejects dominate. The full 148ms is ONLY captured by the unsafe range-accept.
⇒ NO parity-class GO. GATE-PENDING = the counter-attribution measurement (NO code): T8 trace, per-stall PREFETCH_REJECT_BY_GUARD vs prefetch_cache_miss vs PREFETCH_RETURN_SATURATED. reject-by-guard dominates the 3 mid-run stalls ⇒ STOP (only lever is unsafe corruption; parity honestly unreachable here). saturation/cache-miss dominates ⇒ SAFE frontier-priority-dispatch lever, gated + sha-verified + ceiling-by-removal (interleaved N>=7, delta>spread or TIE). Do NOT touch 1152; do NOT claim parity until the counters say a safe lever exists + a gated A/B banks it.

## 2026-06-01 — GATE RESOLVED: counter-attribution + prior frontier-placement-TIE ⇒ the "located fill lever" is the KNOWN RATE floor, BOTH halves DEAD. No safe parity lever.
COUNTER READ (pure-rust dynsym=0, GZIPPY_FORCE_PARALLEL_SM=1, T8, silesia-large, path=IsalParallelSM verified, arm64 — counts are freq/arch-independent for the guard question):
  Prefetch guard-rejects: 1  (≈0)        ⇐ UNSAFE range-accept half is DEAD — there are NO thrown-away range-valid prefetches to recover. Advisor's corruption trap is moot; so is any guard-reject win.
  Prefetch Cache Misses: 33               ⇐ consumer demanded chunks not cached
  Prefetch dispatch: called=452 saturated=372 (82%) total_submitted=55  ⇐ pool full (prefetching_len+1>=8) 82% of calls; 7+ chunks in-flight nearly always.
⇒ Stalls are CACHE-MISS-UNDER-SATURATION, NOT guard-reject. Per the advisor gate this is the "SAFE frontier-priority lever exists" branch — BUT:
KILL SHOT (scoreboard line 463 + 648, already banked): the SAFE frontier-priority lever the gate points to was ALREADY CAUSALLY TIE'd. The frontier-placement oracle forced the gating chunk decoded+resolved AHEAD of the consumer = TIE T4/8/16 (0.992/1.012/0.997); frontier_already_inflight=94% (gzippy ALREADY resolves ahead like vendor); pool.pick.wait dropped 525→420ms but did NOT convert to wall. S1 schedule = RATE-100% AGREES. The consumer waits on the frontier chunk's OWN DECODE finishing, NOT on ready-work-unused.
SYNTHESIS: the recent 167ms-vs-18ms-spread measurement REDISCOVERED the known RATE floor and framed it as a new lever. gzippy's consumer waits 9× longer than rapidgzip's because each frontier chunk takes LONGER to decode (pure-rust single-symbol ~150MB/s vs rapidgzip multi-symbol ~340MB/s) AND the workers are saturated with productive in-order work so the latency can't be hidden — RATE, not placement, not guard-reject. Both halves of the fill lever (unsafe range-accept; safe frontier-priority) are now DEAD: one by counter (guard≈0), one by prior causal oracle (placement TIE).
⇒ The ONLY residual direction is decode RATE (pure-rust→isal-class multi-symbol decode, ~15% bounded per project_t8_saturated_pool_diag) which does NOT reach parity; and the shipped-ISA-L architectural gap (1.19/1.45× T8/16 with FEWER instructions, line 659) which is the diffuse stall gap with no located lever. This matches line 757's STOP branch (consumer wait is RATE-bound, locatable levers exhausted) — surfacing to user pending advisor sign-off on the STOP.

## 2026-06-01 — STOP CONFIRMED (advisor a05a9575, source-grounded): no safe located lever reaches x86 parity. Surface to user.
Advisor strengthened the verdict with a SOURCE fact I'd missed: the SAFE frontier-priority-dispatch lever is ALREADY SHIPPED — chunk_fetcher.rs:1861-1867 assigns on-demand frontier decode priority -1 vs priority-0 speculatives, popped lowest-first from a real BTreeMap priority queue (thread_pool.rs:499). So:
  Q1 (daylight resolve-ahead vs dispatch-priority?): NONE — dispatch-priority is the production code; the frontier-placement oracle forced resolve EVEN FURTHER ahead (94% already in-flight) and added nothing = TIE.
  Q2 (saturated-82% RATE vs far-ahead-crowding?): RATE — priority -1 STRUCTURALLY prevents speculative crowding from delaying frontier dispatch; the only way the frontier waits is all-8-workers-mid-execution = no-idle-worker = RATE. No counter needed.
  Q3 (9× wait vs 2.27× decode-rate excess?): apples-to-oranges, NOT a third mechanism — gzippy's 167ms = ~4 stalls incl. shared ~19ms startup floor; rapidgzip's 18ms = 1 startup stall. Per-stall it's frontier-chunk decode latency × stall-count. count×rate reconstructs it; cache-miss=33 are "frontier not done yet" (RATE), not "never dispatched" (priority -1 prevents).
LOAD-BEARING FACTS all hold on the relevant arch: (a) guard≈0 arch-independent; (b) priority -1 shipped, arch-independent code; (c) frontier-placement oracle TIE measured on x86. (Counter read was arm64 but the guard/saturation facts are scheduling, not timing.)
TERMINAL VERDICT: both fill-lever halves DEAD (unsafe range-accept: guard≈0 nothing to recover; safe frontier-priority: already shipped + oracle-TIE'd). The recent 167-vs-18ms measurement REDISCOVERED the known RATE floor. Residual = (A) decode RATE: pure-rust single-symbol ~150MB/s → isal-class multi-symbol ~340MB/s, ~15% bounded, does NOT reach the 1.47× x86 parity; (B) diffuse shipped-ISA-L architectural stall gap (1.19/1.45× T8/16 with FEWER instructions), no located lever. This is line-757's STOP branch. No cheap discriminator remains. SURFACE TO USER.

## 2026-06-01 — TWO-AGENT CONVERGENCE (user-requested): architectural audit + claims-validation point at the SAME region = the data-plane output tail.
ARCHITECTURAL CONFORMANCE AUDIT (agent a47a8866, source-read, file:line both sides): gzippy ALREADY matches rapidgzip's entire CONTROL PLANE faithfully — chunk lifecycle/consumer loop, prefetcher (1ms re-issue + saturation gate + double-key lookup), FetchingStrategy/splitIndex, BTreeMap priority pool (frontier=-1 IS vendor submitTaskWithHighPriority(-1), not an invention), block finder/partitioner, window-map keying + consumer-side publish timing, u16 marker machinery + clean-window bootstrap→ISA-L handoff (cited ports deflate.hpp:1282↔deflate_block.rs:781 VERIFIED accurate), CRC32 worker-compute/consumer-combine. CLAUDE.md premises confirmed by source. DIVERGENCES are ALL in the DATA PLANE / output tail:
  #8 DIVERGE-MAJOR: marker resolve writes a SEPARATE `narrowed` u8 buffer (chunk_fetcher.rs:2410-2461) instead of vendor in-place reinterpret-then-swap (DecodedData.hpp:305-391). 3-buffer vs vendor 2-touch.
  #10 DIVERGE-MAJOR: output write = BufWriter::write_all over contiguous buffers (chunk_fetcher.rs:3040-3080) instead of writev/iovec gather (ChunkData.hpp:794 toIoVec, DecodedData.hpp:529). [PR#126 feat/writev addresses it, NOT on this branch.]
  #7b/#9 DIVERGE-MINOR→MAJOR (the ENABLER for #8+#10): monolithic Vec<u16>+contiguous Vec<u8> vs vendor 128KiB-granule chunked MarkerVector/VectorView (DecodedData.hpp:231-289, ALLOCATION_CHUNK_SIZE=128Ki).
  #11 MISSING: queuePrefetchedChunkPostProcessing exists (eager_postprocess_prefetched chunk_fetcher.rs:1294) but DISABLED by default; vendor runs it unconditionally.
  EXTRA: speculative window promote/evict (window_map.rs:215-268) — gzippy-only, honestly documented; vendor's chain invariant makes it unnecessary.
  Only overstated doc: chunk_data.rs:104-107 claims buffer layout "matches" DecodedData while omitting chunked-vs-monolithic + 3rd buffer.
CLAIMS VALIDATION (agent a762f6b0, adversarial, frozen/pinned/sha-verified, pure-rust dynsym=0, path=IsalParallelSM):
  C1 guard≈0: CONFIRMED on x86 (0 guard-rejects, 3 runs). Unsafe range-accept has nothing to recover.
  C2 RATE-not-PLACEMENT: CONFIRMED (schedule RATE-100%; during ttp.rx_recv_block waits 8-14/17 chunks decoded but frontier still decoding → ready successors UNUSABLE under in-order consume = RATE by definition; S1 missing-span bug does NOT bite, spans exist via format!).
  C3 consumer-null=+27% T8 (LOAD-BEARING): CONFIRMED + REFINED — recovery is STRICTLY CONDITIONAL on a REAL DOWNSTREAM SINK (file/pipe): sink=/dev/null → TIE (-1%); sink=file → +19.6%/+31.1% (N=21/15). Positive-controlled (consumer_work_executed=0, conservation_ok, decode-busy identical OFF/ON Δ0.05% = consumer-LOCAL). Mechanism = in-order consumer's serial real-sink write: OUTPUT = 42.2ms write_data + 18.6ms write_narrowed ≈ 60ms (24% of 173ms wall), additive/non-overlapping; combine_crc=0.02ms (CRC genuinely O(1), parent right). ⇒ the writev/in-place-resolve plan is validated FOR REAL-SINK (production) workloads; measuring it against /dev/null would FALSELY refute it.
  C4 1.47× cycles: REFUTED on magnitude — real ~1.25× cycles / ~0.92× instructions (gzippy runs ~8% FEWER instructions). 1.47× was turbo/SMT (matches prior "1.42× was turbo"). Direction (memory-stall-bound) CONFIRMED + stronger. Corpus caveat: agent's silesia-large was ~1/3 parent's size (rebuilt from /tmp/silesia.tar); ratios robust, absolutes not comparable.
CONVERGENCE: the ONLY remaining DIVERGE-MAJOR region (data-plane: in-place resolve #8 + writev output #10, enabled by 128KiB-granule #9) is EXACTLY the region C3 positive-controls at +20-31% on real sinks. The structural rewrite the user wants is now PRECISELY SCOPED = faithfully port vendor's 128KiB-granule MarkerVector + in-place applyWindow-then-swap + toIoVec/writeAllToFdVector as ONE coherent data-plane port. CAVEAT: feat/rapidgzip-memory-model built SegmentedU16+in-place and REGRESSED — likely because it added granule machinery WITHOUT capturing the output-write (still wrote through BufWriter); the new port must target toIoVec-DIRECT-to-fd (the C3 win), not just segmentation. And the win is real-sink-only — bench MUST use a file/pipe sink, never /dev/null.

## 2026-06-01 — DATA-PLANE PORT design (advisor abf506fb): the LINCHPIN reshapes the win, port stays.
GOAL (user-set): faithfully port 128KiB-granule MarkerVector + in-place applyWindow-then-swap + toIoVec/writeAllToFdVector as ONE coherent data-plane change; verify with Fulcrum. User directives: (i) use subagents for as much as possible; (ii) dev-time irrelevant — make EVERY reasonable+should-be-performant improvement, measure WHOLE SYSTEM before/after, do NOT bank incremental wins as if individually valuable (layer-don't-revert, whole-system).
LINCHPIN (advisor, vendor-cited): vendor does NOT vmsplice FILES. ChunkData.hpp:794-822 writeAll tries vmsplice then FALLS BACK to plain ::writev (writeAllToFdVector, FileUtils.hpp:765-800) for any non-pipe fd. vmsplice is memory→PIPE only (FileUtils.hpp:453,504). ⇒ the ~60ms C3 file-write is SHARED with rapidgzip (both writev the same bytes to page cache) ⇒ toIoVec/writev-to-FILE is WALL-NEUTRAL vs rapidgzip (explains feat/writev FLAT). The ONLY un-ported vendor data-plane win is vmsplice+SpliceVault on the PIPE sink (FileUtils.hpp:447-528,582-702; SpliceVault lifetime mandatory for correctness, :656-677).
CONVERGENCE CORRECTED: the prior "audit-diverges + C3-60ms ⇒ port closes the gap" FALSELY FUSED a shared file-write cost with the real 1.25×-cycle memory-stall gap. The stall lives on the WORKER FOOTPRINT (the in-place-resolve cache-residency benefit = ORACLE-c territory, never cleanly isolated), NOT the consumer file-write. So C3's +20-31% = "gzippy's own file-write wall", not "more than rapidgzip".
PLAN (whole-system, all reasonable improvements, measure before/after on BOTH sinks):
  KEEP regardless: in-place resolve (eliminate 3rd `narrowed` buffer) + 128KiB granule SegmentedU16 — faithful DecodedData.hpp:316-337, reduces worker footprint (the actual 1.25× territory). Reuse feat/rapidgzip-memory-model's segmented_markers.rs (byte-correct).
  BUILD write path EXACTLY as vendor: pipe→vmsplice WITH SpliceVault lifetime; file→writev (writeAllToFdVector). Reuse feat/writev's fd_vectored_write.rs, ADD vmsplice+SpliceVault. vmsplice is Linux-only (HAVE_VMSPLICE) → perf only measurable on neurotic.
  MEASURE: before/after WHOLE SYSTEM, gzippy-vs-rapidgzip, T4/8/16, interleaved sha-verified, on FILE sink AND PIPE sink (the pipe sink is where vmsplice can move wall). Also split C3 write_narrowed into narrow-scan-vs-syscall (advisor: live confound — does in-place-resolve remove shared userspace scan work?).

## 2026-06-01 — DATA-PLANE PORT landed + BASELINE measured + CORRECTNESS BUG found (advisor ab35d0e3).
PORT (feat/dataplane-2touch, 6 commits ed93a4e..6de39c2, VERIFIED on branch): SegmentedU16 128KiB granule + resolve_in_place→ResolvedMarkers (narrowed 3rd buffer REMOVED) + fd_vectored_write (to_io_vec N-seg gather, writev_all_to_fd, SpliceVault+vmsplice cfg(linux), write_all_to_fd=vmsplice-first-writev-fallback). Local (arm64+x86 Rosetta): cargo test all pass; byte-identical silesia-large sha=23818da7…==gzip; path=IsalParallelSM. NOT compiled: the cfg(linux) vmsplice module (no linux cross-toolchain on Mac).
CORRECTNESS BUG (advisor, vendor-cited, silesia CANNOT catch it): partial-vmsplice failure path double-writes + use-after-frees. If vmsplice gifts some segs then fails mid-list (EPIPE/SIGPIPE): (1) splice drops the moved-in owner → UAF on already-gifted pages; (2) try_splice Err→write_all_to_fd writev's the FULL snapshot from offset 0 → DUPLICATED bytes. Vendor (FileUtils.hpp:535-547) returns errno ONLY at i==0, THROWS at i>0 (never writev-fallback mid-stream). FIX: vmsplice_all must report bytes-gifted; >0 on failure ⇒ FATAL (propagate Err, retain owner, NO writev); only i==0 ⇒ writev fallback. Deviation #1 (SpliceVault len()>1 guard) = FAITHFUL/fine.
GATE before any wall measurement: Linux byte-identity on (a) file (b) SLOW-pipe back-pressure (forces vmsplice to block/recycle pages → exercises SpliceVault lifetime) (c) early-reader-death (EPIPE mid-stream, SIGPIPE-ignored → exercises the bug; ideally ASAN). All must pass or FIX first. Fast-sink silesia is necessary-not-sufficient.
BASELINE "before" (agent afbb57c7, neurotic frozen, pure-rust dynsym=0, path=IsalParallelSM, N=9 interleaved sha-verified, corpus silesia-large 68MB→212MB, file=tmpfs pipe=|sha256sum, input warm):
  file  T4/8/16 gz/rg = 1.191 / 1.214 / 1.078
  pipe  T4/8/16 gz/rg = 1.197 / 1.166 / 1.034
  ⇒ gap is SINK-INDEPENDENT ~1.21× at T8 (BOTH sinks ~equal). PIPE does NOT close the gap ⇒ rapidgzip's vmsplice is NOT what produces the ~1.2×; the deficit is COMMON to both paths = the data plane (decode/marker-resolve/consumer-write), NOT the write syscall mechanism. REVISES the linchpin: the win (if any) is the 2-touch consumer-write collapse (2 writes→1 writev, BufWriter gather removed) + worker footprint (3-buffer→2-touch granule), on the COMMON path — not pipe-vmsplice specifically.
  DECOMP (T8 file, 1 instrumented run): narrow scan is FUSED into apply_window (48.5ms, on parallel WORKERS, off consumer); consumer = write_narrowed 19.0ms + write_data 41.4ms = ~60ms serial + combine_crc 0.02ms. Eliminating narrowed collapses the two consumer writes into one writev = removes a sink/syscall+buffer-split cost (NOT the already-parallel narrow scan).
NEXT: (1) FIX the vmsplice corruption bug (subagent, local). (2) ONE neurotic agent (after box free): Linux build → byte-verify GATE (file+slowpipe+earlydeath) → AFTER measurement vs this baseline (T4/8/16, file+pipe, interleaved sha-verified). SCORECARD: does the 2-touch port move the COMMON ~1.2× on either sink? falsifiers: file REGRESS ⇒ granule overhead resurfaced; pipe flat w/ vmsplice-engaged ⇒ write-mech not the lever (baseline already hints this); footprint unchanged ⇒ narrowed not really gone / segment pool not recycling. vmsplice-engagement counter must be >0 pipe / 0 file.

## 2026-06-01 — DATA-PLANE PORT: correct (gate green) but WHOLE-SYSTEM REGRESSION (agent a36649ab). SURPRISE → advisor + Fulcrum (don't narrate).
CORRECTNESS (gate fully green, vendor-faithful bug fix landed, feat/dataplane-2touch @ d2b342f): vmsplice partial-failure fix = VmspliceFail{err,bytes_gifted} → SpliceOutcome Declined(i==0, intact owner, writev fallback) / Fatal(i>0, owner RETAINED in vault, hard Err, NO writev) / Spliced — mirrors vendor throw-on-i>0. Linux compile clean (first time), pure-rust dynsym=0, path=IsalParallelSM. GATE: (a) FILE sha==ref vmsplice=0 (declines→writev); (b) SLOW-PIPE backpressure sha==ref vmsplice=17 (vault held/dropped, no corruption); (c) EARLY-DEATH clean SIGPIPE exit, correct PREFIX no-dup, valgrind 0 errors (UAF fixed). 5 failing tests are PRE-EXISTING (fail identically on merge-base ef07076).
AFTER vs BASELINE (N=9 interleaved sha-verified, P-core, gz/rg ratio, higher=worse):
  file T4/8/16: 1.191→1.251 / 1.214→1.284 / 1.078→1.172  (Δ +0.060/+0.070/+0.094, REGRESS all 3, direction-consistent; T16 +8.7% clears rg spread 4.1%)
  pipe T4/8/16: 1.197→1.193 / 1.166→1.223 / 1.034→1.066  (T4 TIE −0.004 w/ vmsplice=17, T8 +0.057, T16 +0.032; pipe improved NOWHERE)
  vmsplice engagement: FILE=0, PIPE=17 (counter as designed).
VERDICT (numbers, no spin): port did NOT move wall toward parity — REGRESSED file (all T), NEUTRAL-to-worse pipe. Confirms baseline: vmsplice is NOT the lever (sink-independent ~1.2× gap). 
BUT (per user lock-picking directive): the port = THREE bundled tumblers (128KiB granule, in-place-resolve eliminating narrowed, writev/vmsplice). The BUNDLE regressed — but WHICH tumbler? Agent's "granule overhead resurfaced" is INTUITION held lightly, NOT measured. Hypotheses to test with Fulcrum (don't narrate): (h1) writev over many 128KiB segment iovecs = MORE/smaller kernel writes vs BufWriter's coalesced 1MB → syscall-granularity regression (file, where vmsplice declines→writev); (h2) resolve_in_place per-segment slower than the baseline FUSED replace_markers_lut_narrow (48.5ms) → worker time up, maybe onto critical path; (h3) segment alloc/recycle cost. Correct+reversible+gate-green ⇒ KEEP layered (a set tumbler), do NOT revert; diagnose mechanism via Fulcrum + improve Fulcrum to expose per-sub-change cost.

## 2026-06-01 — REGRESSION NOT PROVEN (advisor a6ebf185). Keep bundle; higher-N re-run before attributing anything.
SIGNIFICANCE (advisor, ruthless): every Δ is INSIDE one combined spread — file T4/8/16 Δ/spread = 0.35/0.49/0.86 (all <1), pipe T4 dead-flat (−0.004) + others <0.3 = TIE. 3/3 file direction-consistency = p=0.125 (NOT significant) AND correlated (one session/thermal, not 3 independent obs). ⇒ NO real regression established; pipe = clean TIE by the project's own Δ<spread rule. I was about to attribute a +0.07 not yet shown to EXIST. FIRST ACTION = file-only re-run at N≥21 (stdev not range), not h1/h2/h3 attribution. If Δ collapses → noise → KEEP port on a TIE; if it holds above spread → real, then discriminate.
KEEP THE BUNDLE (lock-picking doctrine, advisor-concurred): no measured worse-whole-system exists → revert nothing. T1 granule + T2 in-place-resolve = footprint/structure PREREQUISITES with clear combinatorial future (T2 shrinks the exact footprint the OPEN compute-vs-memstall fork is about). T3 writev/vmsplice = faithful-but-INERT (baseline already showed vmsplice buys nothing, gap sink-independent); if its oracle confirms a writev-granularity cost, TRANSFORM writev→coalesced-write (keep the data-plane, drop only the harmful mechanism), do NOT rip out. NOTHING is a dead tumbler.
DISCRIMINATION (only if N≥21 file signal survives spread): two REMOVAL ORACLES on file T8/T16 — (O1) coalesce iovec list into ONE buffer + single write_all (nulls h1 writev-granularity, keeps seg+in-place); (O2) revert resolve to the old fused replace_markers_lut_narrow (nulls h2 resolve-vs-fused, keeps seg+writev). Whichever restores baseline names the tumbler. + Fulcrum ADD: writev syscall count + iovec count + bytes-per-write (port vs baseline) = the h1 discriminator AND a real new parameter the old regions can't show; assert busy+idle==span (avoid combine_crc-style double-count).
NEXT PROBE (the real sink-independent ~1.2×, now that the write mechanism is ELIMINATED from TWO directions — baseline vmsplice-inert + this port wall-neutral-at-best): the FRONTIER CHUNK's decode RATE, compute-bound vs memory-stall FORK (the one OPEN item in project_t8_saturated_pool_diag). Mem-stall oracle scoped to the frontier decode FIRST; if compute-bound → the authorized multi-symbol inner Huffman loop (rapidgzip-diff mechanism = legit, not a re-tread); if mem-stall-bound → footprint (T2 already on the right side). T2's footprint win is on the right side of the fork either way.

## 2026-06-01 — DATA-PLANE PORT verdict FINAL (agent af8ebf2d, N=41 paired): real-small-file-regression / pipe-TIE; BOTH oracles EXONERATE; cause diffuse in granule. Write-mechanism DEAD as the lever (3 ways).
INSTRUMENT CATCH: first N=21 read was contaminated by a hung cargo-test PID (1.3GB RSS, deadlocked-idle) inflating stdev → all-TIE; killing it surfaced the signal. (validate-the-instrument held.)
SIGNIFICANCE (clean box): independent-median = TIE every cell (Δ < combined-sd), BUT paired interleaved (cancels common-mode drift) FILE T8 N=41: mean(AFTER−BEFORE)=+0.01107s t=8.03 38/41 slower; T16 +0.01644s t=9.01. ⇒ port is a REAL small file regression (~+5-8% gzippy wall) masked as TIE by median noise. PIPE = clean TIE all T.
NEW FULCRUM PARAM banked (iovec granularity, file T8, vmsplice=0): BEFORE 33 write() @ mean 6.42MB (BufWriter-coalesced); AFTER 17 writev() @ 1167 iovecs total / 68.65 iovecs/call / 12.47MB/call (128KiB granules). Old write_* spans couldn't show this.
ORACLES (file T8+T16 N=41 paired, both sha-identical) — BOTH EXONERATE:
  O1 null-h1 (coalesce 69 iovecs→1 scratch+1 write): +0.04270 t=20.48 = 4× WORSE ⇒ writev-granularity is NOT the cause and is COUNTER-INDICATED — the multi-iovec writev is CHEAPER; the 200MB coalescing memcpy costs more. Vendor's writev-over-segments is right.
  O2 null-h2 (revert resolve to fused fresh-buffer): +0.00973 ≈ AFTER ⇒ resolve-method is NOT the cause.
  ⇒ residual ~+0.011-0.016s is DIFFUSE in the SegmentedU16 128KiB-granule structure itself (or consumer reorg / CRC-combine order), NO single named owner.
GOAL STATUS: the faithful port (granule+in-place+toIoVec/writev/vmsplice) is COMPLETE + CORRECT (gate-green, byte-identical, valgrind-clean) and Fulcrum has IDENTIFIED the verdict = correct-but-not-a-win (small file regression / pipe TIE; cause diffuse in granule; output-tail/write-mechanism ELIMINATED as the ~1.2× lever, now confirmed 3 ways: baseline-vmsplice-inert + this-port-wall-neutral + O1-coalesce-worse).
KEEP-vs-REVERT (coupled to the OPEN fork): T1 granule has a small measured COST; its combinatorial FUTURE = T2 in-place footprint reduction, which only pays IF the real ~1.2× is memory-stall-bound. So the DECIDER is the next probe = frontier-chunk decode RATE compute-vs-memstall fork. memstall-bound ⇒ keep+extend T1/T2 (footprint foundation); compute-bound ⇒ revert the port (cost, no future) + do the authorized multi-symbol inner Huffman loop. Branch feat/dataplane-2touch stays UNMERGED (not a win) pending the fork.

## 2026-06-01 — PORT done-checkpoint reviewed (advisor a46f0a8e): GOAL SATISFIED; the FALSIFICATION is the headline; fork is REFUTED-both-ways; keep gated on a cheap RSS probe.
GOAL SATISFIED: "use Fulcrum to identify if done correctly" = faithful + byte-correct + truthfully-measured (NOT must-win). Delivered. Declare as completed-measurement-with-honest-negative; do NOT add a "fix the regression first" requirement (the cause is a non-lever).
HEADLINE (advisor, stronger than "small regression"): this is the 2nd SegmentedU16+in-place attempt to regress, and THIS one CAPTURED the predicted-missing toIoVec/writev fix and STILL regressed on file ⇒ FALSIFIES the convergence hypothesis that the data-plane output tail (granule+in-place+writev) was the sink-independent ~1.2× lever. The last DIVERGE-MAJOR region is now ELIMINATED BY CONSTRUCTION. Confirms the prior advisor STOP (a05a9575).
NEXT-PROBE CORRECTION: the compute-vs-memstall fork is the WRONG next probe — BOTH exits already refuted: (compute) the multi-symbol/branchless inner Huffman loop was BUILT + measured FLAT (agent ad456145: branches/byte −13.3%, insns −3.8%, byte-exact, wall in noise; TopdownL1 co-limited BE31/Ret28/BadSpec26 = inherent dispatch floor libdeflate pays too); (memstall) footprint/working-set DEAD both directions (faults=slack 3 oracles; chunk-size sweep dead both ways; gzippy clean-import already 5.3× FEWER LLC-misses than rapidgzip). No cheap discriminator resolves the attribution into a lever — why prior sessions surfaced STOP. Do NOT re-enter without a NEW rapidgzip-diff mechanism. PIVOT to a cell with a clear lever: incompressible T>1 (~0.85×) or single-member T2<T1 (advisor-flagged clearer than the near-optimal bootstrap).
KEEP-vs-REVERT (advisor: gate on evidence not hope): doctrine revert-line = worse-whole-system AND no-combinatorial-future. File regression is real (met); the ONLY future is T2 in-place-resolve being latent-footprint-positive — and granule(regressing) + in-place(future) are COUPLED. So KEEP only if a cheap probe shows in-place-resolve is latent-positive (lower peak RSS; pipe wall already TIE). Else REVERT the granule. EITHER WAY cherry-pick forward the durable CORRECTNESS fixes: vmsplice partial-failure UAF/double-write fix (2a32806), early-reader-death determinism (996c823), SIGPIPE handling.
NEXT: cheap RSS probe (freq-independent) — peak RSS (VmHWM/maxrss) BEFORE vs AFTER, T8, both sinks = the keep-decider. Then report goal-done + falsification headline + pivot recommendation to user.

## 2026-06-01 — KEEP-DECIDER resolved (agent a4d0b2b5): RSS latent-positive ⇒ KEEP feat/dataplane-2touch UNMERGED. GOAL DONE.
RSS PROBE (freq-independent, T8, silesia-large, 54 runs all sha==ref, interleaved A/B N=10): peak RSS (GNU time -v MaxRSS) BEFORE vs AFTER:
  file 506,120 → 488,304 KB = −3.5% (~17.4MB); pipe 501,480 → 486,960 KB = −2.9% (~14.2MB). Non-overlapping distributions, AFTER<BEFORE in 20/20 pairs ⇒ REAL consistent signal (= the eliminated per-chunk `narrowed` U8 buffer). Modest (<5%) but real.
KEEP DECISION (advisor a46f0a8e pre-committed rule: latent-positive lower-RSS + pipe-wall-TIE ⇒ KEEP on evidence; flat ⇒ revert): result is latent-positive ⇒ KEEP feat/dataplane-2touch. UNMERGED (it's a file-wall regression, do NOT push to main). Correctness fixes (vmsplice UAF 2a32806, early-death determinism 996c823, SIGPIPE) stay on the branch — nothing to cherry-pick (reimplement-isa-l has no vmsplice/writev path).
GOAL STATUS = DONE: faithful 128KiB-granule MarkerVector + in-place applyWindow+swap + toIoVec/writeAllToFdVector ported as ONE coherent change (subagents did the work), Fulcrum IDENTIFIED the verdict = byte-correct + faithful + small file regression / pipe TIE + ~3% latent footprint win + the write-mechanism/data-plane-tail ELIMINATED as the ~1.2× lever (convergence hypothesis FALSIFIED, last DIVERGE-MAJOR region closed by construction, prior STOP confirmed).
PIVOT (next direction, NOT auto-started — scope decision for user): x86 single-member data-plane is exhaustively mapped; the fork (compute-vs-memstall) is refuted both ways. Cells with a CLEAR lever: incompressible T>1 (~0.85×, loses below its own T1) or single-member T2<T1.

## 2026-06-01 — WORDING CORRECTION (advisor a6d656b9), finalize.
The prior entry's "footprint FOUNDATION" framing is too generous. Correct record: feat/dataplane-2touch is KEPT UNMERGED as a CORRECTNESS + REFERENCE artifact that CARRIES A KNOWN +5-8% file-wall regression (paired t=8-9). The keep rule required latent-positive AND wall-neutral; the ~3% RSS reduction is real (20/20 non-overlapping = signal not noise, the keep-line is signal-vs-noise not a 5% magnitude cutoff) BUT file is NOT wall-neutral, so the 3% RSS win does NOT launder the file regression into a perf foundation. NEVER push to main. It's retained as: (1) the faithful vendor-cited data-plane reference; (2) the durable correctness fixes (vmsplice UAF / early-death / SIGPIPE, all data-plane-path-only, nothing to cherry-pick since reimplement-isa-l has no vmsplice/writev path); (3) a small real footprint datapoint IF footprint ever becomes the lever (it currently is not — fork refuted both ways).
GOAL DONE confirmed (advisor): faithful + byte-correct + truthfully-measured = what "use Fulcrum to identify if done correctly" asked ("win" was never the bar). Pivot (incompressible-T>1 / single-member-T2<T1) correctly SURFACED to user, not auto-started.

## 2026-06-02 — PIVOT (user): improve Fulcrum + add STATIC-ANALYSIS tooling until it reveals PRECISELY where time is lost, attributed to 3 buckets: (1) too-many-instructions, (2) different/worse-instructions, (3) waiting. Do it in subagents that PRODUCE NUMBERS to iterate on.
First-numbers logic: known run-level facts (T8 frozen) = gzippy 0.92× instructions (bucket 1 = NOT the loss, gzippy does FEWER), 1.25× cycles ⇒ gzippy CPI ≈ 1.36× rapidgzip. That 1.36×-worse-CPI = bucket 2 (worse mix/IPC from the code) + bucket 3 (stalls). TMA top-down splits it: Retiring/Frontend/BadSpec/Backend(Memory vs Core). + STATIC disasm of the hot decode loop (gzippy single-symbol vs rapidgzip/libdeflate multi-symbol) = bucket 1&2 at the CODE level, PERF-FEASIBILITY-INDEPENDENT. This IS the clean number for the long-open compute-vs-memstall fork. First subagent launched to produce: TMA L1/L2 both tools + static hot-loop instruction count/mix comparison. Advisor review on the RESULTS (user redirected: produce numbers, don't block on design consult).

## 2026-06-02 — FIRST 3-BUCKET NUMBERS (agent a8cb2fe6): the clean split. ~70% WAITING (memory) / ~31% DIFFERENT-instructions / ~0% too-many. SURPRISE vs "memory dead" → advisor.
INSTRUMENT: TMA worked at host paranoid=1 (restored to 4), cpu_core P-core pin (off E-core cpu_atom), N=6 interleaved sha-verified, FILE/tmpfs sink. Trap caught+fixed: gzippy has no -o flag → first run errored in 2.5ms + sha-passed a STALE file; re-collected with -c>file (real 3.9B cyc/0.16s). path=IsalParallelSM, pure-rust dynsym=0.
RUN-LEVEL (median N=6, cpu_core): gzippy 3.335B cyc / 4.911B insn / IPC 1.473 / CPI 0.679; rapidgzip 2.775B cyc / 5.339B insn / IPC 1.924 / CPI 0.520. ⇒ g/r = 1.20× cyc, 0.92× insn, CPI 1.31× worse.
TMA L1: gzippy Retiring24.6/BadSpec21.8/Frontend11.7/Backend41.9; rapidgzip 29.8/21.6/16.9/31.7. L2 mem-bound: gzippy 30.4 vs rapidgzip 22.4; core-bound 11.8 vs 9.5; branch-mispred 20.8≈20.9. Memory sub-split: DRAM gz11.1/rg8.8 (+2.3pp), L3 3.9/2.9 (+1.0pp), L2 1.4/1.4, L1 8.6/8.7.
THE SPLIT (excess = gz−rg = +0.559B cyc): bucket-3 WAITING (Backend.Memory) = +0.394B ≈ 70%; bucket-2 DIFFERENT (Core+Frontend+BadSpec) = +0.175B ≈ 31%; bucket-1 (insns) NEGATIVE (gzippy 0.92×, NOT the loss). Memory excess = DRAM+L3 demand-miss (L1/L2 equal) ⇒ decode working set misses cache more than rapidgzip.
STATIC (objdump; perf-annotate segfaulted): top self-cyc — gzippy SPLIT across 3 fns: decode_huffman_body_resumable 21.6% + bootstrap_with_deflate_block 18.4% (window-absent/marker 2nd decode loop, mov/store-heavy) + emit_backref_ring 10.9%; rapidgzip ONE Block<false>::read 31.3% + applyWindow 6.1%. Hot-loop mix (gzippy huffman 2046 insn vs rapid Block::read 1945): gzippy 1.7× more scalar-ALU (26.6% vs 15.8%) + 93 inline BMI2 (67 shlx/18 shrx/8 bzhi) vs rapidgzip ZERO BMI2 (separate peek2). Bucket-2 = the 3-function split + heavier inline bit-ops.
VERDICT: compute-vs-memstall fork RESOLVES toward MEMORY-STALL-DOMINANT (~70%) but NOT pure — real ~31% instruction-mix/structure component. 
SURPRISE vs banked notes (→advisor): "page-walk faults are SLACK (remove=0% wall)" + "footprint/working-set DEAD both directions" — but those tested page-FAULTS/TLB; this is DRAM/L3 DEMAND-MISS stalls (a DIFFERENT quantity, maybe never cleanly tested). CAVEAT: TMA is DESCRIPTIVE not causal — 70%-memory-bound ≠ 70%-removable-wall; need a CAUSAL oracle. KEY coupling: at T8 consumer waits on FRONTIER chunk's decode (RATE, established) — IS that frontier decode the DRAM/L3-stalled region (⇒ on critical path) or is the memory-stall overlapped worker slack? = the next scoped measurement.

## 2026-06-02 — advisor ad38315c: 70%-memory-bound is UN-REFUTED but DESCRIPTIVE; decisive probe = T1-vs-T8 TMA delta (critical-path vs contention-slack).
RECONCILE: demand-miss cache-residency of the DECODE working set is GENUINELY NEW/un-refuted — 3 conflated "memory" things: (A) page-walk faults/TLB = dead (removed faulting cost, NOT residency); (B) chunk-size footprint sweep = dead (changes parallelism granularity, NOT the per-symbol decode working set = fixed Huffman tables + 128KB ring); (C) clean-import 5.3× fewer LLC-misses = measured the window-PRESENT path that BYPASSES the bootstrap marker loop (the 18.4% 2nd decode loop + u16 ring that dominate window-absent T8). Not a contradiction; not the same dead thing.
BUT (the trap): run-level T8 TMA is AGGREGATE over 8 workers — CANNOT separate frontier-critical-path memory-bound from overlapped worker-contention slack (= the copies-wall-neutral / faults-slack trap). 70%-memory-bound ≠ 70%-removable-wall. TMA is descriptive.
DECISIVE CHEAP PROBE (next): T1-forced-parallel TMA (no worker overlap ⇒ every stall on critical path) vs the banked T8. T1≈T8 memory-bound% ⇒ per-chunk decode INTRINSICALLY DRAM-stalled = REAL frontier lever ⇒ proceed. T1≪T8 ⇒ shared-LLC/bandwidth CONTENTION among workers = SLACK (faults trap) ⇒ STOP.
CAUSAL ORACLE (if T1≈T8): memory-latency AMPLIFICATION scoped to the frontier decode hot loop (GZIPPY_SLOW_TABLE: clflush litlen table / pointer-chase), positive-controlled (T1 microbench per-symbol slowdown) + frequency-neutral; amplify-first-confirm-gating then removal-oracle-to-bound. slope≠ceiling.
BUCKET-2 (31%) verdict: mostly INTRINSIC — bootstrap_with_deflate_block 18.4% IS the window-absent marker path, rapidgzip has the IDENTICAL 128KB u16 ring ⇒ the 3-fn split is faithful-port-intrinsic, NOT excess; consolidation = risky surgery, NOT next. BMI2 diff = red herring (core-bound only +2.3pp). The REAL in-scope lever OVERLAPS bucket-3: gzippy's flat 8KB u32 LUT (2048×4B) vs rapidgzip HuffmanCodingISAL 12-bit two-level dense ⇒ plausibly THE oversized decode working set = the +2.3pp DRAM. bucket-2-table-layout and bucket-3-memory = SAME root cause. Confirm via the T1 probe + amplification BEFORE building.
NEXT SUBAGENT: T1-forced-parallel TMA mem sub-split (DRAM/L3/L2/L1) both tools, same binary/workload/host as T8, N>=6 interleaved sha-verified; report {gzippy,rapidgzip}×{T1,T8} memory-bound% + the T1-vs-T8 verdict (frontier-intrinsic vs contention-slack). DO NOT build the table-layout change until this + amplification confirm gating.

## 2026-06-02 — T1-vs-T8 DISCRIMINATOR (agent a8be823f): REFRAMES the gap. Near-parity at T1; the gap is a T8 IPC-COLLAPSE from gzippy-specific DRAM-bandwidth contention. Agent says SLACK/STOP — but that's an INFERENCE not a perturbation → advisor.
NUMBERS (N=7 interleaved sha-verified, paranoid=1→restored 4, cpu_core pins T1=cpu0 T8=0,2,4,6,8,10,12,14):
  T1: gzippy 1.475B cyc / IPC 2.05 / Mem 13.8% (DRAM 3.4) / BadSpec 28.9% / L1 11.7;  rapidgzip 1.406B / IPC 2.05 / Mem 17.4% / BadSpec 19.6%. ⇒ gzippy 1.05× cyc = NEAR PARITY at T1, EQUAL IPC.
  T8: gzippy 3.187B / IPC 1.55 / Mem 27.8% (DRAM 11.9, bandwidth 19.3%) / BadSpec 22.4%;  rapidgzip 2.688B / IPC 1.99 / Mem 19.6%. ⇒ gzippy 1.19× cyc.
KEY: gzippy IPC COLLAPSES 2.05→1.55 (−24%) T1→T8; rapidgzip HOLDS 2.05→1.99 (−3%). gzippy mem-bound +14.0pp (bandwidth 6.4→19.3 = 3×) vs rapidgzip +2.2pp. The gap is NOT per-chunk decode (parity at T1) — it's a T8 SCALING collapse from gzippy-SPECIFIC DRAM-bandwidth contention.
AGENT VERDICT = SLACK/STOP (T1≪T8 mem-bound + rapidgzip-doesn't-grow + bandwidth-saturation-signature = worker thrash not single-stream latency; "faults trap in new clothes"). 
MY CONCERN (→advisor): the SLACK call is an INFERENCE, NOT a causal perturbation (no removal/amplification oracle run). Counter-hypothesis: the consumer waits on the FRONTIER chunk decode; at T8 the frontier worker's IPC collapses TOO under shared-bandwidth contention; if gzippy SPECIFICALLY generates 3× bandwidth (u16 marker buffer = 2× bytes × 8 workers), that contention slows the FRONTIER decode ⇒ ON the critical path, a REAL gzippy-specific lever (reduce per-worker DRAM traffic), NOT slack. "Slack" requires remove-doesn't-move-wall, untested.
T1 ALSO shows a real per-chunk bucket-2: gzippy BadSpec 28.9% vs rapidgzip 19.6% (branch-mispredict, on critical path at T1) — but gzippy still ~parity at T1, so it nets small.
NEXT (advisor to confirm): the causal test for slack-vs-lever — cheapest = per-chunk decode-span duration T1 vs T8 (same chunk slower at T8 ⇒ contention gates frontier ⇒ lever; same ⇒ slack), OR a bandwidth removal/amplification oracle. And: is gzippy's 3× bandwidth (u16 2× buffer) the gzippy-specific reducible cause?

## 2026-06-02 — advisor a0dabed7 OVERTURNS the STOP: it was an attribution-inference, not a perturbation. Real question UNDECIDED → run the causal test.
WHY STOP UNSOUND: the subagent inferred slack from "T1 gzippy less memory-bound than rapidgzip" — but T1 (no cross-worker contention) is SILENT on whether T8 contention slows the FRONTIER decode. T1-absence-of-contention ≠ T8-contention-harmless. Classic attribution trap, pointed the other way.
PRECISE slack-vs-critical CONDITION: CRITICAL if the frontier chunk's decode costs MORE cycles-per-decoded-byte under T8 contention than at T1 (contention steals DRAM cycles from the one worker the consumer blocks on); SLACK if identical (frontier decode compute/L1-bound, BW growth consumed by non-frontier workers with headroom). The discriminator is NOT the T1-vs-T8 mem-bound delta (silent) — it's the frontier decode's cycles/byte.
DECISIVE CHEAP TEST (next subagent): per-chunk worker.block_body (gzip_chunk.rs:1447, span EXISTS) cycles-per-decoded-byte, gzippy T1 vs T8, frozen. cycles (not wall) = freq-proof. Positive control: inject spin into block_body → span grows proportionally. + rapidgzip control (does ITS per-chunk decode slow T1→T8?). RISES at T8 ⇒ frontier decode BW-throttled = LEVER; identical ⇒ slack, STOP correct.
THE LIKELY LEVER (if confirmed) — a FAITHFUL-PORT GAP: rapidgzip has `usedWindowSymbols` (ChunkData.hpp:341-350) — applyWindow SKIPS marker-replacement for window symbols NEVER referenced ⇒ streams far fewer u16 cells through DRAM in the exact resolve path the consumer waits on. gzippy's window_map.rs LACKS this. NOT the u16 width (shared ring). This is the gzippy-specific excess-bytes-streamed source. Confirm via a per-worker BYTES-STREAMED counter (new Fulcrum param) AFTER the cycles/byte test says lever.
RECONCILES RSS-PORT-NEUTRAL: RSS = RESIDENT bytes; lever = STREAMED bytes. In-place port cut resident not streamed ⇒ neutral is consistent, not a refutation. Right lever = reduce bytes-streamed-per-decoded-byte (sparse resolve), not buffer reuse.
FRAME: gzippy PARITY at T1 (1.05×, IPC 2.05=2.05) ⇒ NOT compute-hungry; entire ~1.2× T8 gap = IPC-collapse-under-contention = a CROSS-WORKER BANDWIDTH-EFFICIENCY/scaling lever, genuinely NEW (every prior lever was per-chunk single-stream). NOT a hardware wall UNLESS gzippy's bytes/decoded-byte already matches rapidgzip (the sparse-resolve gap says it does NOT). ORDER: (1) cycles/byte frontier T1-vs-T8 [confirm throttled]; (2) bytes-streamed counter [confirm gzippy>rapidgzip]; (3) port usedWindowSymbols sparse-resolve [measure T8 wall].

## 2026-06-02 — CAUSAL TEST (agent a124b34f): inner-decode bandwidth-throttle REFUTED; real T>1 tax = gzippy-specific u16 MARKER-BOOTSTRAP machinery (31% of T8 cyc), a PATH SWITCH not contention.
INNER DECODE NOT BW-THROTTLED: decode_huffman_body_resumable cycles/byte T1 5.8 → T8 3.7 = 0.64× (DROPS). Bandwidth-throttle-of-inner-decode premise REFUTED. The earlier "1.51× rise" was a 3-function-sum artifact.
REAL FINDING (path switch, perf-validated, N=6 interleaved sha-verified, cycles freq-proof): gzippy total cyc 1.48B(T1)→3.6B(T8)=2.4×. T1 profile = 84% decode_huffman (CLEAN sequential-window bulk), ~0% bootstrap/emit_backref. T8 profile = 23% decode_huffman + 18% bootstrap_with_deflate_block + 13% emit_backref_ring + 9.5% memmove + 5.5% page-faults. T4≈T8 ⇒ T1→T>1 PATH SWITCH (clean vs speculative window-absent u16-marker bootstrap), NOT a bandwidth curve.
GZIPPY-SPECIFIC: rapidgzip total T8 CPU = 1.9× T1 (vs gzippy 2.4×) — rapidgzip does NOT pay the marker-bootstrap tax at T>1. The 31% (bootstrap 18% + emit_backref 13%) is the gzippy-specific T>1 cost. Positive control: GZIPPY_SLOW_DECODE_US grew cycles monotonically (instrument valid).
SURPRISE / TENSION (→advisor): partially contradicts CLAUDE.md "rapidgzip carries the SAME u16 marker machinery (31.25% replaced-marker, 0.113s apply-window)". If both use markers, why gzippy 2.4× vs rapidgzip 1.9×? Likely reconcile = rapidgzip's sparse usedWindowSymbols resolve (ChunkData.hpp:341, skips un-referenced symbols) is CHEAPER, OR rapidgzip decodes more chunks CLEAN (window propagation). This now reads as BUCKET-2 (different/MORE instructions in the marker path), NOT the bucket-3 memory story — and the advisor's prior "bucket-2 is faithful-intrinsic" may be WRONG (gzippy does MORE marker work, not the same).
OPEN (recurring slack-vs-critical): the 31% marker machinery runs on WORKERS. Consumer waits on the FRONTIER chunk decode. Is the marker-bootstrap ON the frontier critical path (frontier chunk is window-absent → uses bootstrap) or overlapped worker slack? Need the causal oracle BEFORE building the sparse-window port. NEXT: advisor on (1) reconcile gzippy-2.4× vs rapidgzip-1.9× marker tax vs "same machinery"; (2) is 31% marker work on frontier critical path; (3) the causal removal/amplification oracle for the marker machinery before the usedWindowSymbols build; (4) is the lever sparse-resolve OR decode-more-clean (window propagation).

## 2026-06-02 — advisor aa9583c6 (code-read): SPARSE-RESOLVE port REFUTED; candidate = emit_backref_ring OVER-MARKING; gating unconfirmed → amplify + marker-fraction in one pass.
CORRECTS PRIOR ADVISOR: usedWindowSymbols (ChunkData.hpp:341-350) ZEROES unused window symbols for window STORAGE COMPRESSIBILITY (its comment: "Set unused symbols to 0 to increase compressibility"), NOT fewer marker resolutions. rapidgzip replaceMarkerBytes (MarkerReplacement.hpp:49) = full-buffer transform, NO sparse skip. ⇒ the "sparse-resolve" lever premise is FALSE — DO NOT build it. (Hold intuition lightly even for advisors — read the code; the new advisor did.)
INTRINSIC vs EXCESS line: INTRINSIC (keep) = the u16 marker RING + apply_window/replace_markers (gzippy's faithful analog in apply_window.rs/replace_markers.rs, == rapidgzip applyWindow/replaceMarkerBytes) + cross-chunk-boundary refs becoming markers. EXCESS (candidate lever) = gzippy emit_backref_ring (deflate_block.rs:1857, 5 call sites in window-absent decode) possibly OVER-MARKING in-chunk back-refs: a ref whose SOURCE ≥ chunk_start is already-decoded in THIS chunk and could resolve INLINE as bytes, but gzippy may emit it as a u16 marker (whole chunk flagged CONTAINS_MARKERS). rapidgzip resolves in-chunk refs as byte copies, only cross-chunk-start → markers. Mechanism (iii) over-marking is the leading hypothesis; (ii) = same marker-fraction but higher cycles/marker-byte (representation cost).
GATING UNCONFIRMED (recurring slack-vs-critical): the 31% marker machinery runs on WORKERS; must confirm it's on the FRONTIER critical path before building. 
NEXT SUBAGENT (one T8 pass = gating + mechanism, NUMBERS not build): (1) GATING = GZIPPY_SLOW amplification scoped ONLY to emit_backref_ring + window-absent bootstrap_with_deflate_block, interleaved T8 wall + freq-neutral sleep control — flat ⇒ SLACK ⇒ STOP; proportional ⇒ on critical path ⇒ build. (2) MECHANISM = gzippy per-window-absent-chunk counter (back-ref bytes emitted as MARKERS vs RESOLVED-inline, split on source≥chunk_start) → the IN-CHUNK-MARKED fraction (high ⇒ over-marking = mechanism iii, lever = inline in-chunk resolution); + rapidgzip markerCount/dataSize control (ChunkData.hpp:306). gzippy-internal in-chunk-marked fraction is decisive even without rapidgzip.

## 2026-06-02 — GATING + MECHANISM (agent a5f4481f): marker machinery ON critical path; over-marking REFUTED; lever = the SHARED window-absent machinery is SLOWER per byte in gzippy. (branch marker-gating-mechanism, counters/knobs only, byte-identical)
GATING = ON CRITICAL PATH (frozen N=9 T8, file/shm, sha-verified): inject into emit_backref_ring+window-absent-bootstrap → wall +0.67s(small)/+0.87s(large), MONOTONIC. Matched-CPU CLEAN-path control moves wall 13×(small)/3×(large) LESS ⇒ marker-SPECIFIC not "any worker CPU". Positive control: knob bit (14.7M emit_backref calls × K). Freq-neutral: frozen turbo-off (sleep-variant unusable: thread::sleep 50µs floor × 14.7M = 140s — broken control caught+discarded). CAVEAT: per-call busy-spin OVERSTATES the slope (defeats ILP/prefetch) ⇒ gating proven, removable CEILING not bounded (need a removal oracle for the ceiling).
MECHANISM = NO OVER-MARKING (refuted 3 ways): (1) in-chunk-marked fraction 99.84% BUT those in-chunk sources are THEMSELVES markers (window-absent ~32KiB marker prefix per chunk until clean zone fills) — NOT inline-resolvable as bytes; (2) source-read: rapidgzip resolveBackreference (deflate.hpp:1346-1410) does the IDENTICAL memcpy+backward-scan, no inline byte-resolution; gzippy emit_backref_ring is a byte-for-byte port of deflate.hpp:1282-1289; (3) rapidgzip control marker fraction 31.46% (158M/503M) vs gzippy 25.5% ⇒ gzippy marks FEWER. ⇒ inline-resolution lever (mechanism iii) DOES NOT EXIST. STOP on iii.
UNIFIED PICTURE (reconciles the whole chain): T1 = clean decode, near-parity, low mem-bound, marker machinery ~0%. T8 = window-absent marker machinery 31% cyc, ON critical path (gating), memory-heavy (u16 stores + backward scan + memmove 9.5% + faults 5.5% = the TMA DRAM/L3-bound region). So bucket-3-memory-bound AND the marker machinery are the SAME region. PARADOX = the LEVER: gzippy marks FEWER (25.5%) yet pays MORE T>1 CPU (2.4× vs rapidgzip 1.9×) ⇒ gzippy's per-marker-byte window-absent machinery is SLOWER than rapidgzip's. The lever = speed up the SHARED window-absent decode/emit/resolve primitive (NOT remove excess markers [none], NOT the clean inner loop [parity at T1, multi-symbol already flat]). emit_backref_ring's "4-7 dependent single-u16 stores + marker backward-scan" per back-ref is the suspect per-byte cost.
NEXT (→advisor): (1) confirm the unified picture + the lever framing; (2) characterize gzippy-vs-rapidgzip CYCLES-PER-MARKER-BYTE in the window-absent path + WHAT makes gzippy's slower (dependent u16 stores? the distance_to_last_marker_byte backward scan? memmove? bootstrap structure?); (3) a REMOVAL oracle to bound the ceiling (amplification overstated); (4) is optimizing the window-absent marker primitive in-scope + un-refuted (distinct from the clean multi-symbol loop).

## 2026-06-02 — advisor ac04a425 (code-read): primitive-rewrite REFUTED; metric corrected; fork=B (architecture: window-absent PREFIX length, not slow primitive). Next = garbage-skip CEILING oracle + prefix comparison.
PRIMITIVE REWRITE REFUTED: emit_backref_ring+marker-scan (deflate_block.rs:1857-2007) is BYTE-FOR-BYTE identical to vendor resolveBackreference (deflate.hpp:1349-1422), short-circuit included ⇒ zero per-byte headroom; the "gzippy primitive is slower" diagnosis is FALSE. DO NOT rewrite the emit.
METRIC CORRECTED: the slow path decodes EVERY window-absent byte → one u16 (literals incl.), so markers ⊊ window-absent bytes. Right denominator = cycles-per-WINDOW-ABSENT-DECODED-byte + absent-byte COUNT, NOT per-marker-byte. The 25.5% vs 31.46% is a fraction of window-absent OUTPUT, not total.
PARADOX DISSOLVED (candidate b): fewer markers + MORE CPU = gzippy pushes MORE bytes through the window-absent path (LONGER speculative prefix), independent of marker fraction. No slow primitive needed.
FORK = B (ARCHITECTURE, faithful-port territory, NOT primitive-rewrite scope): gzippy spends more TIME window-absent — longer per-chunk prefix / slower window propagation. gzippy does NOT port vendor window-sparsity getUsedWindowSymbols (GzipChunk.hpp:60-133, deflate.hpp:1846) — but check if active on rapidgzip's plain -d (gated m_keepIndex && m_windowSparsity). Likely also connects to the DISABLED eager resolve-ahead (audit #11, queuePrefetchedChunkPostProcessing) → more chunks decode CLEAN in rapidgzip → shorter marker prefix. on-path verdict SURVIVES (13:1 specificity > spin caveat).
NEXT (one pass, NUMBERS, no lever build): (1) GARBAGE-SKIP CEILING ORACLE — drive window-absent decode+emit+resolve cost → ~0 (correctness-BREAKING, wall-only, NO sha-verify), measure T8 wall ceiling, frequency-neutral + SLEEP control. = the MAX any window-absent work could buy. ceiling < spread ⇒ direction DEAD, STOP. This ONE number gates the whole campaign. (2) WINDOW-ABSENT-BYTE-COUNT + per-chunk PREFIX LENGTH, gzippy vs rapidgzip — settles A (primitive, unlikely) vs B (architecture: gzippy more absent bytes/longer prefix). gzippy: sum bytes_decoded from worker.bootstrap.outcome trace; rapidgzip: instrument dataWithMarkers / m_decodedBytes-while-window-absent.

## 2026-06-02 — CEILING ORACLE + A-vs-B (agent ae5b9ab2): window-absent direction ALIVE (32.5% ceiling); fork FLIPS to A (slower-per-byte, EQUAL bytes). Most promising lever yet — but the 32.5% is OVER-optimistic. (branch feat/window-absent-skip-oracle @ 20c6eda, knob-OFF byte-identical, 25 tests pass)
CEILING = ALIVE: GZIPPY_SKIP_WINDOW_ABSENT (replaces window-absent u16 decode+emit+resolve with one CLEAN ISA-L bulk decode vs dummy zero window, garbage out, wall-only) — frozen N=11 interleaved T8 /dev/shm: OFF 468ms → ON 316ms = Δ152ms = 32.5% of wall, DISJOINT distributions (min-OFF 445 > max-ON 333), 3.2× spread. Positive control: 43 chunks/201.6MB (95%) skipped, full 211968000B written. 32.5% ≫ the ~17% gap to rapidgzip ⇒ MORE than enough to close it.
A-vs-B = EQUAL ⇒ FORK A (slower-per-byte), NOT B (architecture): gzippy 76.96MB window-absent = 36.31% (16 chunks, prefix median 2.84MB) vs rapidgzip 74.86MB = 35.31% (verified apples-to-apples: rapidgzip markerCount=dataWithMarkersSize ChunkData.hpp:250 == gzippy data_with_markers.len). Ratio 1.03× = EQUAL. REFUTES the fork-B "gzippy longer prefix" prior. ~35% window-absent is STRUCTURAL to 4MiB chunking (each chunk decodes first few MB window-absent until clean 32KiB window arms). Corroboration: rapidgzip verbose — its custom-inflate window-absent decode 1.43s DOMINATES its ISA-L clean 0.59s ⇒ window-absent is the dominant decode in BOTH; gzippy just spends more cycles/byte on it.
CRITICAL CAVEAT (the 32.5% is OVER-OPTIMISTIC): the oracle used ISA-L CLEAN decode for the skip — far faster than ANY marker-emitting decoder. So 152ms = gzippy-pure-Rust-window-absent MINUS ISA-L-clean, which INCLUDES irreducible marker work rapidgzip ALSO pays. REAL achievable ceiling = gzippy-window-absent-per-byte MINUS rapidgzip-window-absent-per-byte (both emit markers) — NOT measured yet. Need that before any rewrite.
SLOW PART = bootstrap_with_deflate_block (NOT emit_backref_ring, which is byte-identical to vendor resolveBackreference): the u16 Huffman decode loop + per-block header/table setup. Inner-decode PRIMITIVE ⇒ AUTHORIZED for reimplementation. Distinct from the clean decode_huffman_body_resumable (parity at T1, multi-symbol already flat).
NEXT (→advisor then measure): (1) cycles-per-window-absent-byte gzippy vs rapidgzip = the REALISTIC ceiling; (2) WHAT in bootstrap_with_deflate_block is slower per byte (per-block table/header re-setup? the u16 decode loop? mode-switch distance_to_last_marker tracking?); (3) confirm in-scope (inner primitive) + un-refuted (≠ the clean multi-symbol loop).

## 2026-06-02 — advisor a9b7d0bf: fork-decider = two cheap read-only counters (A division + B slack-fraction), no build, before anything.
(a) Accept fork A; one cheap hole = literal:backref ratio within window-absent region equal? (else slowness = backref-density not decode-loop).
(b) A-CEILING metric = gzippy:rapidgzip cycles-per-window-absent-byte (removes the ISA-L inflation), frozen-clock + identical byte-def. MATERIALITY BAR ≥~1.5× (can't close ~17% wall from 36% cyc otherwise); <1.15× ⇒ A DEAD ON ARRIVAL. It's a CPU-sum HYPOTHESIS not a verdict (perturbation downstream).
(c) cheapest WHY discriminator = static disasm bootstrap_with_deflate_block inner loop vs rapidgzip Block::read, hunting the RESUMABLE YIELD-CHECK tax (candidate a, highest prior; fix PRE-BLESSED by CLAUDE.md FASTLOOP-margin clause). Deprioritize u16-store (memory: store-reorg futile). Scrutinize table-reuse for scope drift.
(d) B (reduce window-absent FRACTION) = BIGGER prize (only path to BEAT rapidgzip), NOT refuted (placement-TIE ≠ window-availability = category error), in-scope ONLY in the OPPORTUNISTIC weak form w/ a vendor file:line (the "give everyone predecessor window" form = novel + falsified-by-design serialization). B ceiling = UNQUANTIFIED scheduling-slack fraction.
NEXT SUBAGENT (one frozen T8 trace, NUMBERS, no build): (1) A DIVISION gzippy vs rapidgzip cyc-per-window-absent-byte vs the 1.5× bar; (2) B SLACK-FRACTION = fraction of window-absent chunks whose PREDECESSOR real window is published BEFORE the chunk finishes its window-absent prefix (= window-absent bytes convertible-to-clean; near-zero ⇒ B structurally DEAD, >0 ⇒ B probe justified); (3) (a)-hole literal:backref ratio within window-absent region. Both counters decide the fork before any build; if one, run B slack-fraction.

## 2026-06-02 — FORK DECIDER (agent a499df1d): BOTH DEAD + SURPRISE (gzippy window-absent decode is FASTER than rapidgzip). The ~1.18× gap is NOT decode — it's diffuse coordination.
COUNTER 1 (A): gzippy window-absent decode 17.5 cyc/byte vs rapidgzip 27.2 = 0.64× ⇒ gzippy ~35% FASTER per window-absent byte (clean-decode control TIES 1.04×, validates the time-bracket). A is DEAD + the "slower primitive" premise REFUTED — matching rapidgzip would make gzippy SLOWER.
COUNTER 2 (B): slack fraction 0/16 chunks, 0/74.86M bytes. Workers finish ALL window-absent prefixes in ~2.8ms; consumer publishes predecessor windows SERIALLY over 26-117ms — every window arrives AFTER its successor finished its prefix. B STRUCTURALLY DEAD (zero schedulable slack, confirms placement-TIE was right target).
COUNTER 3: gzippy window-absent mix = 71.1% literals / 28.9% backref-markers (rapidgzip mix not extractable). Not a deficit (gzippy already cheaper per WA-byte).
RECONCILE THE 32.5% CEILING: the oracle skipped window-absent → ISA-L CLEAN (faster than ANY marker-emitting decoder), so 32.5% = the SHARED IRREDUCIBLE cost of speculative window-absent decode that BOTH tools pay, NOT gzippy excess. The ceiling MISLED (measured the cost of speculation-itself, which rapidgzip has too).
THE REFRAME: gzippy DECODE is at-or-better than rapidgzip on BOTH paths (window-absent 0.64×, clean 1.04×). The 1.18× T8 gap is ENTIRELY NON-DECODE = coordination/copy/fault/consumer-serial overhead. perf self% (CPU-sum, not wall): gzippy __memmove_avx 7.3% + page_fault 5.4% + ~10% kernel memcg/page-alloc ≈ 20% vs rapidgzip ~6%. BUT prior PERTURBATION established faults=SLACK + copies=wall-neutral. New detail from COUNTER 2: consumer publishes windows SERIALLY 26-117ms while workers finish in 2.8ms = the consumer-serial-publish chain.
→ ADVISOR: (1) accept both-forks-dead + gzippy-faster-decode; (2) is the honest conclusion "x86 T8 gap = diffuse coordination overhead, decode is competitive, the big CPU regions (faults/copies) are established-slack" = the fully-mapped STOP; (3) OR is the consumer-serial-publish chain (26-117ms serial vs 2.8ms workers) a remaining attackable lever (vs the refuted eager_postprocess +195ms)?

## 2026-06-02 — advisor a66b9e02: accept both-forks-dead + gzippy-decode-competitive; worker-offload gap DOES NOT EXIST (code-read); ONE final perturbation earns the STOP.
ACCEPT (no hole): gzippy window-absent decode 0.64× (faster), clean ties 1.04× (validates bracket); B slack 0/16. Decode is competitive-or-better; not the gap.
WORKER-OFFLOAD GAP REFUTED by code-read: BOTH rapidgzip (GzipChunkFetcher.hpp:579 submitTaskWithHighPriority) and gzippy (chunk_fetcher.rs:1604 submit_post_process_to_pool) submit applyWindow/apply_window to the WORKER POOL; consumer only does the inherent last-window build on its thread (both keep last window UNCOMPRESSED, vendor:561). NO resolve-on-consumer-vs-worker gap. Don't retread eager_postprocess (+195ms, moved worker resolve ONTO consumer — different+wrong).
"DIFFUSE STOP" NOT YET EARNED: one un-perturbed region = the consumer's OWN serial per-chunk publish work (materialize_window chunk_fetcher.rs:1482 + get_last_window_vec chunk_data.rs:1179 + insert_owned, the 26-117ms serial chain). Measured by ATTRIBUTION only — doctrine forbids "slack" verdict without perturbation. Distinct from RATE-100% frontier-decode-wait (that=waiting on workers; this=consumer CPU BETWEEN waits). True serial dep (window N needs N-1 materialized).
FINAL PERTURBATION (then STOP or localize): add GZIPPY_SLOW_PUBLISH=N knob spinning N% of the consumer.window_publish_clean + window_publish_marker + materialize_window SELF-time (EXCLUDE wait_replaced_markers span), per chunk on consumer; frozen T8 interleaved N>=7 + freq-neutral SLEEP control + positive control (knob bites). FLAT ⇒ consumer publish = overlapped slack ⇒ EARNED diffuse-coordination STOP (STRONG positive: gzippy decode competitive, residual = irreducible parallel-coordination tax). PROPORTIONAL ⇒ gates wall ⇒ in-boundary lever = reduce per-chunk publish cost (e.g. skip the 32KiB last-window rebuild when tail already clean), NOT offload (vendor does same on same thread).

## 2026-06-02 — FINAL PERTURBATION (agent a4fa0308): consumer-publish ON critical path but EMPTY (slope≠ceiling). EARNED diffuse-coordination STOP. (branch perturb/slow-publish f5e5910, knob-OFF byte-identical sha=ref)
RESULT (frozen T8, N=13 interleaved sha-verified, GZIPPY_SLOW_PUBLISH scoped to materialize_window+get_last_window_vec+insert_owned, EXCL wait/write/CRC): OFF 0.9384s; +97ms on-consumer busy-spin → 1.0163s (+78ms); SLEEP variant +96ms (freq-neutral confirms); SAME +97ms CPU on WORKERS → 0.9455s (+7ms = TIE). ⇒ PROPORTIONAL + consumer-SPECIFIC ⇒ the in-order consumer publish chain GATES the wall. Positive control: injected counter 0/1046/4004µs proportional.
SLOPE≠CEILING (decisive): the region's NATURAL un-injected self-time = ~1.2ms for the whole 503MB decode (measured 3 ways via =100% counter). So injecting adds wall 1:1 (on path) but REMOVING caps at ~1.2ms of a ~90ms full-clock wall = NEGLIGIBLE vs the ~1.18× gap. On the critical path yet NOT a speed-up lever — almost nothing to remove. The proposed skip-32KiB-rebuild fix saves ≤1.2ms. ⇒ EARNED STOP.
=== CAMPAIGN VERDICT (x86 T8 single-member decode gap, FULLY MAPPED) ===
gzippy is at PARITY at T1 and its DECODE is competitive-or-BETTER than rapidgzip on both paths (window-absent 0.64× faster, clean 1.04× tie). The ~1.18× T8 gap is ENTIRELY non-decode and has NO single recoverable lever — every region tested: decode=competitive; window-absent fraction=structurally fixed (0/16 slack); page-faults=overlapped slack (3 oracles=0% wall); copies=wall-neutral; worker-offload=no gap (both submit applyWindow to pool); consumer-publish=on-path-but-~1.2ms-empty; the 32.5% ceiling=SHARED irreducible speculation cost both tools pay. The residual ~1.18× = DIFFUSE parallel-coordination/memory-traffic tax (gzippy's ~20% memmove+fault+kernel-mm vs rapidgzip's ~6%, distributed across 8 workers, each piece individually overlapped-slack, collectively the IPC-collapse 2.05→1.55) — recoverable only by reducing gzippy's TOTAL cross-worker memory footprint simultaneously (data-plane port tried: RSS-3% neutral; allocator levers falsified). STRONG POSITIVE RESULT: the instrument PROVED decode is competitive and located the residual as diffuse coordination, not a hidden lever. → advisor to confirm the STOP is earned (not a 3rd "diffuse" cop-out).
