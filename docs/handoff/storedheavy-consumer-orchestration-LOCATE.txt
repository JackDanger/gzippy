╔═══════════════════════════════════════════════════════════════════════════════════╗
║ TOOL VERDICT (2026-07-09, supersedes ALL hand-analysis below) — the committed,     ║
║ self-validating instrument replaced the 7 throwaway hand-atomic cycles.            ║
║                                                                                     ║
║ INSTRUMENT: gzippy `--features phase-timing` (commits 196eca29+38efa665, OFF by    ║
║ default, byte-exact prod) emits a conserved phase record; `fulcrum phasebreak`      ║
║ (fulcrum @ 2e8b1ee) analyzes host-side with Gate-0 CONSERVATION (wall = waits+cpu   ║
║ +overlap, residual ≤ max(500us,12%)) + inert-oracle trap. It REFUSES a breakdown    ║
║ that does not conserve — so these numbers are gated, not narrated.                  ║
║                                                                                     ║
║ AMD storedheavy T4, N=7, Gate-0 PASS:                                               ║
║   consumer_wall 35.7ms | decode_wait 30.9ms (87%!) | consumer_cpu 2.1ms |           ║
║   drain 0.15ms | future_recv 0ms | blockfind 0ms → dominant = DECODE_WAIT.          ║
║                                                                                     ║
║ VERDICT: storedheavy is DECODE-THROUGHPUT-BOUND — the consumer spends ~87% of the   ║
║ wall WAITING for workers to decode chunks (decode_wait now includes                 ║
║ try_take_prefetched_pumping, the ~97%-of-wall wait my hand-cycles MISSED — found    ║
║ by the tool's conservation gate). The lever is WORKER per-thread DECODE THROUGHPUT  ║
║ (the inner Huffman/stored decode loop — CLAUDE.md-OPEN territory), NOT consumer     ║
║ orchestration and NOT marker-resolution.                                            ║
║                                                                                     ║
║ FALSIFIED BY THE TOOL (both were my hand-attributed pins below):                    ║
║  - "consumer per-chunk orchestration is the cost" — NO: consumer_cpu is 2.1ms.      ║
║  - "marker-resolution / future_recv pipeline (~21ms) is the deficit" — NO:          ║
║    future_recv≈0 on storedheavy (it is stored, not marker-heavy; the inert-oracle   ║
║    trap fires confirming ~no marker resolution). My 21ms→future_recv attribution    ║
║    (via cursor-agent code-reasoning) was WRONG; the deterministic instrument shows  ║
║    that 21ms was actually the uninstrumented try_take_prefetched_pumping DECODE     ║
║    wait, now folded into decode_wait.                                               ║
║                                                                                     ║
║ STILL OWED (tool is honest about it): T8/T16 REFUSE — a ~5ms region grows with T    ║
║ beyond waits+cpu (an uninstrumented consumer wait at high chunk-count). Next tool   ║
║ iteration: add that span to phase_timing until T8/T16 conserve, THEN read them.     ║
║ Then Gate-2 the worker-decode-throughput lever (perturb worker decode, watch wall). ║
╚═══════════════════════════════════════════════════════════════════════════════════╝

storedheavy AMD-Zen2 deficit — FINER CONSUMER-LOOP PHASE-TIMING (measured, 2026-07-09)
=======================================================================================
NOTE: everything below this line is the HAND-ANALYSIS JOURNEY that the TOOL VERDICT
above supersedes. It is kept as a cautionary record of ~5 successive hand-attributed
pins (consumer orchestration, clone, marker-resolution, ...) each overturned by the
next measurement — the exact bias pattern that motivated building the instrument.
Read the box above for the gated finding; read below only for method/history.

WHY THIS FILE: the prior handoff (leverD-overlap-FALSIFIED.txt) pinned the storedheavy
T4/8/16 deficit to marker_inflate.rs:1424 (per-block window-absent stored-flip decode)
by READING THE SOURCE. That was an unvalidated inference. This file is the DETERMINISTIC
measurement that was owed — and it FALSIFIES that pin.

INSTRUMENT: throwaway phase-timing added to chunk_fetcher.rs drive_impl + consumer_loop
+ drain_one_pending (scripts saved at $CLAUDE_JOB_DIR/tmp/phz{2,3,4}_instrument.py):
  - drive_impl checkpoints:  setup (pool_size->consumer_loop call), atconsumer
    (->thread_pool.stop), atfinalize (->block_finder.finalize).
  - consumer_loop cumulative WAIT accumulators (atomic-ns):
      decode_wait   = time stalled in block_fetcher.get_with_prefetch (waiting for a
                      chunk to finish DECODING on a worker)
      blockfind_wait= time stalled in block_finder.get (waiting for the next boundary)
      drain         = RAII total time in drain_one_pending (apply_window rx.recv + write)
  - box: AMD 10.0.2.240 (Zen2), clean tip dfa67ccc, release --features pure-rust-inflate,
    corpus /root/archive/storedheavy.gz (100,016,878 B compressed, ratio ~0.99),
    GZIPPY_FORCE_PARALLEL_SM=1, sink /dev/null. Wall via `date +%s.%N` (noisy ±5ms; the
    INTERNAL distribution is the robust signal, not the absolute wall — fulcrum score
    0.92/0.96/0.97 remains the frozen ratio authority).

MEASURED DECOMPOSITION (3 runs/T; representative):
  region                         T4        T8        T16      verdict
  ------                         --        --        ---      -------
  setup (block-find dispatch)    ~20us     ~20us     ~20us    negligible (async)
  decode_wait                    ~6.5ms    ~5.4ms    ~3.6ms   small, SHRINKS with T -> workers keep up
  drain (applyWin recv + write)  ~0.22ms   ~0.25ms   ~2.1ms   NEGLIGIBLE
  blockfind_wait                 ~9us      ~10us     ~17us    negligible
  REMAINDER (consumer orch.)     ~24ms     ~14ms     ~15ms    DOMINANT controllable serial cost
  finalize (atfinalize-atcons)   ~2ms      ~4ms      ~3ms     small
  flat external (wall-atfinal)   ~21ms     ~22ms     ~20ms    invariant to T; BOTH tools pay
  ------                         --        --        ---
  wall (noisy)                   ~54ms     ~47ms     ~44ms

  REMAINDER := atconsumer - decode_wait - drain - blockfind. It is the consumer/main
  thread's per-CHUNK orchestration between block_finder.get and drain: Arc::try_unwrap
  hold-juggling, harvest_ready_postprocess + process_ready_prefetches bookkeeping run
  EVERY iteration, ConsumerChunkHold construction/clone, window publish, post-process
  submission. (Includes any consumer-thread deschedule while workers saturate cores.)

WHAT THIS FALSIFIES / RULES OUT (measured, not reasoned):
  - The decode kernel (marker_inflate.rs:1424 stored-flip / careful loop) is NOT the
    wall lever: decode_wait is small AND shrinks with T -> the worker pool keeps up with
    the consumer. A deep inner-loop rewrite there is the WRONG target. (Prior source-read
    pin RETRACTED.)
  - apply_window / marker-resolution / write are NOT the lever: drain is 0.2-2ms.
  - Block-finding is NOT the lever: blockfind_wait ~= 0 (fully async, branchless scan).

WHERE THE CONTROLLABLE COST ACTUALLY IS:
  1. Consumer-thread per-chunk ORCHESTRATION (~14-24ms) — the largest movable region.
  2. Flat ~20ms process/mmap/IO/teardown overhead (both tools pay; low leverage).
  gz wins T1 and loses T4/8/16 by a few % (fulcrum 0.92/0.96/0.97). The gap to close is
  small (~1-2ms of a ~44ms wall) and sits INSIDE region 1 — plenty of headroom there
  (halving the ~15ms orchestration would over-close it), IF it is on the critical path.

NEXT DISCIPLINED STEP (Gate-2 owed BEFORE any lever — do not skip again):
  This locate is ATTRIBUTION (a self-consistent time-split), still a HYPOTHESIS about the
  cause. Confirm the consumer orchestration is on the CRITICAL PATH by PERTURBATION:
  inject a known per-iteration delay in the consumer loop body (a core-yielding sleep,
  frequency-neutral) and measure the interleaved wall response — proportional => on the
  path; flat => slack. ONLY if proportional, build the lever: cut per-chunk consumer
  bookkeeping (e.g. hoist/skip harvest+process-prefetch when the pending queue is short;
  avoid the Arc::try_unwrap clone path; batch window publish). Gate on fulcrum score
  (storedheavy T4/8/16 >= 0.99, NO win-cell regression, byte-exact, BOTH arches).
  rapidgzip counterpart to study for a faithful-convergence version: how rg's
  GzipChunkFetcher::processNextChunk keeps consumer per-chunk work off the critical path.

FOLLOW-UP (same session, 2026-07-09) — Gate-2 perturbation + Arc-ownership counters:
  - ITERATION COUNT is TINY: the consumer processes only iters=34 (T4) / 40 (T8) /
    192 (T16) CHUNKS, not ~3000 blocks. So the ~15-24ms remainder is a PER-CHUNK cost
    of ~hundreds of us/chunk over a HANDFUL of large chunks (~3MB/chunk at T4,
    ~520KB/chunk at T16), and it SCALES WITH CHUNK BYTES (~680us/3MB -> ~68us/520KB
    ~= a per-byte op at ~5 GB/s). It is NOT x3000 per-block bookkeeping.
  - CLONE HYPOTHESIS FALSIFIED by counters: arc_misses=0 at every T (arc_hits ~= iters,
    arc_deferred 0-4). The consumer NEVER clones the multi-MB ChunkData
    (SharedChunkData::take_or_clone always TAKES). So the per-chunk cost is NOT a clone.
  - PERTURBATION (Gate-2, +20us/iter core-yielding sleep): at T16 (192 iters, ~12ms
    clean injection) the wall moved ~+16ms and atconsumer ~+12ms -> ~proportional ->
    consumer orchestration is ON the critical path at T16. At T4/T8 (34-40 iters) the
    injection was too small (~2ms) to resolve against ~5ms wall noise -> INCONCLUSIVE
    there; a bigger per-iter injection is needed to confirm T4/T8.
  - CONTAMINATION CAVEAT: the AMD box runs the user's llama; raw atconsumer/wall are
    NOT load-immune. The per-chunk remainder magnitude may include consumer-thread
    DESCHEDULE (CPU contention), inflating it. The ROBUST results here are the RATIOS
    and COUNTERS (decode_wait small & shrinking; arc_misses=0; drain/blockfind ~0),
    which are contention-insensitive. The exact remainder attribution needs a CLEAN
    (llama-paused, taskset-pinned) re-measure.

  NARROWED BY ELIMINATION (robust): the storedheavy deficit is NOT the decode kernel
  (workers keep up), NOT a per-chunk clone (misses=0), NOT apply_window/write (drain ~0),
  NOT block-finding (~0). What REMAINS is the consumer's per-chunk POST-DECODE
  processing (window application / output materialization over the chunk bytes) and/or
  CPU-contention deschedule. NEXT: clean-box (llama-paused) re-measure to split real
  consumer per-byte work from contention; time the specific post-decode step over the
  chunk; only then a lever + fulcrum-score gate both arches.

DECISIVE (same session, 2026-07-09) — consumer-thread CPU vs WALL (CLOCK_THREAD_CPUTIME_ID):
  box idle (load 0.63, llama Sl/idle -> NOT contention-contaminated).
    T   wall(consumer)  consumer_CPU  decode_wait  drain
    4   ~31ms           ~3.0ms        ~7ms         ~0.2ms
    8   ~20ms           ~3.6ms        ~5ms         ~0.25ms
    16  ~20ms           ~12.6ms       ~3.5ms       ~1.9ms
  The consumer thread uses only ~3ms CPU across a ~31ms consumer phase at T4 -> it is
  WAIT-BOUND, not work-bound. The "~15-24ms remainder" I earlier attributed to consumer
  orchestration is the consumer SLEEPING while workers decode, NOT serial CPU work.
  => "consumer per-chunk post-decode processing is the lever" is FALSIFIED too.

FINAL ELIMINATION MAP (all measured this session, robust to load):
  - decode kernel as a CONSUMER pin:      OUT (decode_wait small, shrinks with T)
  - per-chunk ChunkData clone:            OUT (arc_misses=0)
  - apply_window / marker / write (drain):OUT (~0.2-2ms)
  - block-finding:                        OUT (~0, async)
  - consumer-thread serial CPU work:      OUT (consumer_CPU ~3ms; wait-bound)
  What REMAINS: the wall is gated by the PARALLEL DECODE PIPELINE — worker decode
  THROUGHPUT + coordination latency (the consumer waits on it). Both gz and rg barely
  parallelize incompressible data (rg walls also degrade past T4); gz trails rg ~4%
  (fulcrum 0.92/0.96/0.97). The only in-scope lever left is WORKER-SIDE per-thread
  decode throughput (the CLAUDE.md-OPEN inner Huffman/stored loop) — NOT any consumer
  change. NB this is the WORKER inner loop (throughput), distinct from the CONSUMER
  decode path retracted above.

  NEXT (Gate-2 before any lever): throughput perturbation — slow/speed the WORKER
  decodeBlock inner loop by a known factor and measure the interleaved wall; if the
  wall tracks worker-throughput, a per-worker inner-loop speedup (libdeflate/ISA-L
  technique, arch-specific) is the lever. Gate on fulcrum score storedheavy T4/8/16
  >= 0.99, no win-cell regression, byte-exact, both arches. This is a multi-cycle
  inner-loop campaign, not a quick lever.

PINNED (2026-07-09, cursor-agent Composer + direct code evidence): the ~21ms
consumer-idle gap is the consumer BLOCKING in `recv_post_process_blocking`
(chunk_fetcher.rs:1660 and :1668, the vendor `wait.future_recv` span) — waiting for
each chunk's MARKER-RESOLUTION / apply_window POST-PROCESS future to complete, in
encoded order, while pumping successors. My drain RAII guard (0.2ms) MISSED this
because the wait is in the consumer_loop body, NOT inside drain_one_pending. So the
earlier "apply_window is cheap" read was measuring the wrong place: apply_window runs
in the worker POST-PROCESS and the consumer serially waits for it here. => the
storedheavy deficit IS the marker-resolution pipeline (worker apply_window throughput
+ the in-order serial dependency of window-absent speculative decode), NOT raw decode
and NOT consumer CPU. This is the faithful u16-marker architecture cost; rg does the
same apply_window (~0.113s traced) but overlaps/executes it more efficiently.
  LEVER CLASS (to Gate-2 next): reduce apply_window/marker-resolution cost or improve
  its overlap so the consumer's recv_post_process_blocking wait shrinks. For storedheavy
  most chunk bytes are literals/stored with markers only near chunk starts — apply_window
  may be scanning/copying more than the marker-bearing prefix. Confirm by perturbation,
  then a targeted apply_window optimization; gate on fulcrum score both arches.

TOOLING (user directive 2026-07-09: "build into Fulcrum, make it not guesswork"):
the 7 throwaway phz*.py atomic hacks are being replaced by a committed instrument —
a gzippy compile-time `phase-timing` feature (zero prod-path env knobs; feature OFF by
default, mirrors coz_probe) emitting a structured phase record, consumed + Gate-0
conservation-validated by a `fulcrum phasebreak` subcommand. Its first run DETERMIN-
ISTICALLY confirms future_recv ~= 21ms (this locate becomes tool output, not narration).

STATUS: EXHAUSTIVE deterministic elimination done — every consumer-side candidate
falsified by measurement (decode-wait ratios, arc counters, drain RAII, and the
decisive consumer_CPU<<wall). The storedheavy deficit is worker-decode-pipeline-bound;
the sole remaining in-scope lever is worker inner-loop throughput (CLAUDE.md-open).
Win state (all cells >= 0.99) still UNMET on storedheavy T4/8/16 — but the target is
now fixed by measurement, and all wrong targets are eliminated with evidence.

─── perf LOCATE of the worker decode (2026-07-09, WEAK tier — hypothesis) ───
perf record on AMD storedheavy T4 (debuginfo build, 4187 samples): of the WORKER
decode (which phasebreak gated as 87% of the wall):
  ~41% __memmove_avx_unaligned_erms (byte copy) — of which ~28% (=~11% of decode,
       ~10% of wall) is asm_exc_page_fault → do_anonymous_page → alloc_pages =
       FIRST-TOUCH page-faults on freshly-allocated ANONYMOUS per-chunk decode
       buffers (not the input mmap). Remaining ~13% is the real copy.
  ~59% gzippy decode logic (LTO-inlined, symbols unresolved: hot addr clusters
       0x4c08x + 0x9fa5x) = the Huffman/marker symbol-decode loops.
⚠ SHARE≠WALL TRAP: the page-fault share looks large but Lever C (per-chunk buffer
  warm-reuse at T>1) was ALREADY fulcrum-score-falsified as "no effect" — i.e. that
  share may be SLACK (a faulting worker overlaps other workers' in-order progress).
  BUT Lever C's gate ran during the discredited-measurement period and never verified
  the reuse actually REDUCED faults (could have been "lever didn't fire", not "slack").
  DECISIVE RE-TEST owed: reuse the faulting buffer, use phasebreak/perf to PROVE faults
  drop, then fulcrum-score gate the wall — faults-drop + wall-flat ⇒ slack CONFIRMED
  (bank, don't ship); wall-moves ⇒ Lever C was a false-negative (win). If slack, the
  lever is the ~59% Huffman inner loop (CLAUDE.md-open) — Gate-2 perturb then optimize.

─── GATED FALSIFY: page-fault removal is SLACK (2026-07-09, share≠wall PROVEN) ───
Re-test (worktree, byte-exact, patch: docs/handoff/leverC-refire-SLACK-CONFIRMED.patch):
faulting buffer = SegmentedU8::buf via chunk_buffer_pool::take_u8; manual_buffer_pool_enabled()
was T1-oracle-only, so every T>1 worker fresh-allocs per chunk (= Lever C's target). Enabled
warm reuse at T>1 (current_worker_pool_index().is_some()), no pin (avoids 7065e1e8 regression).
PROVEN the lever FIRED (the step Lever C skipped): perf do_anonymous_page+clear_page share
5.69%→5.28% (non-overlapping over 3 replicates), minor-faults 15.5k→13.7k (2.0-2.7σ).
GATED the wall: fulcrum score storedheavy AMD FLAT at every T (T4 .9425→.945, T8 .9675→.9625,
T16 .9725→.9475; all Δ<spread; N=9×4 sessions). silesia T8 win-cell no regression. Byte-exact
(sha==gzip -dc, silesia+monorepo+storedheavy+synthetic incompressible, T1/4/8/16); 1005 tests.
VERDICT: first-touch fault time is OFF the critical path at T>1 (overlapped by other threads'
Huffman/marker work) → share≠wall CONFIRMED. NOT shipped. The ONLY remaining decode_wait
component is the ~59% Huffman/marker inner-loop COMPUTE — that is the sole surviving lever.

─── ADVISOR (cursor-agent Composer, 2026-07-09) — CORRECTS a skipped causal step ───
"decode_wait dominates (87%)" forces only: the lever is WORKER per-thread decode
THROUGHPUT (not consumer/marker). It does NOT force "Huffman compute" — perf's 59%
bucket COLLAPSES three hypotheses: (H) Huffman/bit-reader compute, (M) u16 store/drain
memory-BANDWIDTH, (S) chunk-granularity/scheduling. "Huffman is the lever" is a
HYPOTHESIS, not gated.
DISCRIMINATOR (Gate-2, owed): two-arm worker perturbation on storedheavy AMD T4 (N≥7,
fulcrum score): Arm H = inject delay in decode_marker_fast_loop AFTER lut decode/consume
BEFORE ring write; Arm M = inject delay in drain_to_output/MarkerSink::push_slice only.
Compare Δwall/Δinject: H≫M → compute-bound; M≫H → u16-bandwidth-bound; both flat despite
decode_wait≈87% → scheduling/granularity (also fix T8/T16 conservation gap).
CODE LEAD (advisor read marker_inflate.rs): decode_marker_fast_loop does a SPECULATIVE
8-byte WIDENED store (packed literals→u16, one 8-byte unaligned write per packet) EVEN
for a single literal; vendor ISA-L appendToWindow does one SCALAR u16 store per literal.
"rg does the same u16 scheme" is architecturally true but MICRO-arch FALSE on the hot
literal path → gz may pay extra Zen2 store/L1 bandwidth.
FIRST TRY (ranked #1, cheap, byte-safe): a `sym_count==1 && literal` fast arm doing ONE
u16 store (skip the widen+8-byte store), matching vendor per-literal pattern. PRECHECK
(advisor disqualifier): measure the sym_count distribution first — if most iterations are
already 2-3 packed literals, #1 won't help; if single-literals dominate (expected for
literal-heavy incompressible data), implement + fulcrum-score gate. Falsifies cheaply.

═══ ORACLE FIXED + TRUE GAP (2026-07-09) — supersedes the stale frozen-cell target ═══
fulcrum score was UNRUNNABLE (3-way, hard-required isal_inflate symbols; ISA-L decode
removed). Fixed to 2-way native-vs-rg (fulcrum main @ 3959149; kept FLAVOR-N, corpus/
decomp SHA pins, comparator native-ELF check, SINK LAW, N>=7 best-of-N; dropped only the
isal arm + FLAVOR-I). Re-scored on AMD Zen2 (FROZEN performance governor, taskset-pinned,
N=11, sha-verified, dist=RESOLVED), gzippy-native @ 9896c2b7 vs rapidgzip-native ELF:
  storedheavy T4 = 0.96 FAIL | T8 = 0.99 PASS | T16 = 0.97 FAIL | silesia T8 = 0.98 FAIL
TRUE gap = 1-4% (NOT the stale 0.92=8% nor abmeasure 18%). The 0.92 cell was src-sha
443a9e6 (27d drift) self-flagged NOISY; abmeasure ran on a contended, unfrozen-governor
box (procs_running 1-4) measuring cycles not wall.
REFRAME: (a) storedheavy T8 already PASSES; (b) silesia T8 (a normal corpus) also FAILs
at 0.98 → the deficit is BROAD and SMALL (~1-4% across cells), NOT storedheavy-specific.
The whole session's storedheavy-decode focus was too narrow.
OWED GATES before trusting the broad-deficit interpretation (per governing law):
  - Gate-0(b) COMPARATOR SELF-TEST: the 2-way score's rg-vs-rg (or a symmetry check) must
    read 1.0 ± spread; a ~2% harness/comparator offset would explain silesia's 0.98 as a
    TIE not a real deficit. NOT yet run in 2-way mode (only a native-vs-native UNIT test).
  - Gate-3 CROSS-ARCH: single AMD box only. Intel replication owed.
If the comparator self-tests to 1.0 and Intel replicates, the real target is a BROAD ~1-4%
AMD deficit (decode-throughput-bound per phasebreak) — attack that, not storedheavy alone.

███ CRITICAL INSTRUMENT BUG (2026-07-09): fulcrum score TIMES A FILE-WRITE ███
score.rs::timed_run (417-445) sinks stdout to a REGULAR FILE and times it — violating
the project /dev/null sink law ([[reference_bench_instrument_gotchas]]: file-sink
penalizes the FASTER arm, manufactures phantom sign-flips). Empirical proof (raw
interleaved best-of-11, /dev/null, taskset-pinned, frozen governor, AMD):
  silesia   T8: score(file)=0.98 FAIL  →  /dev/null = 1.15  (gz WINS decode by 15%!)
  storedheavy T4: score(file)=0.96      →  /dev/null = 0.76  (gz loses decode by 24%)
Absolute walls: silesia gz 270ms(file)→142ms(devnull); rg 264→164. storedheavy gz
121→42; rg 116→32. The ~80-128ms file-write (100-211MB output) is a near-fixed cost
added to BOTH arms that DILUTES/FLIPS the decode-time ratio. So:
  - the "broad 1-4% deficit" (esp. silesia 0.98) is largely a FILE-SINK ARTIFACT — gz
    actually WINS normal cells on /dev/null;
  - storedheavy is a BIGGER real loss than the file-sink showed (0.76 not 0.96) —
    decode-throughput-bound, the true problem cell;
  - a SECONDARY real finding: gz's file-WRITE is ~28ms slower than rg's on silesia
    (128 vs 100 extra) — a writer-efficiency lever, separate from decode, only visible
    with a file sink.
FIX OWED: score must TIME to /dev/null (per sink law) and sha-verify correctness in a
SEPARATE untimed run (pipe stdout to sha256sum), then RE-SCORE — the file-sink cells
(incl the just-banked "true gap") are all contaminated and must be re-measured.

═══ CONFIRMED /dev/null RE-SCORE (2026-07-09, sink fixed, fulcrum main @ b908b3d) ═══
Matched A/B (SAME binaries, file-sink vs /dev/null, AMD frozen, N=11, sha-verified,
gz-native @ 9896c2b7 vs rapidgzip-native 0.16.0). Step-0 sha-verified both sinks decode
correctly; file-sink walls ~3x inflated + noisier (confirmed the bug is real).
  cell            file-sink    /dev/null(TRUE)   flip
  silesia   T4    0.97 FAIL    1.05 PASS         FAIL→PASS (gz wins)
  silesia   T8    0.98 FAIL    1.14 PASS         FAIL→PASS (gz wins)
  monorepo  T8    0.86 FAIL    1.01 PASS         FAIL→PASS (gz wins)
  storedheavy T4  0.95         0.75 FAIL         real loss (was diluted)
  storedheavy T8  0.99         0.87 FAIL         real loss
  storedheavy T16 0.98         0.90 FAIL         real loss
TRUE PICTURE: gzippy WINS the normal corpora on AMD /dev/null; the deficit is
CONCENTRATED in storedheavy (incompressible) and is BIGGER than the file sink showed
(~10-25%, not near-tie). storedheavy is decode-THROUGHPUT-bound (phasebreak decode_wait
87%; on /dev/null gz decodes 100MB @ ~2.4GB/s vs rg ~3.2GB/s). THE WIN = close
storedheavy T4/8/16 to >=0.99. Eliminated levers: page-fault(SLACK), single-literal(TIE).
NEXT: H-vs-M worker Gate-2 (Huffman-compute vs u16-store-bandwidth vs scheduling) → inner-
loop lever (CLAUDE.md-open). Owed: cross-arch (Intel) replication of the /dev/null picture.
SECONDARY lever: gz file-WRITE ~28ms slower than rg on silesia (writer efficiency, separate).

███ COURSE-CORRECTION (2026-07-10): the fast loop is only 8.7% — careful_tail is 91% ███
Store-elimination oracle (worktree commit 214a52d3, byte-inexact, N=9, AMD frozen):
  base T4=48ms; ctrl(same-addr volatile)=46ms TIE (byte-exact); stack(reloc,no-TLS)=60ms
  +25%; probe(reloc,TLS)=61ms +27%. Both RELOCATED variants regress the SAME → it is
  NOT store-bandwidth (that predicts probe FASTER); it is a REGISTER-PRESSURE/codegen
  confound — a 2nd raw pointer in decode_marker_fast_loop defeats the N2 register-mirror.
  perf-stat: IPC 0.85, store-port-stall analog 0.43% (negligible) → NOT store-bound.
  ⇒ NO u8-literal-lane lever justified. Loop hints codegen/register-pressure-bound.
CRITICAL: byte-accounting shows decode_marker_fast_loop's store site produces only
~8.7M of storedheavy's 100M output bytes — **~91% flows through decode_careful_tail**
(uninstrumented). So ALL prior storedheavy inner-loop analysis (Gate-2 spin 1:1, H-vs-M,
store-oracle) was on a MINORITY path. The Gate-2 "1:1 wall response" only proved the fast
loop is executed+on-path, NOT that it dominates TIME (the huge per-hit injection ×33K
hits became the dominant term itself). THE REAL HOT PATH FOR STOREDHEAVY = decode_careful_
tail (and/or the stored-block bulk-copy path) — never characterized. NEXT: account ALL
storedheavy output bytes by path (fast-lit/fast-backref/careful/stored-copy), time each,
find the dominant time-consumer + WHY the fast loop exits to careful, THEN the real lever.
Advisor prognosis (cursor-agent + Qwen-fast): full parity 15-35%; Qwen leans accept-residual.

█████ BOTH-TOOLS MEASUREMENT (2026-07-10) — the divergence is the BLOCK FINDER █████
Built fulcrum/tools/memtrace.c (LD_PRELOAD cross-tool copy/alloc counter; fulcrum @ 538cddb).
COPY (storedheavy, output=100MB): gzippy copies 1.02x output (102MB bulk memcpy, ~4k calls);
rapidgzip copies 4.34x (434MB) + 126MB memset. => "gz double-copies" FALSIFIED — gz copies
LESS than rg and is still slower. The stored copy is NOT the lever (it's bulk single-pass).
SYMBOL-RESOLVED perf (both tools, fulcrum insn ledger, both closed):
  gz 193M instrs IPC 1.55; rg 209M IPC 1.73 (rg does MORE, faster — gz is IPC-stalled).
  gz TOP fns: find_next_dynamic_block 49.2M(25.5%), run_contig asm 37.3M(19.3%),
    finish_decode_chunk_contig 25.7M, find_next_uncompressed_block 14.6M, validate_huffman
    13.1M, memchr-avx2 9.7M, libc memmove 6.6M(3.6%).
  gz BLOCK-FINDING total (find_next_dynamic+find_next_uncompressed+validate_huffman+memchr)
    = 86.7M = 44.9%.  rg block-finding (seekToNonFinalDynamicDeflateBlock) = 36.6M = 17.5%.
  => gz spends ~2.4x more on BLOCK FINDING than rg on storedheavy. THIS is the dominant
     divergence, not the copy. (Also: rg CRC=35.3M via ISA-L fold; gz CRC=0.9M — gz WINS CRC.)
CAVEAT: 44.9% is instruction SHARE; the block finder runs ASYNC (phasebreak blockfind_wait~0),
  so it MAY be slack (share!=wall). Gate-2 two-arm perturbation IN FLIGHT (block-finder vs
  run_contig decode) to decide which gates the wall before building a lever.
FULCRUM BUG found: insn-attr's plan uses `perf script -F ...,srcline,insn` which splits each
  record onto 2 physical lines → --analyze falsely reports 51% opcode-byte drop (true 6.5%/3%).
  Fix the plan generator (drop srcline) or the parser. Flagged, not fixed.

──── GATE-2 (block-finder vs decode, 2026-07-10, SLEEP frequency-neutral, N=11) ────
Two-arm perturbation (worktree diag/gate2-bf-vs-dec @ 43a4f3aa). SPIN mode gave an 8.7x
turbo-depression artifact under tight pinning (discarded per the freq-neutral-control rule);
SLEEP is the trustworthy result. Δwall/Δinject slopes at 40ms: BF=0.247, DEC=0.231 — TIED,
both ~25% wall-coupled. rapidgzip control flat (31-32ms). VERDICT: neither the block finder
nor the decode is a clean 1:1 wall lever — the parallel pipeline overlaps ~75% of any single
region's delay. The block finder is NOT pure async slack (real 25% coupling) but NOT the whole
story. JOINT READ: gz does similar TOTAL work to rg (193M vs 209M instr) but PARALLELIZES WORSE
(1.3x vs 1.9x on 4 threads) — that parallelism gap IS the 42ms-vs-31ms wall gap. Biggest single
reducible divergence feeding it = the block finder (gz 86.7M vs rg 36.6M, +50M), concentrated in
find_next_uncompressed_block (14.6M) + validate_huffman_codes (13.1M). LEVER IN FLIGHT: converge
gz's block finder to rg's cheaper scan (skip stored blocks via LEN; cheaper candidate rejection),
gate on fulcrum score. If it fires but the score stays flat (25% coupling absorbed by overlap),
the next front is PARALLELISM/scheduling (gz 1.3x vs rg 1.9x). Gate-3 cross-arch owed either way.

──── FALSIFY: finder first-hit change + PINNED ROOT CAUSE (2026-07-10) ────
Finder first-hit early-exit (branch feat/blockfinder-first-hit-converge @ 91f7890d,
byte-exact, +92/-14 blockfinder_validation.rs): does NOT fire on storedheavy (stored regions
have no candidates → whole 8KiB window scanned regardless of first_only). Total instr flat
(194M→192M), fulcrum score BIMODAL/noisy no-improvement, win cells hold. KEEP as a faithful
no-regression tie for candidate-dense corpora; NOT a storedheavy win.
PINNED ROOT CAUSE (deterministic): the 2.4x block-finding excess is NOT the finder's
per-position work (faithful port) — it is the CONSUMER's boundary-ADVANCE. gz's slow-path
consumer (chunk_fetcher.rs:3265 with_sync_boundary_search) WINDOW-SCANS the finder to locate
each successive block boundary across stored regions; rapidgzip WALKS the deflate block chain
during decode (read header → skip stored LEN bytes / decode → next block), calling the finder
only for the chunk ENTRY point. On 98.9%-stored data gz re-scans stored regions to find
boundaries reachable directly via LEN. This is BOTH the block-finding excess AND the leading
suspect for the parallelism deficit (gz 1.3x vs rg 1.9x = the 42-vs-31ms wall gap).
LEVER IN FLIGHT: converge the consumer boundary-advance to rg's chain-walk (use the decoder's
post-block position; skip stored LEN; finder only at chunk entry). Gate on fulcrum score.
If it fires but score flat → parallelism limit is elsewhere → measure via fulcrum consumer+scaling.

█████ DEFINITIVE both-tools SCALING (2026-07-10, perf-stat, deterministic) █████
storedheavy, AMD Zen2, gz-native @ 9896c2b7 vs rapidgzip-native:
              gz instr    rg instr   gz faults  rg faults   task-clock
  T1          139M        174M       2,228      3,383       gz 40.5ms / rg 53.9ms
  T4          321M        297M       15,534     14,762      gz 121.9ms / rg 95.3ms
  T8          348M        330M       14,754     21,071
  inflation   2.31x       1.71x      7x         4.4x
SETTLED FINDINGS (both-tools, measured):
  1. gz SINGLE-THREAD decode is LEANER than rg (139M < 174M) — gz WINS T1.
  2. PAGE-FAULTS ARE NOT THE LEVER — rg faults ~same as gz (14,762 vs 15,534 @T4). The
     prior fault-reuse "SLACK" verdict was CORRECT; both tools pay it. STOP re-testing faults.
  3. THE GAP = gz's PARALLEL OVERHEAD: gz inflates 2.31x (T1->T4) vs rg 1.71x = gz's parallel
     path adds ~+59M more instructions than rg's. A decode that WINS at T1 LOSES at T4.
  => not copy, not inner-loop, not page-faults, not block-finder-per-position — it's that gz's
     SPECULATIVE PARALLEL-DECODE machinery (markers / apply-window / chunk coordination) costs
     more per added thread than rapidgzip's. LEVER = make the parallel path as lean as rg's.
  NEXT: decompose the +59M parallel overhead (marker-decode vs apply-window vs block-coordination
  vs re-decode) — target the biggest component, converge to rg, gate on fulcrum score. This is
  the faithful-port parallel-efficiency front the campaign kept circling; now measured to a number.

█████ BLOCK-FINDER REMOVAL ORACLE — STRONG-tier, quiet N=21, gated (2026-07-10) █████
Oracle: feed ground-truth chunk entries, skip all find_next_* (Gate-0: finder calls
32/38/190→0 at T4/8/16, byte-exact sha==gzip -dc, 100% oracle hits). fulcrum abmeasure
(quiet, llama-paused, N=21, /root/abq_t{4,8,16}_bforacle.json):
  T   instr base→after     cyc base→after     rg/after(>=0.99 win)  paired after<base
  4   317M→220M (-31%)     386M→339M (-12%)    0.81 (loses)          16/21
  8   357M→245M (-31%)     473M→437M (-8%)     0.82 (loses)          14/21
  16  820M→239M (-71%)     738M→423M (-43%)    1.13 (gz BEATS rg)    21/21
VERDICT: block-finding IS the T16 wall lever (removal → gz beats rg, 21/21, decisive).
The finder EXPLODES at high T: 820M instr / 8.2 per byte at T16 vs 317M/3.2 at T4; 190
chunk-entry scans at T16 vs 32 at T4. WITHOUT block-finding, T16≈T4 (~239M vs 220M) — the
entire T16 inflation IS the block-finding. At T4/T8 block-finding is only 31% AND high-IPC
(cheap cycles) → removal doesn't win; those cells lose to the DECODE/MARKER work (separate front).
LEVER (T16 win path): reduce block-finding at high T — either (a) fewer/larger chunks (why 190
at T16? over-chunking?) or (b) cheaper per-scan finder (port rg incremental first-hit seekTo* +
BTYPE pre-check). Removal oracle shows either wins T16. Gate on fulcrum score. T4/T8 need the
decode/marker efficiency front separately. llama resumed (STAT=Sl, no orphan). Oracle diag on
box /root/gz-bf-oracle.

█████ FALSIFY: block-finding is NOT the wall lever — the T16 "win" was a CYCLES-MIRAGE █████
Direct paired WALL A/B (N=61, common-mode cancels, llama-paused, /dev/null): removing ALL
block-finding (bf-oracle) at T16 is a DEAD WALL TIE (28/27, 36 vs 37ms). The chunk-cap fix
(190→31 chunks, instr 701M→255M, byte-exact) REGRESSED T16 wall (41 vs 37ms — fewer chunks =
less parallelism). So block-finding is 71% of INSTRUCTIONS at T16 but OVERLAPPED — NOT on the
wall-critical path. The earlier removal-oracle "T16 rg-parity 1.13" was CYCLES (perf-stat), not
WALL — cycles!=wall. Block-finder lever (both fix (a) fewer-chunks and (b) cheaper-scan) = DEAD.
CORE GATED FACT (the disease): gz WINS storedheavy single-thread (T1~38ms < rg T1~52ms; gz decode
is leaner) but BARELY PARALLELIZES it (T1~38 → T8~34 → T16~37ms, ~1x) while rg parallelizes
(52→40→31ms, ~1.7x). rg reaches 31ms with ~24 chunks; gz needs 190 chunks and still caps at 37ms
→ gz's per-chunk parallel throughput is lower. EVERY instruction-heavy region (block-finding, copy,
inner-loop, page-faults) is OVERLAPPED — removal ties the wall. The gap is the PARALLELISM FAILURE
itself: gz's parallel machinery's overhead/serial-dependencies cancel the speedup. Distributed, not
a single removable region. NEXT: decompose WHERE the parallel speedup dies (phasebreak across
T1/4/8/16 — the phase that does NOT shrink with T is the serial bottleneck; candidates: in-order
consumption, window handoff, marker-resolution ordering, memory-bandwidth contention).
