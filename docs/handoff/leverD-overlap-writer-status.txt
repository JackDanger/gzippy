OVERLAP-WRITER LEVER (leverD, origin/perf-overlap-writer @ 14b9663f, patch scratchpad/leverD-overlap-writer.patch):
THE LOCATED FIX. Phase-timing (drive_impl, storedheavy T4): setup=20us, consumer_loop=30ms(94%), finalize=2ms => the deficit is the SERIAL in-order writev inside the consumer loop (Amdahl tail). output_writer.rs doc says exactly this + ships a background writeFunctor thread. Wired drain_one_pending non-pipe fd path -> output_writer::submit_chunk. BYTE-EXACT (differentials sha==gzip -dc silesia/logs/software/storedheavy-T1/4/8/mm3; fmt+clippy clean). NOT DONE: 1 test fails (test-invariant, NOT corruption — chunk release/recycle now happens in the writer thread, breaks a LIVE_CHUNKS/deletion-trap/recycle_deferral counter). NEXT: (1) resolve that 1 test (preserve recycle accounting in-consumer before submit, OR update the invariant), (2) perf-gate on the box: build 14b9663f, fulcrum score storedheavy T4/8/16 vs rapidgzip must reach >=0.99 + no win-cell regress, both arches, (3) merge if it passes. This is the strongest candidate — byte-correct + directly targets the phase-timing-confirmed serial writev.
