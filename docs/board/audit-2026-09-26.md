# Whole-codebase maintainability audit — 2026-09-26 (six-agent wave)

Scope: comments (find + delete), dead code, API/module structure, test-suite
coherency, docs-tree rules consolidation. Trunk dated c9fd768c (all lever work
merged; the stacked box leg pends hardware+SSO, PR #383 draft).

Agent-31's deletions are on branch `audit/comment-hygiene` (7 commits,
verified: -9 net comment lines in 7 files; lib gate + one_encode_only +
thread_byte_parity green; build clean). The four read-side reports append
verbatim below their actionable cores. The comment FIND report's deleted core
is agent-31's PR; its stale-but-receipt-embedded prose (split_recall.rs:91,
probe.rs:165, block_cost_probe.rs:27-31, parse/mod.rs:2005) needs a
prose-editing pass, not delete-only.

---



## Dead-code audit (agent-32)

completed">All checks complete. Here is the consolidated dead-code audit.

# Dead-surface audit — `src/` (SCOPE: dead-code)

Board-dated against `docs/board/sprint-2026-09-25.md` (L9 retune passes=2 confirmed live at `src/compress/deflate/level.rs:499-508`; `GRID_REF_THREADS=4` at `src/compress/pipelined.rs:120`, used `:193`; near-opt-parallel-flush landed per Cargo.toml:152-183).

## 1. Cargo features that gate nothing (defined, never referenced in code)

| Feature | Evidence | Risk |
|---|---|---|
| `l3-tune` (Cargo.toml:271) | 0 `cfg` refs in src/tests/examples/CI/Makefile; doc says "See level.rs's level-3 arm" but no gate exists there | **Safe-delete** (deleting an unused feature cannot change any build) |
| `exact-huffman` (Cargo.toml:283-290) | 0 `cfg` refs; the described route landed instead as runtime knob `params.try_exact_huffman` (`level.rs:95`, certified at `level.rs:524-527`) | **Safe-delete + fix the doc** (doc advertises a gate that doesn't exist) |
| `libdeflate` (Cargo.toml:348) | Bare `libdeflate = []`, 0 refs anywhere | **Safe-delete** |
| `isal` (Cargo.toml:74) | Doc claims "enabled by build.rs when static lib is built" — build.rs has no `CARGO_FEATURE_ISAL`予以 handling (only `ASM_KERNEL`, `parallel_sm`, `pure_inflate_decode`, build.rs:62-105) | **Safe-delete + stale doc** |
| `counters` (Cargo.toml:111) | 0 real `cfg` refs; its only "reference" is prose inside a doc comment (`anatomy_counters.rs:458`) claiming a `#[cfg(feature = "counters")]` that doesn't exist | **Safe-delete**, doc lie adjacent |
| `rpmalloc-stats` (Cargo.toml:330-335) | 0 refs to `feature = "rpmalloc-stats"`; the claimed `GZIPPY_RPMALLOC_STATS`-gated `dump_global_stats` has no occurrence in src | **Probe-first**: the feature still flips `rpmalloc-sys/statistics` in the vendored C allocator, so codegen isn't a no-op; delete only with the doc trail |
| `pure-rust-encoder` (Cargo.toml:80) | Self-declared no-op ("gates nothing"); kept deliberately so external `--features` still resolves | Known no-op — leave, or delete with a changelog note |

## 2. Feature-gated code never enabled in CI (compiled out in every CI flavor)

CI enables only: `gzippy-native`, `isal-compression`, `pure-rust-inflate`, `oracle`, `anatomy-counters` (`.github/workflows/ci.yml:104-144,416`). tests/examples/scripts additionally use `split-recall`, `split-probe`, `anatomy-wall`, `global-rpmalloc`. Never built anywhere:

- `near-opt-parallel-flush` — 15 gate sites (e.g. `compress/deflate/bitstream.rs:357,771,804`); its pinned byte-identity tests live in `tests/l9_t4_chunk_cost_probe.rs:167` behind the feature and are `--ignored` probes "never a regression gate". Board: PR #379 shipped it DEFAULT-OFF awaiting the stacked box leg. **Dormant-by-design, but CI never even compiles the flavor** — silent-rot risk until the box leg lands.
- `trace` (`infra/trace_spans.rs:25,99`), `profile` (`decompress/inflate/consume_first_decode.rs:2353-2420`), `debug_decode` (`consume_first_decode.rs:556-567,2379-2385`) — hand-rolled instruments, zero CI/consumer enables.
- `bucket-oracle-null-mf` (1 site), `bucket-oracle-no-histogram` / `-no-emission` — live as `cfg(not(...))` elimination oracles at `parse/mod.rs:358,370,461,477`, never enabled. Cargo.toml:184-204's doc anchors are **stale**: it cites `parse/gated.rs:391-399` (file doesn't exist), and `greedy.rs:45-53`/`lazy.rs:61-64` now point at unrelated call sites.
- `coz` (`coz_probe.rs:13-34`), `global-rpmalloc` (`main.rs:12-14`), `l1-tune` (36 sites incl. CLI `--tune`), `ladder-tune`, `split-recall`, `split-probe`, `block-cost-probe`, `anatomy-wall` — documented instruments; the gap to flag is only that CI never builds them either (ci.yml's own #260 rationale admits the class).

## 3. `#[allow(dead_code)]` — actually dead vs. reachable

**Confirmed dead (no caller under any feature/level):**
- `src/utils.rs:1` blanket `#![allow(dead_code)]` hides three zero-caller helpers: `get_file_metadata` (:27), `format_size` (:81), `format_percentage` (:98) — only "callers" are their own unit tests (:107-124). **Safe-delete** + drop the blanket allow. (`detect_format_from_file`/`is_compressed_file` are alive via `decompress/io.rs:389,472`.)
- `src/format.rs:1` blanket allow covers two never-called methods: `default_extension` (:23), `magic_bytes` (:31). **Safe-delete**.
- `src/error.rs:6` enum-level allow masks variants never constructed crate-wide: `Compression` (:12), `PermissionDenied` (:30), `Thread` (:33), `UnsupportedFormat` (:42); plus dead ctors `compression()` (:50), `thread()` (:62), `internal()` (:66). `Internal`/`Parse` variants are alive (5/4 sites). **Probe-first** — these are public lib API (`gzippy::error`), semver-visible.
- `src/infra/io_thread.rs` — entire `WriteAhead` mechanism (:12 enum, :24 struct+impl): zero callers anywhere (src, tests, examples, benches, fulcrum); every `WriteAhead::new` is self-test (:99-184). ~100 lines dead in production, kept alive only by its own test module. **Safe-delete with its tests.**
- `src/decompress/bgzf.rs` legacy decode kernels: `get_fixed_tables_turbo` (:204), `decode_huffman_consume_first` (:236), `decode_huffman_turbo` (:509), `decode_huffman_asm_x64` both cfg variants (:881, :1105), `copy_match_asm` (:1120 — only "caller" is the dead `decode_huffman_asm_x64` at :1036/:1052), `decode_huffman_ultra` (:1132), `decode_huffman_packed` (:1368), `sum_member_isize` (:2231, annotation itself says the grid driver uses the grow-safe fallback). Production kernels are the two-level LUT path wired from `decompress/mod.rs:190-272`; these kernels are reachable only from bgzf's own `#[cfg(test)]` block (e.g. :2824, :3360, :3377). **Probe-first** — delete with their test-module consumers.

**Reachable — annotation misleads or is stale:**
- `src/compress/deflate64.rs:336,345` `compress_deflate64(_to_writer)` marked allow(dead_code) but **are called** from `lib.rs:335-347`. Stale annotation. (`compress/mod.rs:238,253` use the same pattern with accurate "called from lib.rs" comments.)
- `pipelined.rs:412,456` `compress_buffer`/`compress_file` "C-FFI oracle method; no in-crate caller" — compiled behind `cfg(any(test, feature = "ffi-oracle"))` (:410) and I found no consumer in src/tests/examples/benches/fulcrum; the C-FFI claim should be verified against fulcrum's FFI harness before deletion. **Probe-first**.
- `backends/inflate_bit.rs:25-89` — whole module is `#[cfg(test)]` (backends/mod.rs:15-20); its fns are bench-reachable only via the `#[doc(hidden)]` re-export (`lib.rs:58-62`, consumer `benes/engine_isolation`). Reachable-in-dev; keep, but the module doc already says so.
- `src/tests/fixtures.rs`: field `Fixtures.multi_member_gz` (set, **never read** — `routing.rs` builds its own multi-member fixture; annotation ~:25) and generator `binary_1mb()` (zero callers, ~:37-39). **Safe-delete**.
- noise floor: `segmented_markers.rs:118,125,132,406,636`, `staged_bits.rs:99,139,145,167,197`, `single_member.rs` (~30 sites), async_block_finder.rs:224 — vendor-parity/test surfaces of the rapidgzip port; consistent with their annotations, leave as cluster.

## 4. CLI flags parsed but unread / unreachable plumbing

- `--best`/`--fast`: fields `GzippyArgs.best`/`.fast` (`cli.rs:39,43`, set `cli.rs:212-219`) are read **now**here outside cli.rs — only the `compression_level` side-effect is live. **Safe-delete the two fields.**
- `-R/--rsyncable`: parsed (`cli.rs:188,400`), **rejected** at `main.rs:323-328` before any io — so `compress/io.rs:380-404` (rsyncable mmap branch), `compress/parallel.rs:702` (`compress_rsyncable`), `parallel.rs:647` (`split_rsyncable`), and the `route::RSYNCABLE` const (`compress/mod.rs:62`) are **unreachable by any invocation**, with zero test/example consumers. io.rs:356's comment "`--rsyncable` keeps its content-defined split (also pure now)" is **stale** vs the flag-honesty rejection. **Probe-first** (it's presumably the seed for issue #302) — at minimum fix the comment.
- `-H/--huffman`, `-U/--rle`, `-i/--independent`, `-A/--alias`, `-z/--zlib`, `-K/--zip`: parsed and *read to reject loudly* (`main.rs:322-362`) — not dead, but the io.rs:354 comment "(and `--huffman` / `--rle`)" describes routes the runner can no longer reach: stale.
- All other parsed fields verified read (`index_file` via multiline `index_mode.rs:62,116`; `synchronous` `compress/io.rs:531`, `decompress/io.rs:203`; `comment` `compress/io.rs:808`; `no_time` `compress/io.rs:795`; `recursive` both io paths).

## 5. Duplicated helper implementations

- **`reverse_bits` × 4 identical copies** (decode side): `decompress/inflate/consume_first_table.rs:323`, `decompress/inflate/specialized_decode.rs:349`, `decompress/inflate/libdeflate_entry.rs:795`, `decompress/block_walker.rs:644`. Plus two table-based variants on the compress side (`compress/deflate/huffman/fast.rs:50`, `compress/ldx/huffman.rs:379`) which are intra-verified by `huffman.rs:750`'s exhaustive test — leave those two alone. The four loop copies are plain duplicates; risk: they're private for inlining on byte-exact hot paths → **probe-first**.
- **`explicit_zopfli` predicate × 3**: `args.zopfli_iterations.is_some() || args.zopfli_no_split || args.zopfli_split_max.is_some()` hand-rolled at `compress/mod.rs:126-128`, `compress/io.rs:359-360`, `compress/io.rs:627-629`. One `#[inline]` helper on `GzippyArgs` dedupes it; behavior-preserving → **safe** (three-way consistency is load-bearing for routing).
- `format_size`/`format_percentage` (utils.rs) partially overlap with `list_file`'s inline ratio + the CLI's k/m/g parsing (`cli.rs:576-589`) — their unreachability (§3) makes them the dedupe-relevant deletions.

## 6. Stale docs contradicting the current engine map (found while dating dead surface)

- `lib.rs:275-279` `compress_raw` docs: "same backend hierarchy … ISA-L SIMD on x86_64 for levels 0–3, then libdeflate one-shot" — contradicted by `compress/mod.rs:152-159` and Cargo.toml Increment-7 (sole pure-Rust engine). `lib.rs:286-289` `decompress_raw` docs: "Uses libdeflate … Falls back to a flate2/zlib-ng streaming decoder if the output exceeds 1 GiB" — the actual impl is `inflate_consume_first` with growth to a **2 GiB** cap (`decompress/mod.rs:920-936`). `lib.rs:210-213` `decompress` docs ("ISA-L single-member, or libdeflate one-shot") contradict Cargo.toml:66-71 (both decode-FFI paths removed). Three stale doc blocks on the crate's front door.
- `.github/workflows/ci.yml:370-371` comment: isal-compression "routes T1 L0–L3 through ISA-L in the spawned binary" — no production call into `isal_compress` exists (only `parallel.rs` oracle paths, backends/mod.rs:14-17 says "OFF the production compress routing graph"). The CI release flavor carries ISA-L FFI for an unreachable route.
- Cargo.toml:74 (`isal` doc) and Cargo.toml:189 (`bucket-oracle` anchors, incl. the nonexistent `parse/gated.rs`), per §1/§2.

## Summary counts

- **Safe-delete** (zero risk): 6 dead features (`l3-tune`, `exact-huffman`, `libdeflate`, `isal`, `counters`, `rpmalloc-stats` pending rpmalloc-sys note), 3 utils helpers, 2 format methods, 4 dead error ctors + 4 never-constructed variants (public-API caveat: semver), `WriteAhead` module, `Fixtures.multi_member_gz` + `binary_1mb()`, `best`/`fast` CLI fields.
- **Probe-first**: bgzf legacy kernels (delete with their test contracts), `compress_rsyncable` family (keep as #302 seed or delete with receipt, but fix the io.rs comment), `pipelined.rs` "C-FFI oracle" methods, the 4 decode-side `reverse_bits` copies, `rpmalloc-stats`.
- **Dormant-by-design, CI-never-compiled**: `near-opt-parallel-flush` (+ its `--ignored` probe suite), the bucket-oracle trio, `trace`/`profile`/`debug_decode`/`coz`/`l1-tune`/`ladder-tune`/`anatomy-wall` — rotation-risk only; recommend a CI "instruments compile + probe" job if the #260 taxonomy is extended.

## API/structure audit (agent-33)

completed">Audit complete. Here is the full report.

---

# API-Structure Audit — gzippy (scope: crate shape)

**Dated anchors used** (from `docs/board/sprint-2026-09-25.md` + code): thread-free chunk grid `GRID_REF_THREADS = 4` (`src/compress/pipelined.rs:120`), the `near-opt-parallel-flush` feature DEFAULT OFF (`Cargo.toml:183`, landed PR #379), L9 retune to `passes = 2` (`src/compress/deflate/level.rs:505-507`), L8 keeps L11-knob route (`level.rs:509-511`), pure-Rust decode graph (no C-FFI), L1/L3/L6/L7 legacy routing exceptions (`level_uses_ldx`). Anything contradicting these is stale.

## A. lib.rs public surface — API-required vs accreted

`src/lib.rs` itself is disciplined: 16 top-level `pub fn` + 2 types + 1 alias set, with the internal trees suppressed by `#[doc(hidden)] pub mod` (lib.rs:89-98) and a stated semver boundary ("Items under `compress::`/`decompress::` are not covered by semver", lib.rs:86-88).

- **API-required**: `compress`, `compress_with_threads` (lib.rs:119, 143), `compress_to_writer[_with_threads]` (175, 196), `decompress[_with_threads]` (216, 229), `decompress_to_writer[_with_threads]` (244, 261), raw pair + aliases `compress_raw`/`deflate_encode`/`decompress_raw`/`deflate_decode` (280-298), the four Deflate64 fns (314-347), `classify` + `DecodePath` + error types (356, 101-102).
- **Accreted-but-honest**: `set_output_file` no-op lib stub with `#[doc(hidden)]` (lib.rs:82-83) — fine; `encode_gzip_reader_to_writer` marked `#[allow(dead_code)] // library API entry point` (`src/compress/deflate/mod.rs:676-684`) — documented accretion.
- **Inconsistent with the policy**: `pub mod fixtures` and `pub mod holdout` (lib.rs:97-98) are measurement/pin surfaces (fixtures.rs, holdout.rs `PINS`) rendered by rustdoc as product API, while every other measurement surface (`isal_decompress_oracle`, `infra`, `coz_probe`) is `#[doc(hidden)]`. One attribute each fixes it.
- **Cross-module-only `pub`**: `bgzf::inflate_into_pub` (`bgzf.rs:181`) and `bgzf::copy_match_into` (`bgzf.rs:1680`) are `pub` solely to reach tests/siblings; the `_pub` suffix is itself naming noise inside a doc-hidden tree.

## B. bgzf.rs — the 7.5k-line problem, quantified

`src/decompress/bgzf.rs` = **7,561 lines: ~2,495 production + ~5,065 inline tests** (`mod tests` at :2507, `mod optimization_tests` at :4090). Worse, of the production ~2,500 lines only **~900 are live**:

- **The dead strategy museum, lines 185–1789 (~1,600 lines).** Four superseded inflate loops, each carrying `#[allow(dead_code)]`, i.e. deliberately kept:
  - `decode_huffman_consume_first` (`bgzf.rs:238`) — **zero callers in the entire repo** (grep-verified: only the definition matches);
  - `decode_huffman_ultra` (`:1134`) — zero callers;
  - `decode_huffman_packed` (`:1369`) — zero callers;
  - `decode_huffman_turbo` (`:511`) — called **only** from its own inline tests (:2850, :3360, :3377);
  - supporting dead mass: `get_fixed_lit_len_lens`/`get_fixed_tables_turbo` (:186, :205 — sole caller is the test at :2837), `copy_match_asm` (:1061/:1121 — called only from turbo), `copy_match_into` (:1680 — callers are the dead decoders + tests :7143-7165).
  The real production decoder is `inflate_into → inflate::consume_first_decode::inflate_consume_first` (`bgzf.rs:174-176`).
- **Live production code**, lines 1790–2495: BGZF route (`decompress_bgzf_parallel_to_vec` :1790, `decompress_bgzf_parallel` :1874, `decompress_bgzf_pipelined` :1889, streaming :2003) and multi-member route (`parse_gzip_header` :2047, `scan_member_boundaries_fast` :2090, gates `sum_member_isize`/`fast_path_ok` :2232/:2236, `gz_coverage_is_pure` :2295, multi-member entries :2370/:2479). Both routes are wired from `decompress/mod.rs:353-368, 518, 534, 903` and the classifier (`mod.rs:218, 265`).

**Proposed split** (after the dead mass is deleted the file drops to ~3,400 test + ~900 prod):

```
src/decompress/bgzf/
├── mod.rs          BgzfBlock, parse_bgzf_blocks, inflate_into(_pub),
│                   decompress_bgzf_parallel[_to_vec], decompress_bgzf_pipelined,
│                   decompress_bgzf_streaming            (≈ lines 1–184, 1790–2046)
├── multi_member.rs parse_gzip_header, scan_member_boundaries_fast,
│                   sum_member_isize, fast_path_ok, gz_coverage_is_pure,
│                   gz_member_len, decompress_multi_member_parallel[_to_vec]
│                                                        (≈ lines 2047–2495)
└── tests.rs        mod tests + mod optimization_tests    (lines 2507–7561, via #[path])
```
Re-export to keep `decompress::bgzf::*` paths and all 10+ cross-module/test callers (`decompress/mod.rs:190, 218, 265, 353-367, 518, 534, 903`; `src/tests/*`) compiling unchanged. Benefit: the module name finally stops lying ("bgzf" hosts plain multi-member gzip logic — see F below), and a reader on the decode graph reads 900 lines, not 7.5k. Blast radius: pure moves + re-exports; zero behavior change; decode golden/routing suites are the safety net. (Same pattern exists one level deeper — `parallel/marker_inflate.rs` is 7,795 lines with tests from :4575 — but that is outside this audit's named file; flag it as the next candidate.)

## C. Naming drift

1. **ldx / port / libdeflate-port / legacy.** One concept, four vocabularies: the module is `ldx` (`compress/ldx/mod.rs:1` "a FAITHFUL, function-by-function port of libdeflate's compressor"); the predicate is `level_uses_ldx` (`deflate/mod.rs:587`); prose says "the port" / "the libdeflate port" (`deflate/mod.rs:588`, `pipelined.rs:362`, `compress/mod.rs:9`); the probe arm is `ldx_gzip` (examples/ir_runner.rs, board P2); and the census names the **native** engine `legacy_encode()` (`deflate/mod.rs:566-583`, editor's note at :571 "LEGACY_ENCODES"). Calling our own shipped engine "legacy" is the worst of the four — the reader initially parses it as the pre-port engine being retired, which is precisely backwards. Consolidate: `ldx` for the module, `port_encode`/`native_encode` for the census, and kill the word "legacy" in gate comments where it means the native engine (deflate/mod.rs:561-635).
2. **Dangling topic sentence.** `deflate/mod.rs:507`: "Levels whose production encoder is our own libdeflate port (`compress::ldx`)." is glued onto `encode_gzip_bytes_to_vec`'s doc (which takes any level 0–12 and owns none specifically); it is the missing heading of the "THE PORT IS THE PRODUCT" essay that follows (:509-537). Move it back to `level_uses_ldx`'s doc or restore the heading.
3. **parallel vs pipelined — four meanings, two of them hazardous.**
   - `compress::parallel::ParallelGzEncoder` (`parallel.rs:193`) — the RETIRED "GZ" C-FFI encoder, `#[cfg(any(test, "ffi-oracle"))]`-quarantined except its header type (see D1).
   - `compress::pipelined::PipelinedGzEncoder` (`pipelined.rs:219`) — the production T>1 encoder (`compress_buffer_pure`, :299) — but its Gate-4 route string is `PURE_PARALLEL_PIPELINE` (`compress/mod.rs:60`), and the T>1 params fn is `params_parallel` (`level.rs:425`), and the scheduler entry is `compress_parallel` (`infra/scheduler.rs:536` — whose own comment at :536 already mislabels the situation, saying "the only caller is the C-FFI `ParallelGzEncoder`").
   - `decompress::bgzf::decompress_bgzf_pipelined` (`bgzf.rs:1889`) — "pipelined" here means member-thread pipelining on the decode side, unrelated to `PipelinedGzEncoder`.
   - `decompress::parallel::*` — the decode fleet, a wholly different subsystem under the same noun.
   The specific reader trap: grep "ParallelGz" and land on the retired oracle, one directory next to the production encoder. The Gate-4 strings are externally grepped (`fulcrum cpreflight`'s `encode-path=` token, mod.rs:72-77), so rename the *types/functions*, not the route constants — or fix only `scheduler.rs:536`'s comment and record the glossary in one place.

## D. Asymmetric abstractions

1. **`compress/parallel.rs` is tri-modal** (`src/compress/parallel.rs`): production `GzipHeaderInfo` + `to_member_header` NOT gated (:58-99), C-FFI oracles gated `#[cfg(any(test, "ffi-oracle"))]` (:110-652: `adjust_compression_level`, `get_block_size_for_level`, `ParallelGzEncoder`, `compress_single_member`, `compress_block_bgzf_*`), and the rsyncable pair NOT gated (:654 `split_rsyncable`, :702 `compress_rsyncable`). Production code imports a shared header type from inside the oracle module (`compress/mod.rs:29`, `pipelined.rs:31`), and the honest `SimpleOptimizer` has correct treatment (mod.rs:19-23). The gate should be per-item, the production items (`GzipHeaderInfo` → `compress/gzip_header.rs`, rsyncable → `compress/rsyncable.rs`) should move out.
2. **`-b`/`--blocksize` is honored on exactly one route.** Consumed at `compress/mod.rs:216-222` and `compress/io.rs:424, 636` → `PipelinedGzEncoder::set_block_size_override` only (pipelined.rs:271). Zopfli route drops it explicitly (mod.rs:136-140, documented); rsyncable's block sizes are hardcoded in `split_rsyncable` (:655, ~128 KB target) with no `-b` plumbed; T1 has no grid. Help text at `main.rs:713` says "parallel compression (pigz semantics)" — defensible for today's routes but a trap waiting for issue #302 to land (`--rsyncable` is CLI-rejected today, `main.rs:323-327`, while io.rs:380-400 keeps the wired machinery). Worth a doc sentence where `-b` is *ignored*, not where it works.

## E. State threading (bw / out / scratch) that wants a struct

1. **`deflate_into`'s 9 positional args** (`deflate/mod.rs:364-374`: `bw, buf, data_start, in_end, input_total_len, level, is_last, sync_flush, parallel`) threaded from five wrappers whose pairwise differences (sync-flush vs bit-splice) live only in comments (`:125, :155, :250, :437`). Same file also carries the **dictionary-padded buffer assembly duplicated 4×** (:170-191, :196-216, :262-279, :280-301). A `ChunkJob` (input slice + `data_start`/`in_end`/`input_total_len` + `is_last`/`sync_flush`) plus one `padded_with_dict(dict, data)` helper removes four copies of correctness-critical pad logic.
2. **`fast::run` takes 13 positional args** (`parse/mod.rs:521-535, 563-577`): `buf, data_start, in_end, input_total_len, statics, bw, is_last, block_length, use_dynamic, limit_hash_update_inserts, bucket2, cost_gate, budget` — every new L1 knob means another position to typo. Bundle the runtime-only knobs (`block_length, use_dynamic, inserts, bucket2, cost_gate, budget`) into a `FastParseCfg`; keep the const generics.
3. **`near_optimal::run`'s chunk driver is ~14 loose locals** (`near_optimal.rs:1150-1193`: `in_cur_base, in_next, in_next_slide, next_hashes, cache_ptr, split_stats, new_match_len_freqs, prev_observations, prev_num_observations, prev_block_used_only_literals, …`) loop-carried across the fill/flush split — the exact state the #379 flush-overlap and the stacked box leg (board's lever #3 stacks) already thread across threads. A `ChunkFillState` struct turns "what does the flush reader touch?" into a visible field list. Hot code — mechanical, but pin suites (perf_shape, l9_t4_chunk_cost_probe) hold it.

## F. Stale comments contradicting current facts (dated vs board)

- `bgzf.rs:3683`: "=== PRODUCTION PATH: inflate_into_pub() → libdeflate C FFI ===" — **stale twice over**: production is pure-Rust (`bgzf.rs:170-176`), and lib.rs:3-5/pipelined.rs docs claim it; `decompress/mod.rs:315-316` says "no C-FFI".
- `decompress/inflate/consume_first_decode.rs:3304-3308`: "inflate_consume_first() is NOT the production path. Production uses inflate_into_pub() → libdeflate C FFI." — inverted; consume_first_decode IS the shipped production decoder.
- `infra/scheduler.rs:536` — Increment-7 wording that names the C-FFI encoder as caller (C3 above).
- `bgzf.rs:2497-2505` — a "Single-Member Parallel Decompression (rapidgzip strategy)" signpost at the tail of bgzf.rs describing machinery that lives in `decompress/parallel/single_member.rs`, misleading navigation.

---

## Ranked refactor plan (10 items)

| # | Item | file:line | Reader-facing benefit | Blast radius |
|---|------|-----------|----------------------|--------------|
| 1 | **Delete the dead decode-strategy museum** (4 strategies + fixed-table builders + copy helpers). Zero-callers verified for consume_first/ultra/packed; turbo is test-only. | `bgzf.rs:185-1789` (238, 511, 1061/1121, 1134, 1369, 1680) | −~1,600 LOC of production file; decode hot path reads as "one production decoder + two routes" instead of a museum; kills the `#[allow(dead_code)]` fog and stale FFI prose living inside those test mods | None at runtime. Delete the turbo-only test blocks (:2822-3377) with them; golden/routing/correctness suites + `mod.rs:903` path unchanged |
| 2 | **Split bgzf.rs → overall `bgzf/` module tree** (mod.rs + multi_member.rs + tests.rs as in §B) once #1 lands | `bgzf.rs:1-184, 1790-2495, 2507-7561` | 2nd-largest file in repo becomes ~900 lines of live BGZF/multi-member code; navigable module boundaries where names match content | Pure moves + re-exports; all 10 internal call sites keep `decompress::bgzf::*`; integration tests unchanged; verify with full `cargo test` |
| 3 | **Extract `GzipHeaderInfo` → `compress/gzip_header.rs`; move rsyncable pair → `compress/rsyncable.rs`; gate the rest of parallel.rs uniformly** | `parallel.rs:58-99, 654-772`; import sites `compress/mod.rs:29, pipelined.rs:31, io.rs:386, 396, 424, 636` | One concept per module: the file named `parallel` stops mixing the production header type, a quarantined C-FFI oracle, and a CLI-gated route | 4-6 import-path edits under doc(hidden) tree; no bytes change; compile-verified by `cargo check --all-features` |
| 4 | **Fix the stale C-FFI decode comments** (dated receipt: production decode is pure-Rust) | `bgzf.rs:3683-3693`; `consume_first_decode.rs:3304-3308`; `scheduler.rs:536-538` | Docs stop contradicting lib.rs:3-5, `mod.rs:315`, and the pure-Rust board claim; auditors stop chasing a phantom libdeflate decompressor | Comments only; zero compile surface |
| 5 | **parallel/pipelined vocabulary unification** (type/function renames only; Gate-4 route strings frozen) | `pipelined.rs:299` (`compress_parallel_pipeline_pure`), `level.rs:425` (`params_parallel`), `bgzf.rs:1889`, glossary source `compress/mod.rs:32-80` | grep "parallel" reaches production code, not the retired oracle; future readers stop triaging two encoders with overlapping names | Moderate: renames are internal (doc(hidden) tree); external greps on `encode-path=` strings must not change (mod.rs:72-77); update `scheduler.rs:536` comment in same commit |
| 6 | **`ChunkJob` struct + single dict-pad helper** replacing `deflate_into`'s 9-positional signature and the 4× duplicated padded-buffer blocks | `deflate/mod.rs:364-374` (sig), `:170-216, :262-301` (dup blocks) | Pad/dictionary correctness exists in exactly one place; the four public wrappers' differences (`is_last`/`sync_flush`/splice-meta) become named, greppable fields | Same-file refactor, 5 call sites + near_opt_flush dispatch closure; byte-identity suites (bitsplice_roundtrip, golden, thread_byte_parity) are the gate — struct is by-ref, no alloc |
| 7 | **`FastParseCfg` bundle for `fast::run`'s 13 positional args** | `parse/mod.rs:521-535, 563-577, 601-629`; target `parse/fast.rs::run` | Adding the next L1 knob stops being a 13-slot positional ceremony with silent pass-swaps; the const-generic strategy stays explicit | One file's dispatch + fast.rs signature; zero-byte-output proven by existing size_invariants/entry_point_identity suites |
| 8 | **`ChunkFillState` struct in `near_optimal::run`** hoisting the ~14 loop-carried locals | `near_optimal.rs:1150-1193` (state), `:1195-1631` (loop) | The flush-overlap feature's shared-state surface ("what does flush k see from fill k?") becomes explicit fields — pre-req hygiene for the stacked box leg the board gates L9/T4 on | HOT parser: mechanical field hoist only, `#[inline]` sites untouched; perf_shape + `l9_t4_chunk_cost_probe` byte-identity probes confirm no drift |
| 9 | **Consolidate ldx/port/legacy vocabulary + fix the dangling doc sentence** | `deflate/mod.rs:507` (orphan sentence), `:566-583` (`legacy_encode` naming), `compress/mod.rs:9-11` glossary, `ldx/mod.rs:1` | A reviewer can trace every board term ("the port", `ldx`, `legacy`, `level_uses_ldx`) to one definition; "legacy" stops mislabeling our own native engine | Comment/census-rename only (`snapshot()` consumers: `tests/one_encode_only.rs`); no output bytes touched |
| 10 | **`#[doc(hidden)]` the measurement modules** `fixtures`/`holdout`, per the crate's own policy line | `lib.rs:97-98` (policy at lib.rs:56-67, 89-95) | rustdoc no longer advertises pin-fixture generators as product API; the measurement-surface convention is uniform | One attribute each; all in-crate/example/test users compile unchanged |

Notes on confidence: all zero-caller claims were grep-verified across `src/`; the bgzf line ranges come from the `#[cfg(test)]`/`mod` boundaries read directly; the pipeline facts (GRID_REF_THREADS=4, feature `near-opt-parallel-flush` DEFAULT OFF, `passes = 2` at L9) were verified in source and match the 2026-09-25 board. I did not run the build/test suites (read-only audit); items 1, 2, 3, 6, 7, 8 should land with the crate's byte-identity suites as their gate, per the repo's own discipline.

## Test-suite audit (agent-34)

completed">All evidence is in. Here is the audit report.

# gzippy test-suite audit — scope: `tests/` + `src/tests/`

Board facts used for dating: `GRID_REF_THREADS=4` landed (`src/compress/pipelined.rs:120,193`), `thread_byte_parity.rs` gates exist, `near-opt-parallel-flush` landed default-off (PR #379), near-opt wall timers `near_opt_fill`/`near_opt_flush` landed (`src/compress/deflate/anatomy_wall.rs:247-252`, wired at `near_optimal.rs:1226,1402,1421,1488,1507`), L9 retune = passes 2 (`src/compress/deflate/level.rs:499-508`).

**Verified live:** `cargo test --test thread_byte_parity` → 3 passed. `cargo test --features near-opt-parallel-flush --test l9_t4_chunk_cost_probe chunks_are_byte_identical_to_the_serial_stream` → 1 passed in 5.7 s.

## STALE findings (dated against the board)

- **S1 — sprint doc contradiction.** `docs/board/sprint-2026-09-25.md:58-62` claims the three `tests/thread_byte_parity.rs` gates are "currently failing as designed". They pass now (`c9fd768c` era tree). The pin landed at `pipelined.rs:193`; the doc's §3 "turn green the day unification lands" is that day.
- **S2 — `src/tests/pure_parallel_encoder.rs:9-13` is self-contradictory.** Header line 4-5 still says the T>1 path is "identical to T1" (pre-`params_parallel` era), while the 2026-09-25 NOTE says cross-thread byte identity is "not yet asserted … this pin's claim is stale until then" — yet `tests/thread_byte_parity.rs:91-114` (green) now asserts T2/T8/T16 digests == T4 at every level. Fix the header; optionally extend `every_thread_count_is_valid_gzip` (`:115-137`) to assert the digests, making the module consistent with v3.1.
- **S3 — probe labels lag the passes-2 retune.** `tests/l9_t4_chunk_cost_probe.rs` header (`:8-16`) was updated, but the test labels were not: `prod_l9t4_depth400` prints "1 production (near-opt@L11, depth400)" missing passes-2, and `l11_alias_check` (`:135`) is no longer an alias (L9 passes 2 vs L11 passes 4 — the header itself says so). Receipts generated by these probes will repeat the stale claim.
- **S4 — stray `#[ignore]` on a non-test helper.** `tests/l9_t4_chunk_cost_probe.rs:113` — `#[ignore]` above `fn matrix_for` is inert.

## Ignore inventory & verdicts (the "ignore-verify" list)

**Not stale — keep ignored:**

| Test | Reason verdict |
|---|---|
| `tests/pipe_parallel.rs:201` `true_pipe_matches_file_arg_at_p4` | Checked against code: `src/compress/io.rs:658-669` still routes FIFO stdin to the whole-buffer T1 branch ignoring `-p`. Live routing bug — reason current. |
| `tests/anatomy_wall.rs:277` `parse_match_covers_every_level_0_through_9` | Not stale: the port levels `{0,2,4,5,8,9}` still have no `parse_match` timers (consistent with `tests/one_encode_only.rs:110` `PORT_EXCEPTIONS={1,3,6,7}` and sprint §2). BUT the reason's premise moved: the near-opt side of lever-0 landed (`anatomy_wall.rs:247-252`), and **no test at all** asserts those new regions reconcile/nonzero — see M4. Partial re-arm (levels {1,3,6,7}) possible now. |
| `tests/rival_flags.rs:334,422,459` | Feature contracts tied to issues #302/#303; unrelated to router/timers. Keep. |
| `tests/cli_dropin.rs:501` | gzip-contract divergence triage. Keep. |
| `tests/size_invariants.rs:244`, `tests/ir_budget.rs:107`, `tests/ir_vs_ldx.rs:340,391` | Pin generators/measurement harnesses. Keep. |
| `src/tests/routing.rs:1276,1337,1475`, `src/tests/diff_ratio.rs:197` | Perf-box gates. Keep. |
| `src/tests/three_oracle_diff.rs:448`, `src/tests/inflate_fuzz_loop.rs:165` | Extended fuzz (CI runs bounded sibling). Keep. |
| `src/tests/latch_interleave.rs:68`, `src/decompress/parallel/fd_vectored_write.rs:626,697,751`, `rpmalloc_alloc.rs` (11×), `chunk_fetcher.rs:3224,3310,3355` | Serial-only process-global counters / slow integrations. Keep. |

## Duplicate coverage (merge/delete)

- **D1 — exact duplicate: `tests/raw_deflate.rs:7` vs `tests/lib_api.rs:400`.** `deflate_encode`/`deflate_decode` are `pub use` aliases of `compress_raw`/`decompress_raw` (`src/lib.rs:295-298`); both tests roundtrip at [1,6,9]. lib_api adds not-gzip (`:413`) and bad-data (`:426`). **Delete** `tests/raw_deflate.rs` (16 lines).
- **D2 — strict subset: `tests/bitsplice_roundtrip.rs:29-64` ⊂ `tests/t4_pins.rs:108-157`.** Same fixtures × levels {1,2,6,9}×threads {1,2,4,8} ⊂ LEVELS 1..12 × {2,4,8,16}, same binary spawn, same two decoders (ours + system gzip); t4_pins adds determinism detects. **Delete** or fold into t4_pins.
- **D3 — two generations of one net: `src/tests/deflate_encoder_foundation.rs` (Increment 1, literals-only) vs `src/tests/deflate_encoder_matches.rs` (Increment 2, same three-oracle + proptest machinery, levels 2-9).** Fold foundation's unique cases or extract the shared three-oracle helper.
- **D4 — decode-thread parity twice: `src/tests/trace_parity.rs:125-183` vs `src/tests/correctness.rs:603,614,631`** (`test_bgzf_t1_through_t8`, `test_multi_member_t1_through_t8`, `test_bgzf_zeros_t1_through_t4`). Same axis (decode at T1..T8 byte-identical). Let trace_parity own the axis; drop the correctness.rs siblings.
- **D5 — `src/tests/golden.rs` is not golden.** Its "golden reference" is generated at runtime from libdeflate (`:12-19`) — i.e. another oracle roundtrip, overlapping `correctness.rs:569-594` (`test_production_decoders_agree_*`). Merge into correctness.rs (or store real goldens if the name should earn its keep).
- **D6 — gzip-format detector tested in three places:** `correctness.rs:250-286`, `routing.rs:455,474`, plus routing assertions in `src/tests/multi_member_chunked.rs`. Consolidate the detector cells.
- **D7 — proptest twins: `src/tests/pure_parallel_encoder.rs:189-234`** (tiny/incompressible-multichunk/cross-boundary, levels {1,6,12}, 3-oracle) vs `tests/deflate_proptest.rs:211-393` (same shape families, broader, incl. arbitrary-bytes). Merge the oracle decode into deflate_proptest's shapes and retire the src-side proptest.
- **Not dupes:** `perf_shape.rs` / `anatomy_pins.rs` / `startup_cost.rs` hold three distinct invariants (exact T1 work volume; T4:T1 ceiling ratios + alloc invariance; 1-byte fixed init cost — the only T>1 pin), each documented in its own header. Keep coverage; optionally share the ~200-line TSV/diff harness repeated across the 8 pin suites.

## Rename

- `tests/t4_pins.rs:109` `t1_is_deterministic_and_every_thread_count_emits_valid_gzip` axis is no longer "T4": it pins every count 2..16 — and the file's header (`:3-21`) now describes the STEP-2 validity bar post-parity-unification. Rename suite intent (e.g. `thread_validity_pins`).
- `tests/l9_t4_chunk_cost_probe.rs:123,135` → `prod_l9t4_depth400_passes2`, `passes4_control` (per S3).
- `tests/anatomy_wall.rs:332` `wall_output_is_absent_from_a_feature_off_style_but_present_here` → its body only checks the stderr lines exist exactly once — the name describes an impossible condition for this feature-on file.
- `src/compress/deflate/level.rs:1165` `t1_vs_parallel_l897_routing_asymmetry_is_deterministic` — "l897" is undecodable; the test's real subject is the L9 passes-2 retune vs L8 alias.

## Internal assertions that smell like refactor-cages

- `src/compress/deflate/level.rs:1194` — `assert!(par.max_search_depth.is_multiple_of(4) || par.strategy != t1.strategy)` is near-vacuous at L9: `par.strategy != t1.strategy` is true by construction (NearOptimal vs Lazy2), so the "x4 depth leak onto the near-opt parser" the guard exists for would pass if reintroduced. Pin the depth directly or drop the arm.
- `src/compress/deflate/level.rs:1197-1220` `strategy_mapping_matches_increment_scope` equality-pins `params(l).strategy` — the same file's own deleted-test lesson (`:1222-1239`: "A knob that is DECLARED FREE TO CHANGE must not be pinned by an equality assertion") argues this will red-light every deliberate routing flip while catching nothing a red flip isn't. Reframe to a documented-receipt style like `one_encode_only.rs` does (routing is census-based there precisely for this reason).

## Missing behavior coverage

- **M1 — the parallel-flush feature has zero CI execution.** `near-opt-parallel-flush` is default-off (`Cargo.toml:183`) and no workflow builds it (grep across `.github/workflows/` — nothing). `Cargo.toml:163-164` claims the byte identity is "pinned by `tests/l9_t4_chunk_cost_probe.rs::parallel_flush_*`" — the gate exists (`tests/l9_t4_chunk_cost_probe.rs:281`, not `#[ignore]`d) and I verified it passes in **5.7 s**, but it compiles to nothing outside the feature. Add one CI line (e.g. to the anatomy job, `ci.yml:414-418`): `cargo test --release --features near-opt-parallel-flush --test l9_t4_chunk_cost_probe chunks_are_byte_identical_to_the_serial_stream`.
- **M2 — the organic veto latch is never exercised.** The chunk-end flip tally → latch at `near_optimal.rs:957-970` only executes with the feature on; every test drives the latch through `set_stale_flag_for_tests` (`tests/l9_t4_chunk_cost_probe.rs:414-422`), so the *tallying* branch (`:962-966`) runs only on the zero side. A crafted all-literals non-first block (or a directly testable tally unit) is the missing half of "flip = audible quarantine".
- **M3 — cross-`-p` digest parity is unpinned at L10-12.** `tests/thread_byte_parity.rs:94` digests only L1..9, while T>1 serves L10-12 through the same thread-free grid + `params_parallel` (`pipelined.rs:604-668`; the grid unit covers 1..=12 at `:65`; `t4_pins.rs:26` roundtrips L10-12 at T2-16). Extend the digest loop (verify the near-opt T>1 output actually ties first).
- **M4 — no anatomy-wall test names the new regions.** `near_opt_fill`/`near_opt_flush` are wired (`near_optimal.rs:1226,1402,1421,1488,1507`) but the only wall conservation test (`tests/anatomy_wall.rs:169`) runs at L1, where both are structurally zero — the exact measurement gap lever-0 exists to close. Cheap addition: a feature-ons assert (nonzero + conserved) at an L9-T4 or L11 shape.

## Keep (contract-carrying, current)

`tests/thread_byte_parity.rs` (verified green), `one_encode_only.rs` (census router guard), `libdeflate_structure_parity.rs`, `entry_point_identity.rs`, `size_invariants.rs`, `perf_shape.rs` + `anatomy_pins.rs` + `startup_cost.rs`, the pin TSV lattice (`fingerprint_suite`, `block_pins`, `smallfile_pins`, `ldx_census`, `divergence_accounting`), `deflate_proptest.rs`, `lib_api.rs`, `failure_ux.rs`, `pipe_parallel.rs` (live tests), `streaming_api_is_honest.rs`, `scheduler_memory_bound.rs`, `ir_budget/ir_vs_ldx`, `symbol_canary`.

Unverified/caveat: D3/D4/D6 consolidation and the M3 L10-12 extension were asserted from reading, not run; the two `cargo test` runs and the io.rs routing check were executed.

## Docs-tree audit (agent-35)

completed">Audit complete. All facts below are dated against the current campaign record (`docs/board/sprint-2026-09-25.md`, the parity design v3.1, the retune-lap record, and trunk code as of `c9fd768c`).

# Docs coherency audit — gzippy docs/

## Baseline: current campaign facts (used to date every finding)

- Thread-free chunk grid: `GRID_REF_THREADS = 4` at `src/compress/pipelined.rs:120` (landed PR #374, commit `a29b87b7`); `target_chunks = GRID_REF_THREADS × cpt` at `pipelined.rs:193` — deliberately **not** the live thread count.
- Parity gates: `tests/thread_byte_parity.rs` — 3 tests (`:63`, `:91`, `:124`). **I ran them: 3 passed, 0 ignored, 0 failed** on this checkout. Green since PR #374 (commit `595f2278`: "thread_byte_parity 3/3 green").
- near-opt-parallel-flush: landed PR #379 (`908f587e`), DEFAULT OFF (`Cargo.toml:183`), adjudication-gated, promotion precondition (zero-flip corpora) recorded in the Cargo.toml comment block.
- L9 retune: passes=2 early-return at `src/compress/deflate/level.rs:505-507`; trunk `a29b87b7` carries it (PR #373).
- Trunk verdict: SHIP (`docs/board/records/2026-09-25-retune-lap/README.md:80-87`); named open cell `pigz:silesia.tar:L9:T4:wall = 1.0822`; the Lazy2-revert leg for L9/T>1 is DEAD by the +2.9% cap measurement (README:16, 92-93).
- Exceptions stand at L1/L3/L6/L7: `src/compress/deflate/mod.rs:587-627` (`!matches!(level, 1 | 3 | 6 | 7)` at `:626`).

## A. Rules stated in multiple copies that drift

### A1. The T(N) parity charter — three mutually inconsistent positions, none of them the landed one

1. **Negative rule (2026-07-28), stated twice inside one file**: `CLAUDE.md:69-74` — "THE ONLY CORRECTNESS BAR … T>1 may emit different bytes than T1. Byte-identity … is never a goal and never a gate" — duplicated verbatim at `CLAUDE.md:98-103` (whole STEP-2 paragraph appears twice, including the "regenerating the cage" receipt parenthetical).
2. **Positive exact-parity plan (2026-09-03)**: `CLAUDE.md:44` "Phase 1 = exact state carry (T>1 == T1 == libdeflate bytes, seam class dies by construction)"; `docs/plan-2026-09-one-encoder.md:39-41` "make T>1 emit the *same bytes*"; `:102-105` "Output of T2/T4/T8/T16 == T1 == libdeflate".
3. **Current enforceable position (2026-09-25)**: `docs/board/parity-unification-design.md:1-4` — "cross-T (N>1) bit-parity + T1 as a distinct, census-tied stream", with v1's T1==T4 digest parity explicitly withdrawn (`:10-24`), now enforced by the green `tests/thread_byte_parity.rs` gates and PR #374.

A cold reader gets all three; only (3) matches the code (T1 whole-buffer = distinct stream class, asserted by `t1_stream_is_a_distinct_class_within_the_size_tie`). Note the drift also lives in **code comments**: `src/compress/mod.rs:69-74` re-quotes the 2026-07-28 retraction and then adds "The T>1 size leg is closed by making SEAMS smaller" — a statement `CLAUDE.md:105-128` says was *measured false* on 2026-08-01, and that the current resolution (pinned grid + T1 distinct class) replaced. That comment is a live regeneration instance of the exact mechanism the project documents.

Related stale sentence: `docs/compressor-architecture.md:8-10` "T>1 emits the T1 bytes by construction; `scripts/campaign/parity-census.sh` is the falsifier" — false twice: T>1 does not emit T1 bytes on this trunk, and the falsifier was replaced by the parity gates.

### A2. The wall bar — charter gloss is stricter than the rule it cites

- `CLAUDE.md:84-86`: "**WALL NEVER LOSES** (a wall regression is a conviction under any circumstances — clauses 3/5/7's cross-layout machinery unchanged)".
- `docs/promotion-rule.md:40-99` (the file it defers to): clause 5 authorizes a **bounded** wall erosion on winning cells (`post_ratio <= min(0.80, 1 - 3*layout_floor)`), i.e. a confirmed wall regression that stays under the margin floor is *not* a conviction. "A wall regression is a conviction under any circumstances" contradicts the clause it cites in the same sentence.
- Third and fourth copies: `docs/plan-2026-09-one-encoder.md:280` ("wall NEVER loses"), `docs/handoff-2026-09-04.md:240`. Fifth copy rides in the instrument: every `try.json` rule string (e.g. `docs/board/records/2026-09-22-ec2-c7a4xl/attempt-2/wave-s2-final/try.json:854`) embeds "(owner directive 2026-09-05: <1% size hit, wall never loses)" — an out-of-repo restatement no doc sweep can reach.

### A3. The size-cap clause — "<1%" verbatim vs "⇐ 1%" codified, and the boundary is unresolved everywhere

Owner verbatim "<1%": `CLAUDE.md:82`, `docs/promotion-rule.md:55`, `docs/plan-2026-09-one-encoder.md:276`, `docs/handoff-2026-09-04.md:238`. Codified "≤1%": `promotion-rule.md:56,119-120`, `CLAUDE.md:86-87`, `plan:280-281`, `docs/board/parity-unification-design.md:23,32`, `docs/board/sprint-2026-09-25.md:112,183`, `docs/board/retune-wall-lap-runbook.md:20,52`. The only place the discrepancy is *named* — owner "< 1%" vs rule "≤ 1%" vs fulcrum code `+1e-12` — is an attack record: `docs/board/attack/redteam/redteam-370-f3406ff.md:231-236` (§3a, LOW, "must be settled in the same turn") — it never was, and the attack records are otherwise read-only history. This is a rule whose edge-case dispute is documented only in a closed packet.

## B. Stale claims (each contradicted by the current facts)

1. **sprint-2026-09-25.md:58-62** — parity gates "currently failing as designed… turn green the day unification lands". Unification (PR-2 grid pin) has landed; gates are green (verified run + commit `595f2278`).
2. **sprint-2026-09-25.md:101,110-122** — the "+23,413 B/8 MiB ≈ +0.29% ⇒ inside the ≤1% authorized size-spend… revert L9/T>1" ship shape with a "projected ~0.6-0.7" pigz wall. `parity-unification-design.md:26-35`convicts that row's arithmetic as double-division and prices the revert at **+2.9%** (10× the cap); the retune-lap record declares the leg DEAD and the measured wall improvement is 1.38 → **1.0822**, not 0.6-0.7. The sprint's "REVISED SHIP SHAPE" and the design doc / runbook assert opposite ship shapes for the same lever.
3. **design doc's own TODO list is stale**: `docs/board/parity-unification-design.md:141-149` "Stale-comment sweeps owed" — item 1 is done (`src/compress/ldx/mod.rs:222-231` now reads "PRODUCTION entry point… routed by `src/compress/deflate/mod.rs`"), item 2's "sprint plan §7" text no longer exists anywhere, item 3 is done (`sprint:11-12` already cites `level.rs:560`). The owed-list survived its own completion.
4. **CLAUDE.md:41-43** — "#363 (`good_match`) and #364 (`len-3`) **retire** the L6/L7 and L3 routing exceptions… L1 is the only remaining exception". Both PRs NO-SHIPPED (git records `821d6ccf`, `de1cbc21`; sprint P1 `:140-149`), and four exceptions stand (`mod.rs:626`). Same wrong claim repeated in `docs/plan-2026-09-one-encoder.md:27-30` and in the supersession banner `docs/encoder-campaign-plan.md:5-6`.
5. **CLAUDE.md:36-37** — "The 55-commit stack `perf/t1-output-cap` (UNMERGED, no PR… being fixed first)" and "a thread-aware chunk grid": both false on trunk (stack landed as #366/#367-era slices per `handoff §3.3`; the grid is thread-free, `pipelined.rs:120,132`).
6. **`docs/production-paths.md:93-149`** — §2 "Compression routing tree" (ISA-L arm at L≤3, "ratio probe on first 64 KiB", libdeflate one-shot vs flate2/zlib-ng two-encoder split) describes the pre-Increment-7 C-FFI routing and its content-detector pick, while the code comment at `src/compress/mod.rs:66-77` says "Increment 7 — the sole production compress path; no C-FFI compressor in the routing graph" and the Gate-4 route const set (`mod.rs:29-60`) contains none of the paths that section names. Worse, `production-paths.md:4-5` demands "Do not trust … CLAUDE.md over this file". §1/§3/§4 (decode) remain consistent with `src/decompress/`; §2 must be regenerated or retracted.
7. **`docs/board/records/2026-09-25-retune-lap/README.md`** — duplicated structure: "## The verdict union" at `:18` *and* `:24`, "## Decision" at `:21` ("(PENDING)") *and* `:80` (SHIP). A fill-in template left in place; the `:18-22` stub contradicts the filled copy and would be read by an artifact-grepping session.
8. **Record-dir collision**: `records/2026-09-25-retune/` (one JSON) vs `records/2026-09-25-retune-lap/` (the adjudication) — near-suffix naming invites citation of the wrong one.
9. **Directory collision + raw artifacts**: `docs/boards/` (plural, two pre-pivot wall-L6T1 `fd8fde5e` census files, Aug 30) coexists with `docs/board/` — a near-missed directory pair holding one raw/manually pre-pivot artifact and the active board.
10. **"Read this first" three-way race**: `docs/handoff-2026-09-04.md:3-4` (and §4.1 read-order at `:150-152`), `CLAUDE.md:12-16` ("plan doc … read it FIRST"), and `plan-2026-09-one-encoder.md` itself — none of them points at the actually-current state (`docs/board/sprint-2026-09-25.md` + parity design + runbook + retune-lap record). The plan doc's newest fact is 2026-09-05 (`:273-299`); it is the declared live-state but is three sprint-weeks stale.

## C. The regeneration mechanism — and why the current copy-propagation rule cannot work

`CLAUDE.md:425-435` enshrines the process: "A retraction must reach EVERY copy, root and leaf… a claim that has regenerated once will regenerate again." This audit found the mechanism failing on the **(new) instances** above: the keystone parity paragraph now duplicates within CLAUDE.md itself; a code comment in `src/compress/mod.rs:66-74` carries both a rule quote and a falsified closure claim; the retune-lap README duplicates its own verdict headings; the size cap rests on ~10 prose restatements with no settled boundary; and one doc (`production-paths.md`) claims authority over the charter while describing three routers of code ago. The pattern is consistent: **rules are restated in prose wherever they are relevant, and every restatement becomes a second source with its own drift clock.**

## D. Board file overlap — classification

**Active (keep as working docs):** `docs/board/sprint-2026-09-25.md` · `docs/board/parity-unification-design.md` · `docs/board/retune-wall-lap-runbook.md` (stacked leg pending) · `docs/promotion-rule.md` (the one live rule file) · `CLAUDE.md` · `docs/branch-garden-manifest.md` (batches 2-4 still awaiting owner ack).

**Should be CLOSED as records (evidence frozen, keep for receipts only):**
- Already closed by banner but pointed at a now-stale plan doc: `fixed-vs-pernode.md`, `l1-next-lever.md`, `residual-after-227.md`, `verified-board-120bfa9c.md`, `wall-census-complete.md`, `l1-hash3-is-the-whole-gap.md`, `l1-is-algorithmic.md`, `l2-component-map.md`, `l2-instruction-attribution.md`, `l3-vendor-diff.md` — add the same banner to the last five (they carry none) and update all banners to point past the plan doc.
- Attack packets `docs/board/attack/**` (Sep-5/Sep-25 verdict playbooks, red-team, per-class analyses) — closed packets; add a `README.md` manifest saying so.
- `docs/handoff-2026-09-04.md` — session record; its §4 queue has all executed or superseded.
- Pre-pivot strategy docs: `encoder-campaign-plan.md` (bannered), `target-encoder-and-gap-analysis.md` (**no banner exists** — it is the 135 KB "target shape" doc whose premise the owner's 2026-08-23 port election reversed), `compressor-architecture.md`, `encoder-architecture.md` (both partially bannered; their T1/T>1 byte claims are now wrong, see A1).
- Decode-era docs (`parallel-decode-architecture.md`, `structural-gap-rapidgzip.md`, `production-decode-callgraph.md`) — aligned with CLAUDE.md's "DECOMPRESSION IS DONE AND WON" wall; mark archival-but-reference.
- `docs/boards/` (plural) contents → into `records/`.

## E. Consolidation plan (the deliverable)

**Single-source-of-truth layout — each rule stated once, every other file cites it:**

1. **`docs/promotion-rule.md` = the only rule file.** Absorb into it, as one paragraph each, the three drifted rules: the cap (decide the boundary once — owner was "<1%", rule says "≤1%", fulcrum enforces "+1e-12 slack"; red-team `§3a` proposed a half-open interval plus boundary pins — adopt that in prose), the wall bar (replace CLAUDE.md's "conviction under any circumstances" gloss with clause 5's actual semantics), and the parity charter (cross-T parity is a hard gate; T1-vs-T(N) divergence is a declared structural property with the 0.25%/seam-128B tie, per `tests/thread_byte_parity.rs:120-124`). CLAUDE.md keeps process rules and *cites* the rule file; deletions of those sentences from CLAUDE.md/plan/handoff are then edits to a pointer, not to the rule.
2. **`docs/board/sprint-2026-09-25.md` = the only state file** (append-only RESULT blocks; results land as dated sections, headers get STALE-with-pointer markers instead of rewrites — the sprint's Probe-1b paragraph is the example where a stale "REVISED SHIP SHAPE" still argues a dead lever). Replace the "live state = plan doc" pointer in `CLAUDE.md:14` which now follows sprint docs as top of the read order and the plan doc gets a closed-state banner.
3. **CLAUDE.md edits this plan implies:** delete the duplicated STEP-2 paragraph (`:94-103`); fix `:41-43` (four standing exceptions; #363/#364 NO-SHIP with pointer to `sprint:140-149`); fix `:36-37` (stack merged; thread-free grid); scope `:84-85`'s wall parenthetical to clause-5 semantics; re-point `:12-16`.
4. **Code-comment rule (prevention):** code comments *link* to the owning pair (dash: `tests/thread_byte_parity.rs`, `parity-unification-design.md`) instead of paraphrasing rules — the `src/compress/mod.rs:66-74` block is the wrong pattern; rewrite it as "parity contract lives in tests/thread_byte_parity + docs/board/parity-unification-design.md; T>1 bytes are grid-determined (GRID_REF_THREADS), T1 whole-buffer distinct."
5. **Directory hygiene:** move `docs/boards/*` into `docs/board/records/2026-08-XX-wall-L6T1-fd8fde5e/`; fold `records/2026-09-25-retune/size-census.json` into `records/2026-09-25-retune-lap/` (retire the bare-name dir); add `docs/board/records/README.md` declaring the RECORDS contract (append-only, no prose, cite as point-in-time).
6. **Archive markers, no deletion:** pre-pivot strategy docs (`encoder-campaign-plan.md`, `target-encoder-and-gap-analysis.md`, `compressor-architecture.md`, `encoder-architecture.md`) get a top-level `<!-- ARCHIVE — receipts only;inäkten live numbers: docs/board/…-->` banner (target doc needs its first banner); decode/architecture docs get an `ARCHIVE (decode, closed 2026-07-18)` marker; `attack/**` gets `attack/README.md` marked CLOSED.
7. **`production-paths.md`:** re-scope header to decompression only (`§1/§3/§4`) and delete or regenerate §2 from `src/compress/mod.rs` route::consts — until regenerated, the "do not trust CLAUDE.md over this file" sentence must be dropped, because the file is currently the stalest map in the tree.

Not verified beyond reading: `gzippy-tuning.7`'s "T1 decompress near-parity" row (`:136`) is decompression-side and outside the encoder campaign; no drift action proposed there. I did not run fulcrum (no binary locally, per `handoff §5`); all parity-test claims were verified by direct local `cargo test --test thread_byte_parity` run.

## Comment findings (agent-30)

The actionable core is executed by agent-31 on `audit/comment-hygiene` (see the header). Findings kept for a later prose pass: split_recall.rs:91, probe.rs:165, block_cost_probe.rs:27-31, parse/mod.rs:2005 — each sits mid-receipt so a delete-only pass was correctly refused.
