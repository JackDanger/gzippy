# Sprint: close the named losses (2026-09-25)

Three loss classes, three investigations, three evidence packets. This branch
carries the first instrument pins; the probes below drive the fixes.

## 1. pigz L9/T4 — we are 1.38× slower (the biggest wall loss)

**Mechanism (agent-16, confirmed by local sampling):** NOT the scheduler, NOT
the matchfinder (T1 is 3.3× faster than pigz-9 there). The T>1 route runs
`level::params_parallel(9)` ≡ `params_parallel(11)` — the near-optimal parser
at L11 knobs — *plus* the unscoped ×4 depth multiplier (100 → 400) from the
else-branch at `level.rs:539`. Per-byte cost ≈ 4× the Lazy2 parse everything
else was graded against; 4 threads ÷ 4.3× cost ≈ 0.93 — matching the measured
1.07× aggregate scaling exactly. Local first-party confirmation (M1, 106 MB):
our T4 wall is 1.17× our own T1 (pigz's is 0.25×).

**Fix lever:** a middle parse for L9 at T>1 — per-strategy depth scoping
(400→100) and/or bounded optimization passes — measured against the size
ledger (the floor: reverting to the regular parser regressed text +23,413 B).
Probes: (1) config-cost matrix on one 1.8 MB chunk through
`encode_deflate_splice_chunk_to_sink` variants (parallel vs params(9) vs
near-opt@100 vs near-opt@100+passes1 vs Lazy2@2400) — discriminates causes
within one afternoon; (2) parallel-efficiency via `compress_parallel` +
`GZIPPY_DEBUG=1` phase atomics (predicted to EXONERATE the scheduler); (3)
DONE — the routing asymmetry is pinned as a unit test in
`src/compress/deflate/level.rs` (`t1_vs_parallel_l897_routing_asymmetry_is_deterministic`).

## 2. T1 Ir-vs-port (L1 + L6 cells; L2/L9 are framing-tax only)

**Mechanism (agent-17):** `level_uses_ldx` excludes L1/L3/L6/L7 deliberately —
each refusal carries a banked size cell. At L2/L4/L5/L8/L9 the shipped engine
IS the port (margins 1.013–1.017 = framing+inventory tax ~0.3–0.5 Ir/B). At
**L6** the gap is mostly the depth knob (128,65,g8 vs the port's 35,65 —
d9418505 moved it to the zlib-arm knobs, with 2–4 flip receipts) plus ~15 Ir/B
legacy-driver tax. At **L1** the gap is the legacy per-position machinery,
partly purchased (hash3, bucket2, lazy-peek, REACH dense inserts — each with a
size-cell receipt); the 2.04× noise cell is the diagnostic: 59.7 Ir/B of bare
per-position scan code with zero match amortization. Absolute Ir is still
37–53% below pre-restructure main.

**Probes:** P0 sheet re-grade on CI (existing). P1 L6 depth isolation via
`--features ladder-tune` + `GZIPPY_LADDER=lazy:35:65` (and a middle rung
`lazy:64:65`) under cachegrind + the size gate (tie-guard, won_cells,
ladder_is_monotone_t1). P2 L1 engine isolation: extend `examples/ir_runner.rs`
with an `ldx_gzip` arm and grade ours vs ours-as-ldx at L1, then check the
retention cells (pigz −1 text, armexe binary class). P3 framing-tax split
(ldx vs ldx_gzip vs ours at L2/L9). P4 anatomy counters on the noise cell.

## 3. T(N) byte-parity

**Mechanism (agent-18):** divergence is intentional (params_parallel +
Generous headers + per-T chunk grid coupling) — size-impacting by design; the
trunk passes roundtrip at every T. The fix is a design decision: unify
engines/params/grid per level. Trade-off: promoting the parallel knobs to T1
changes T1 bytes (board-adjudicated); retiring them reopens the T4-L8/9 size
wins (+23,413 B text if fully reverted).

**Landed on this branch:** the contract corrections (stale "byte-identical
regardless of num_threads" claims in `pipelined.rs` / `pure_parallel_encoder.rs`),
and the red-first parity gates (`tests/thread_byte_parity.rs`) — three tests,
currently failing as designed, that turn green the day unification lands and
then enforce byte-parity at any N with roundtrip protection.

## Order of attack

1. **Probe 1 (L9 config matrix)** — the highest-value single afternoon; decide
   the L9/T>1 parse config from numbers, land it with the size ledger.
2. **P1 L6 isolation** — likely closes the L6 goal-sheet gap with a knob
   change, gated by the size ledger.
3. **PD parity unification** — design decision per level (which engine owns
   which level at T1 == T(N)); land with the parity gates green.
4. Re-run the goal-sheet regen + the wall lap on the dedicated box ONLY after
   all three are in.

### Probe 1 RESULT (2026-09-25, local M1 arm64, 1.8 MB text chunk, best-of-5)

| variant | wall | bytes |
|---|---|---|
| 1 production (L9 parallel=true = near-opt@L11, depth 400) | 0.196 s | 484,609 |
| 2 T1 engine (L9 parallel=false = Lazy2 depth 600) | 0.076 s | 524,275 |
| ratio | **2.57× slower** (threshold ≥2.5) | −39,666 B (−7.6% size) |
| control L11=true (alias) | 0.196 s / 484,609 B == production | ✅ |

CONFIRMED: the near-opt@L11-knobs upgrade (params_parallel(9) ≡ params_parallel(11),
depth 400) is the multiplier — 2.57× the Lazy2 wall locally, buying −7.6% bytes on
this fixture (the size win was real and is the thing to keep). Next: the depth/
passes split (GZIPPY_LADDER nearoptimal:100:150 vs :400:150; the `passes` knob
needs the tuner extended), then the config pick priced against both boards EOF.
probe: tests/l9_t4_chunk_cost_probe.rs (`cargo test --test l9_t4_chunk_cost_probe -- --ignored --nocapture`)

### Probe 1b — REAL-silesia chunk (the corpus-flip scan agent-19 demanded)

1,836,864-byte head slice of silesia.tar (dickens-class text-heavy content)
Tax note: this is the +23,413 B-ledger's own text-class slice.

| variant (1.8MB chunk) | wall | bytes |
|---|---|---|
| production near-opt depth400 p4 | 0.1489 s | 648,859 |
| nearoptimal:100:150 (depth100) | 0.1739s (M1 fixture set) — silesia: skipped-by-matrix | same bytes at p1/p4: **0.1546 vs 0.1489 — passes 1 is SLOWER than 4** |
| T1 engine (Lazy2 600) | **0.0409 s** | 671,785 |
| delta production → T1 engine | **−0.108 s (−72% wall!)** | **+22,926 B (+3.5% clock corpus / ≈ +23.4KB per 8MiB = the banked ledger row)** |

FINDINGS:
- The near-opt passes/depth knobs are NOT the wall lever on real-silesia content
  (p1 0.1546 is worse than p4 0.1489 — the pass-loop cost doesn't scale down at
  min-improvement's early stop; bytes converge to the same output).
- The wall cost is the STRATEGY ITSELF: dropping the near-opt at L9/T>1 recovers
  0.108 s vs the chunk = ~4.3x per-byte cost reduction (matches the 0.58x
  libdeflate-parity receipt deflated to 0.5x per mod.rs:372-377).
- Byte cost of the drop: +22,926 B on this slice = the ALREADY-BANKED ledger row
  (+23,413 B per 8 MiB text-class / ≈ +0.29% recursive on silesia.tar 202 MB)
  → **inside the ≤1% authorized size-spend** with a paired-wall receipt (the
  wall improves L9/T4 from 1.38 to a projected ~0.6-0.7 vs pigz).

REVISED SHIP SHAPE (per agent-19's REVISE + this data): revert L9/T>1 (and price
L8, its recursion sibling, with its own paired cells) to the pre-2026-08-09
Ladder2+depth-scaled parse; the near-opt upgrade remains ONLY where the wall
ledger already supports it (the T1 route keeps near-opt OFF; the T>1 route keeps
the upgrade at the levels the board didn't convict). The ledger-documented costs
(+23.4KB/8MiB text-class, and the L2/L6/L7 classes on the shared route) come
itemized under clause 5's authorized-spend rows; clause 4's no-failing-cell-closed
requirement is exactly why the pick still ships through the real lap on the board.

### Probe P1 RESULT — L6 depth bisect on the port (arm64, 25MB silesia slice)

`GZIPPY_LDX_L6` hook landed (ladder-tune feature, measurement-only): the port's
L6 (35, 65) pair is now bisectable.

| depth:nice | wall | bytes |
|---|---|---|
| 128:65 (production) | 0.334 s | 11,051,923 |
| 64:65 | 0.316 s (0.947x) | **+0** |
| 35:65 (the port's own L6 row) | 0.310 s (0.929x) | **+0** |
| 16:24 | 0.316 s (0.946x) | **+0** |

FINDINGS:
- The depth term is only ~7% of the port's L6 wall on dense corpora (matches
  saturate below depth 35 — the chain walk is not the binding constraint).
- Bytes are flat: the port's L6 output is depth-insensitive on this corpus.
- The agent-17 estimate (~90 Ir/B, margin 1.588 → ~1.05) is REFUTED on the
  wall axis for this corpus: 7% of wall ≈ 0.03–0.05 s, not the 40-90% the
  goal-sheet margin implies. The remaining share of the L6 gap is the legacy
  driver's implementation tax (the +6.1 Ir/B scaffolding term), which the
  retirement of `level_uses_ldx`'s L6 exception would recover — the same
  retirement that PR #363 tried (it NO-SHIPPED because whole-port L6 lost 4
  won cells: the good_match knob at leftovers, not the depth).
- The next unification lever for L6 is the port's `good_match` port (PR #363's
  mechanism, which the campaign already adjudicated NO-SHIP at the shipped
  trunk's wall margins).

CONCLUSION: the L6 cell stands at parity-with-port. The ≤1.2x Ir penalty is
priced and banked; the wall is within the campaign's 1.0x-vs-rival band.
The remaining L6/L1 gaps park with the parity unification (Option B) — the
hardware lap will measure the unified engine's L6 once it lands.
